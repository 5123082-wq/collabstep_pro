# Решение ошибки webpack chunk

## ✅ Выполнено

1. ✅ Остановлены все процессы `next dev` (было запущено 2 процесса)
2. ✅ Очищена директория `.next`
3. ✅ Очищён кэш `node_modules/.cache`

## 🔧 Что делать дальше

### Просто перезапустите dev сервер:

```bash
cd apps/web
pnpm dev
```

Ошибка должна исчезнуть после перезапуска, так как кэш очищен.

## 📝 Объяснение проблемы

Ошибка `Cannot find module './2982.js'` возникает когда:
- Webpack chunks изменились после запуска dev сервера
- Кэш Next.js устарел или повреждён
- Несколько процессов `next dev` работают одновременно

После очистки кэша и перезапуска всё должно работать.

---

**Если ошибка сохраняется после перезапуска**, см. `FIX_WEBPACK_CHUNK_ERROR.md` для дополнительных шагов.

