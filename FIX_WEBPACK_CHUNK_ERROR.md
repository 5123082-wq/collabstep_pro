# Решение ошибки "Cannot find module './2982.js'"

## Проблема
Next.js не может найти webpack chunk файл `2982.js`. Это происходит из-за устаревшего или повреждённого кэша.

## ✅ Выполненные действия

1. ✅ Остановлены все процессы `next dev`
2. ✅ Очищена директория `.next`
3. ✅ Очищён кэш `node_modules/.cache`

## 📋 Следующие шаги

### Вариант 1: Перезапуск dev сервера (рекомендуется)

```bash
# 1. Убедитесь, что все процессы остановлены
pnpm kill:ports  # или вручную: pkill -f "next dev"

# 2. Перезапустите dev сервер
cd apps/web
pnpm dev
```

### Вариант 2: Полная очистка и пересборка

Если ошибка сохраняется:

```bash
# 1. Остановить все процессы
pkill -f "next dev"

# 2. Полная очистка
cd apps/web
rm -rf .next
rm -rf node_modules/.cache

# 3. Пересобрать (опционально)
pnpm build

# 4. Запустить dev сервер
pnpm dev
```

### Вариант 3: Если проблема сохраняется

Иногда помогает удаление lock файла и переустановка:

```bash
# ВНИМАНИЕ: Это переустановит все зависимости
cd apps/web
rm -rf .next node_modules/.cache
cd ../..
rm pnpm-lock.yaml
pnpm install
cd apps/web
pnpm dev
```

## 🔍 Проверка

После перезапуска:
1. Откройте браузер
2. Перейдите на `http://localhost:3000`
3. Проверьте консоль браузера на наличие ошибок
4. Попробуйте открыть `/app/dashboard`

## 💡 Причина ошибки

Ошибка возникает когда:
- Webpack chunks были изменены после запуска dev сервера
- Несколько процессов `next dev` работают одновременно
- Кэш `.next` повреждён или устарел
- Динамические импорты изменились, но кэш не обновлён

## 📝 Примечание

Если ошибка появляется только для dashboard, возможно проблема в динамическом импорте:
```tsx
const DashboardPageContent = dynamic(() => import('./_wip/dashboard-page'), { ssr: false });
```

Убедитесь, что файл `_wip/dashboard-page.tsx` существует и корректно экспортирует компонент.

