# 🤖 Подробная инструкция по подключению AI ассистентов

## 📋 Содержание

1. [Получение API ключа OpenAI](#1-получение-api-ключа-openai)
2. [Настройка биллинга](#2-настройка-биллинга)
3. [Создание файла конфигурации](#3-создание-файла-конфигурации)
4. [Добавление API ключа](#4-добавление-api-ключа)
5. [Запуск приложения](#5-запуск-приложения)
6. [Проверка работы](#6-проверка-работы)
7. [Решение проблем](#7-решение-проблем)

---

## 1. Получение API ключа OpenAI

### Шаг 1.1: Регистрация на OpenAI

1. Откройте в браузере: **https://platform.openai.com/signup**
2. Зарегистрируйтесь или войдите в существующий аккаунт
3. Подтвердите email адрес (проверьте почту)

### Шаг 1.2: Создание API ключа

1. После входа перейдите в раздел **API keys**:  
   **https://platform.openai.com/api-keys**

2. Нажмите кнопку **"+ Create new secret key"** (Создать новый секретный ключ)

3. Введите название ключа (например: `CollabStep Development`)

4. **⚠️ ВАЖНО:** Скопируйте ключ **немедленно**! Он показывается только один раз и больше не будет доступен.

5. Сохраните ключ в безопасном месте (менеджер паролей, защищённый файл)

**Формат ключа:** `sk-proj-XXXXXXXXXXXXXXXXXXXXXXXXXXXX` или `sk-XXXXXXXXXXXXXXXXXXXXXXXXXXXX`

---

## 2. Настройка биллинга

⚠️ **КРИТИЧЕСКИ ВАЖНО:** Без настройки оплаты API не будет работать!

### Шаг 2.1: Добавление платёжного метода

1. Перейдите в раздел **Billing** (Биллинг):  
   **https://platform.openai.com/account/billing/overview**

2. Нажмите **"Add payment method"** (Добавить способ оплаты)

3. Добавьте кредитную карту или другой платёжный метод

4. Подтвердите платёжный метод

### Шаг 2.2: Установка лимита расходов

1. В том же разделе Billing найдите **"Usage limits"** (Лимиты использования)

2. Установите **Hard limit** (жёсткий лимит) - например, **$5-10** для начала

3. Это защитит вас от неожиданных расходов - API перестанет работать при достижении лимита

💡 **Рекомендация:** Начните с $5-10 для тестирования. Средняя стоимость одной операции ~$0.001

---

## 3. Создание файла конфигурации

### Шаг 3.1: Переход в директорию проекта

Откройте терминал и перейдите в директорию веб-приложения:

```bash
cd apps/web
```

### Шаг 3.2: Создание файла .env.local

Создайте файл `.env.local` в директории `apps/web/`:

**На macOS/Linux:**

```bash
touch .env.local
```

**На Windows (PowerShell):**

```powershell
New-Item -Path .env.local -ItemType File
```

**Или через любой текстовый редактор:**

- Создайте новый файл с именем `.env.local` в папке `apps/web/`

---

## 4. Добавление API ключа

### Шаг 4.1: Открытие файла .env.local

Откройте файл `.env.local` в любом текстовом редакторе (VS Code, Notepad++, Sublime Text и т.д.)

### Шаг 4.2: Добавление конфигурации

Скопируйте и вставьте следующую конфигурацию в файл `.env.local`:

```bash
# ============================================
# OpenAI API Configuration
# ============================================

# Ваш API ключ (ОБЯЗАТЕЛЬНО)
# Замените "sk-proj-ваш-реальный-ключ-здесь" на ваш реальный ключ
OPENAI_API_KEY=sk-proj-ваш-реальный-ключ-здесь

# Включение AI функциональности (ОБЯЗАТЕЛЬНО)
NEXT_PUBLIC_FEATURE_AI_V1=true
FEATURE_AI_V1=true

# ============================================
# Опциональные настройки (можно оставить по умолчанию)
# ============================================

# Модель OpenAI (по умолчанию: gpt-3.5-turbo)
# Варианты: gpt-3.5-turbo, gpt-4, gpt-4-turbo
OPENAI_MODEL=gpt-3.5-turbo

# Максимальное количество токенов в ответе (по умолчанию: 1000)
OPENAI_MAX_TOKENS=1000

# Temperature - контроль случайности (0-2, по умолчанию: 0.7)
# 0 = детерминированный, 2 = очень креативный
OPENAI_TEMPERATURE=0.7
```

### Шаг 4.3: Замена ключа на реальный

**Замените** `sk-proj-ваш-реальный-ключ-здесь` на ваш реальный API ключ, который вы скопировали на шаге 1.2.

**Пример:**

```bash
OPENAI_API_KEY=sk-proj-abc123def456ghi789jkl012mno345pqr678stu901vwx234yz
```

⚠️ **ВАЖНО:**

- Не добавляйте пробелы вокруг знака `=`
- Не используйте кавычки вокруг ключа
- Убедитесь, что ключ скопирован полностью

### Шаг 4.4: Сохранение файла

Сохраните файл `.env.local` (Ctrl+S или Cmd+S)

---

## 5. Запуск приложения

### Шаг 5.1: Установка зависимостей (если ещё не установлены)

Если вы только что клонировали проект или зависимости не установлены:

```bash
# Из корня проекта
cd ../..
pnpm install
```

### Шаг 5.2: Запуск сервера разработки

**Вариант 1: Простой запуск (без WebSocket) - рекомендуется для начала**

```bash
# Из корня проекта
pnpm run dev:simple
```

**Вариант 2: Полный запуск (с WebSocket для real-time функций)**

```bash
# Из корня проекта
pnpm run dev:full
```

**Вариант 3: Стандартный запуск**

```bash
# Из корня проекта
pnpm dev
```

### Шаг 5.3: Ожидание запуска

Дождитесь сообщения в терминале:

```
✓ Ready in 2.5s
○ Compiling / ...
```

Когда увидите:

```
▲ Next.js 14.x.x
- Local:        http://localhost:3000
```

Сервер запущен! 🎉

---

## 6. Проверка работы

### Тест 1: Генерация описания задачи

1. Откройте браузер и перейдите: **http://localhost:3000**

2. Войдите в систему (используйте демо-аккаунт или свой)

3. Откройте любой проект

4. Нажмите **"Создать задачу"**

5. Введите название задачи, например: `Создать REST API для пользователей`

6. Нажмите кнопку **"✨ Сгенерировать описание"** (должна быть видна, если AI настроен)

7. Подождите 2-5 секунд

8. **Ожидаемый результат:** Поле описания автоматически заполнится структурированным текстом

✅ **Если описание сгенерировалось - AI работает!**

### Тест 2: Проверка в консоли браузера

1. Откройте DevTools (F12 или Cmd+Option+I на Mac)

2. Перейдите на вкладку **Console**

3. Не должно быть ошибок типа:
   - ❌ `"OPENAI_API_KEY is not set"`
   - ❌ `"AI service is not configured"`
   - ❌ `"Invalid API key"`

4. Должны быть успешные логи:
   - ✅ `[AI Audit] { success: true, ... }`

### Тест 3: AI-агенты в проекте

1. Откройте проект

2. Перейдите на вкладку **"AI-агенты"** (если доступна)

3. Нажмите **"Добавить агента"**

4. Выберите **"AI Помощник"**

5. **Ожидаемый результат:** Агент должен добавиться без ошибок

### Тест 4: Чат с AI

1. В проекте откройте вкладку **"Чат"**

2. Напишите: `@ai-assistant привет`

3. Отправьте сообщение

4. **Ожидаемый результат:** AI-агент ответит в течение 5-10 секунд

---

## 7. Решение проблем

### Проблема: "OPENAI_API_KEY is not set"

**Симптомы:**

- В консоли браузера ошибка: `OPENAI_API_KEY is not set`
- Кнопка "Сгенерировать описание" не работает или не видна

**Решение:**

1. **Проверьте расположение файла:**
   - Файл должен быть в `apps/web/.env.local` (не в корне проекта!)
   - Проверьте путь: `apps/web/.env.local`

2. **Проверьте содержимое файла:**

   ```bash
   cd apps/web
   cat .env.local
   ```

   Должна быть строка: `OPENAI_API_KEY=sk-proj-...`

3. **Проверьте формат ключа:**
   - Ключ должен начинаться с `sk-proj-` или `sk-`
   - Не должно быть пробелов вокруг `=`
   - Не должно быть кавычек

4. **Перезапустите сервер:**

   ```bash
   # Остановите сервер (Ctrl+C)
   # Запустите заново:
   pnpm dev
   ```

5. **Очистите кэш Next.js:**
   ```bash
   cd apps/web
   rm -rf .next
   pnpm dev
   ```

---

### Проблема: "Invalid API key"

**Симптомы:**

- Ошибка: `Invalid OpenAI API key`
- Ошибка 401 в консоли

**Решение:**

1. **Проверьте ключ на OpenAI:**
   - Откройте: https://platform.openai.com/api-keys
   - Убедитесь, что ключ активен

2. **Создайте новый ключ:**
   - Если старый не работает, создайте новый
   - Обновите `.env.local` с новым ключом

3. **Проверьте пробелы:**
   - Убедитесь, что нет лишних пробелов в `.env.local`
   - Формат: `OPENAI_API_KEY=sk-proj-...` (без пробелов)

4. **Перезапустите сервер**

---

### Проблема: "Rate limit exceeded"

**Симптомы:**

- Ошибка: `OpenAI API rate limit exceeded`
- Ошибка 429 в консоли

**Решение:**

1. **Подождите 1 минуту** - лимит сбрасывается

2. **Проверьте использование:**
   - Откройте: https://platform.openai.com/usage
   - Посмотрите количество запросов

3. **Увеличьте лимит в настройках OpenAI:**
   - Перейдите в настройки аккаунта
   - Увеличьте rate limit

---

### Проблема: "Insufficient quota"

**Симптомы:**

- Ошибка: `Insufficient quota`
- API не отвечает

**Решение:**

1. **Добавьте баланс:**
   - Откройте: https://platform.openai.com/account/billing/overview
   - Добавьте средства на счёт

2. **Проверьте платёжный метод:**
   - Убедитесь, что карта активна
   - Проверьте, что нет проблем с оплатой

3. **Для нового аккаунта:**
   - Может потребоваться первый платёж ($5+)
   - Проверьте email от OpenAI

---

### Проблема: Медленные ответы (>10 секунд)

**Симптомы:**

- AI отвечает очень долго
- Таймауты

**Решение:**

1. **Переключитесь на более быструю модель:**

   ```bash
   # В .env.local измените:
   OPENAI_MODEL=gpt-3.5-turbo
   ```

2. **Уменьшите MAX_TOKENS:**

   ```bash
   OPENAI_MAX_TOKENS=500
   ```

3. **Проверьте интернет соединение**

4. **Проверьте статус OpenAI:**
   - Откройте: https://status.openai.com/
   - Убедитесь, что сервис работает

---

### Проблема: Кнопка "Сгенерировать описание" не видна

**Симптомы:**

- Кнопка AI не отображается в интерфейсе

**Решение:**

1. **Проверьте feature flags:**

   ```bash
   # В .env.local должны быть:
   NEXT_PUBLIC_FEATURE_AI_V1=true
   FEATURE_AI_V1=true
   ```

2. **Перезапустите сервер** после изменения `.env.local`

3. **Очистите кэш браузера:**
   - Ctrl+Shift+R (Windows/Linux)
   - Cmd+Shift+R (Mac)

---

### Проблема: Сервер не запускается

**Симптомы:**

- Ошибки при запуске `pnpm dev`
- Порт 3000 занят

**Решение:**

1. **Остановите процессы на портах:**

   ```bash
   pnpm run kill:ports
   ```

2. **Проверьте, что порт свободен:**

   ```bash
   lsof -i :3000
   ```

3. **Переустановите зависимости:**
   ```bash
   rm -rf node_modules apps/*/node_modules
   pnpm install
   ```

---

## 📊 Дополнительная информация

### Ожидаемые затраты

При использовании `gpt-3.5-turbo`:

| Операция                  | Стоимость |
| ------------------------- | --------- |
| Генерация описания задачи | ~$0.001   |
| Суммирование комментариев | ~$0.0012  |
| Чат с AI (1 сообщение)    | ~$0.0008  |
| AI-агент ответ            | ~$0.0004  |

**Примерный бюджет:** 100 операций ≈ $0.10

### Мониторинг использования

1. Откройте: **https://platform.openai.com/usage**
2. Просмотрите:
   - Текущие расходы
   - Использование токенов
   - Количество запросов

### Безопасность

⚠️ **НИКОГДА не делайте:**

- ❌ Не коммитьте `.env.local` в git
- ❌ Не делитесь API ключом публично
- ❌ Не используйте production ключ для разработки

✅ **ВСЕГДА делайте:**

- ✅ Храните ключи в `.env.local` (файл игнорируется git)
- ✅ Используйте разные ключи для dev/staging/prod
- ✅ Ротируйте ключи раз в 3-6 месяцев

---

## 📚 Полезные ссылки

- **OpenAI Dashboard**: https://platform.openai.com/
- **API Keys**: https://platform.openai.com/api-keys
- **Usage**: https://platform.openai.com/usage
- **Billing**: https://platform.openai.com/account/billing/overview
- **Status**: https://status.openai.com/
- **Documentation**: https://platform.openai.com/docs

---

## ✅ Чек-лист настройки

Отметьте выполненные шаги:

- [ ] Зарегистрирован на OpenAI
- [ ] Создан API ключ
- [ ] Настроен биллинг (добавлена карта)
- [ ] Установлен лимит расходов ($5-10)
- [ ] Создан файл `apps/web/.env.local`
- [ ] Добавлен `OPENAI_API_KEY` в `.env.local`
- [ ] Добавлены `NEXT_PUBLIC_FEATURE_AI_V1=true` и `FEATURE_AI_V1=true`
- [ ] Сервер запущен (`pnpm dev`)
- [ ] Протестирована генерация описания задачи
- [ ] Проверена консоль браузера (нет ошибок)

---

## 🎉 Готово!

Если все шаги выполнены и тесты прошли успешно - AI ассистенты подключены и готовы к работе!

**Что дальше:**

- Используйте AI для генерации описаний задач
- Добавляйте AI-агентов в проекты
- Используйте чат с AI для помощи в управлении проектами
- Экспериментируйте с разными моделями (gpt-3.5-turbo, gpt-4)

---

**Версия:** 1.0  
**Последнее обновление:** 2025-01-27  
**Статус:** ✅ Готово к использованию
