# Cursor Rules для Collabverse

> Эти правила описывают, как работать с репозиторием Collabverse в Cursor: стек, кодстайл, CI и использование Context7 для документации.

## 🏗️ Архитектура проекта

Это монорепозиторий на pnpm с двумя основными приложениями:
- `apps/web` — Next.js 14 (App Router) фронтенд
- `apps/api` — TypeScript модуль для backend логики (экспорт через `src/index.ts`)

### Структура
- Компоненты: `apps/web/components/`
- Страницы: `apps/web/app/` (App Router)
- API логика: `apps/api/src/`
- Документация: `docs/`
- Конфигурация: `config/`

## 🛠️ Технологический стек

- **Frontend**: Next.js 14, React 18, TypeScript 5.5
- **Styling**: Tailwind CSS с кастомными design tokens
- **State**: Zustand для локального состояния
- **Validation**: Zod для схем валидации
- **Database**: Drizzle ORM с Vercel Postgres
- **Auth**: NextAuth.js v5 (пакет `next-auth`, новая версия Auth.js)
- **Testing**: Jest (unit), Playwright (e2e)
- **Package Manager**: pnpm 9+

## 📚 Использование Context7 для документации

**ВАЖНО**: Перед использованием любой внешней библиотеки всегда получай актуальную документацию через Context7.

### Когда использовать Context7

Используй Context7 в следующих случаях:
1. **Перед использованием новой библиотеки** — получи актуальную документацию
2. **При работе с незнакомым API** — проверь правильный способ использования
3. **При обновлении зависимостей** — убедись, что используешь актуальный API
4. **При решении проблем** — найди правильные примеры и best practices

### Как использовать Context7

#### Шаг 1: Разрешение библиотеки
Сначала найди правильный ID библиотеки:
```
В Cursor используй MCP-инструмент mcp_context7_resolve-library-id с именем библиотеки
Например: "next.js", "react", "zod", "drizzle-orm", "zustand"
```

#### Шаг 2: Получение документации
После получения library ID, запроси документацию:
```
В Cursor используй MCP-инструмент mcp_context7_get-library-docs с:
- context7CompatibleLibraryID (из шага 1)
- mode: "code" для API и примеров кода (по умолчанию)
- mode: "info" для концептуальных гайдов и архитектуры
- topic: конкретная тема (например, "hooks", "routing", "validation")
```

### Примеры использования

**Пример 1: Работа с Next.js**
```
1. resolve-library-id: "next.js"
2. get-library-docs: "/vercel/next.js", topic: "app router", mode: "code"
```

**Пример 2: Работа с Zod**
```
1. resolve-library-id: "zod"
2. get-library-docs: "/colinhacks/zod", topic: "validation", mode: "code"
```

**Пример 3: Работа с Drizzle ORM**
```
1. resolve-library-id: "drizzle-orm"
2. get-library-docs: "/drizzle-team/drizzle-orm", topic: "queries", mode: "code"
```

### Правила работы с Context7

1. Перед использованием или обновлением любой внешней библиотеки проверяй документацию через Context7.
2. Для концепций используй `mode: "info"`, для примеров — `mode: "code"`.
3. Всегда сверяй версию библиотеки с `package.json` и ищи документацию под нужную версию.

### Библиотеки, используемые в проекте

Основные библиотеки, для которых может понадобиться документация:
- **Next.js** (`next`) — App Router, Server Components, Server Actions
- **React** (`react`) — хуки, компоненты, контекст
- **Zod** (`zod`) — валидация схем
- **Drizzle ORM** (`drizzle-orm`) — работа с БД
- **Zustand** (`zustand`) — state management
- **NextAuth.js** (`next-auth`) — аутентификация
- **Tailwind CSS** (`tailwindcss`) — стилизация
- **Lucide React** (`lucide-react`) — иконки
- **Radix UI** (`@radix-ui/*`) — UI компоненты

## 📝 Стиль кода и конвенции

### TypeScript
- Используй строгий режим (`strict: true`)
- Всегда типизируй пропсы компонентов
- Используй `type` для типов, `interface` только когда нужно расширение
- Избегай `any`, используй `unknown` если тип неизвестен
- Используй `as const` для литеральных типов

### React компоненты
- Функциональные компоненты с TypeScript
- Именование: PascalCase для компонентов (`AppSection.tsx`)
- Client компоненты: добавляй `'use client'` директиву
- Используй `forwardRef` для компонентов, которые должны принимать ref
- Props типизируй через `type`, не `interface` (если не нужно расширение)

### Импорты
- Группируй импорты: внешние библиотеки → внутренние модули → типы
- Используй абсолютные пути через `@/` алиасы
- Удаляй неиспользуемые импорты (ESLint правило `unused-imports`)

### Стилизация
- Используй Tailwind CSS классы
- Используй design tokens из `design-tokens.ts` для цветов, spacing, typography
- Используй `cn()` утилиту для условных классов
- CSS переменные через `var(--variable-name)`

### Именование
- Компоненты: `PascalCase` (например, `AppSection`)
- Файлы компонентов: `PascalCase.tsx`
- Хуки: `camelCase` с префиксом `use` (например, `useUnreadNotifications`)
- Утилиты: `camelCase` (например, `cn`, `getUserRoles`)
- Константы: `UPPER_SNAKE_CASE` (например, `TOAST_MESSAGES`)

## 🚀 Команды и workflow

### 1. Установка и подготовка
```bash
pnpm install --frozen-lockfile  # Установка зависимостей
pnpm ensure-env                  # Проверка переменных окружения
npx playwright install --with-deps  # Установка браузеров для E2E
```

### 2. Локальная разработка
```bash
pnpm dev                    # Запуск фронтенда
pnpm dev:ws                 # Запуск WebSocket сервера
pnpm build                  # Продакшен сборка
pnpm start                  # Запуск продакшен версии
```

### 3. Проверки (ВАЖНО: всегда запускай перед PR!)
```bash
pnpm -w lint                # Линтинг
pnpm -w typecheck           # Проверка типов
pnpm -w test                # Unit тесты
pnpm test:e2e               # E2E тесты
pnpm docs:lint              # Линтинг документации
pnpm docs:links             # Проверка ссылок в документации
```

### 4. Полная проверка (verify)
Команда `pnpm verify` выполняет все проверки в правильном порядке:
1. `pnpm ensure-env` — проверка переменных окружения
2. `pnpm -w lint` — линтинг
3. `pnpm --filter @collabverse/web typecheck` — проверка типов
4. `pnpm --filter @collabverse/web build` — сборка
5. `pnpm --filter @collabverse/web run check:routes` — проверка маршрутов
6. `pnpm test` — unit тесты
7. `pnpm exec playwright install --with-deps` — установка браузеров
8. `pnpm test:e2e` — E2E тесты
9. `pnpm verify:routes` — дополнительная проверка маршрутов
10. `node scripts/run-vercel-build.mjs` — симуляция Vercel сборки

### 5. Специфичные для пакетов
```bash
pnpm --filter @collabverse/web dev
pnpm --filter @collabverse/web build
pnpm --filter @collabverse/web run check:routes
```

## ⚠️ КРИТИЧЕСКИ ВАЖНО: CI/CD правила

**Приоритет №1: Прохождение CI проверок**

Перед любым PR или пушем ВСЕГДА:
1. Запускай локально все проверки из CI
2. Убедись, что `pnpm -w lint` проходит без ошибок
3. Убедись, что `pnpm -w typecheck` проходит без ошибок
4. Убедись, что `pnpm -w test` проходит без ошибок
5. Убедись, что `pnpm docs:lint` и `pnpm docs:links` проходят

**Если пользователь просит подготовить PR или пуш:**
- Сначала предложи запустить локальные проверки
- НЕ предлагай `git push` пока не исправлены все ошибки
- Если есть ошибки — исправляй их в первую очередь

## 🎯 Feature Flags

- Фичи часто скрыты за feature flags
- Конфигурация: `config/feature-flags.ts` и `apps/web/lib/feature-flags.ts`
- Для временных изменений используй feature flags вместо прямых изменений
- Проверяй переменные окружения: `NAV_V1`, `AUTH_DEV`, `FIN_EXPENSES_STORAGE` и т.д.

## 📁 Структура файлов

### Next.js App Router
- Страницы: `apps/web/app/[route]/page.tsx`
- Layouts: `apps/web/app/[route]/layout.tsx`
- Loading: `apps/web/app/[route]/loading.tsx`
- Error: `apps/web/app/[route]/error.tsx`
- Not-found: `apps/web/app/[route]/not-found.tsx`

### Компоненты
- UI компоненты: `apps/web/components/ui/`
- App компоненты: `apps/web/components/app/`
- Feature компоненты: `apps/web/components/[feature]/`

### API и бизнес-логика
- Репозитории: `apps/api/src/repositories/`
- Сервисы: `apps/api/src/services/`
- Схемы валидации: `apps/web/lib/schemas/` (Zod, только фронтенд)

## 🔧 Паттерны и best practices

### Работа с данными
- Используй Zod схемы для валидации (в `apps/web/lib/schemas/`)
- Используй Drizzle ORM для работы с БД
- Server Actions для мутаций данных
- Server Components для получения данных на сервере

### Состояние
- **Server state**: Next.js Server Components и Server Actions — основной подход для работы с данными
- **Глобальное состояние**: Zustand для клиентского состояния, которое должно быть доступно в разных компонентах
- **Локальное состояние**: `useState` для простых случаев внутри одного компонента

### Обработка ошибок
- Используй Error Boundaries для критических секций
- Всегда обрабатывай ошибки в async функциях
- Показывай понятные сообщения пользователю

### Маршруты
- После изменения маршрутов запускай: `pnpm --filter @collabverse/web run check:routes`
- Убедись, что все маршруты имеют необходимые файлы (page, layout, loading, error)

## 🧪 Тестирование

- Unit тесты: `apps/web/tests/unit/`
- E2E тесты: `apps/web/tests/e2e/`
- Используй Jest для unit тестов
- Используй Playwright для E2E тестов
- Тесты должны проходить перед коммитом

## 📚 Документация

- Основная документация: `docs/`
- Быстрый старт: `docs/getting-started/quick-start.md`
- Настройка: `docs/getting-started/setup.md`
- Архитектура: `docs/architecture/system-analysis.md`

## 🎨 UI/UX конвенции

- Используй компоненты из `apps/web/components/ui/` для базовых элементов
- Следуй design system из `design-tokens.ts`
- Используй семантические HTML элементы
- Обеспечивай accessibility (a11y)
- Используй правильные ARIA атрибуты

## 🔐 Безопасность

- Никогда не коммить секреты в код
- Используй переменные окружения для конфиденциальных данных
- Валидируй все пользовательские данные через Zod
- Проверяй права доступа перед показом контента

## 💡 Советы по работе

1. **Всегда проверяй типы**: TypeScript должен быть доволен
2. **Используй Context7 для документации**: перед использованием библиотеки получай актуальную документацию
3. **Следуй существующему стилю**: смотри на похожие файлы в проекте
4. **Используй существующие компоненты**: не создавай новые, если есть подходящие
5. **Документируй сложную логику**: добавляй комментарии где нужно
6. **Тестируй изменения**: запускай тесты после изменений
7. **Проверяй маршруты**: после изменения страниц проверяй routes

## 🚫 Чего избегать

- ❌ Не используй тип `any`.
- ❌ Не игнорируй ошибки линтера.
- ❌ Не коммить код без проверки типов.
- ❌ Не создавай дублирующий функционал.
- ❌ Не изменяй маршруты без проверки.
- ❌ Не пушь код, который не проходит CI.
- ❌ Не используй библиотеки без проверки документации через Context7.

## 📞 Когда нужна помощь

Если что-то непонятно:
1. **Используй Context7** для получения документации по библиотекам
2. Посмотри существующий код в проекте
3. Проверь документацию в `docs/`
4. Посмотри на похожие компоненты/функции
5. Спроси пользователя для уточнения

---

**Помни**: 
- Главная цель — писать чистый, типизированный код, который проходит все проверки CI и следует конвенциям проекта.
- **Всегда используй Context7 для получения актуальной документации** перед использованием внешних библиотек.
