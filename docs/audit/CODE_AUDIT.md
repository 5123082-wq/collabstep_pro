# Аудит кода - Полный отчет

**Дата:** 2025-02-04  
**Последнее обновление:** 2025-02-04  
**Статус:** 🔴 Проверки падают (lint/test/typecheck)

---

## 1. Обзор проекта

- **Тип:** Monorepo на Node.js/pnpm
- **Фронтенд:** Next.js 14 (React 18, Zustand, Tailwind, Zod, dnd-kit, react-big-calendar)
- **API:** Внутренний пакет `@collabverse/api` с in-memory репозиториями/сервисами для мокового бэкенда
- **CI:** GitHub Actions (preflight, сборка, линт, typecheck, юнит-тесты, проверки документации)

**Общая оценка:**

- ✅ Хорошая модульная разбивка (отдельный API-пакет, Next.js app router)
- ⚠️ Частично исправлены проблемы качества: большинство критичных проблем решено, остались проблемы с линтингом и тестами

---

## 2. Архитектура и структура

### apps/web

- Next.js приложение с app router
- Конфиги Tailwind/PostCSS
- Сегменты `(marketing)` и `(app)`
- Состояние через Zustand
- Моковые API-роуты
- Доменные типы и моки проектов/финансов

### apps/api

- Библиотека доменных репозиториев/сервисов
- TypeScript, экспортируется как npm-пакет
- Репозитории: проекты, финансы, админ-модули, документы

### docs/

- Системный обзор, roadmap и отчёты
- Содержит следы прежней системы этапов (Stage 1–4)

### scripts/

- Вспомогательные утилиты (проверка окружения, preflight, seed данных)

**Плюсы:**

- ✅ Ясное разделение UI и мок-бэкенда
- ✅ Доменные типы и репозитории отделены от Next кода
- ✅ Наличие автотестов и e2e-конфига

**Проблемы (текущее состояние):**

- 🔴 ESLint падает (парсинг `drizzle.config.ts` + ~179 ошибок правил)
- 🔴 Тест `stageCopy.spec.ts` падает из-за формулировок в аудит-доках
- 🔴 Typecheck из корня покрывает только web и падает по `TS6133`; API не проверяется
- 🟡 Jest конфиг содержит устаревший `globals`; возможный ESM-поломщик в websocket-runner
- 🟢 Шумные логи в тестах, скрипт `test` обновляет снапшоты

---

## 3. Основные проблемы (по приоритетам)

### P0 – Критичные (блокируют CI)

1) **Тест на запрещённый этап падает**  
   - `apps/web/tests/unit/stageCopy.spec.ts` находит совпадения в аудит-доках (`AUDIT_README.md`, `AUDIT_SUMMARY.md`, `CODE_AUDIT.md`, `FIXES_ACTION_PLAN.md`, `VERIFICATION_CHECKLIST.md`).  
   - Нужно либо исключить эти файлы из выборки, либо переписать формулировки без триггера.

2) **Parsing error для `drizzle.config.ts`**  
   - `apps/api/drizzle.config.ts` не в `apps/api/tsconfig.json`.  
   - Добавить в `include` (предпочтительно) или игнорировать в `.eslintrc.cjs`.

3) **Массивные lint-ошибки `any` и связанных правил**  
   - API: `ai-agents-repository.ts` (20+), `wallet-repository.ts` (все методы принимают `tx: any`), `data/memory.ts:71`, `invitations-repository.ts:140,178`, `organizations-repository.ts:160`, `users-db-repository.ts:87`, `websocket/types.ts:15`.  
   - WEB: крупные кластеры `any`/`ban-ts-comment`/`unused`/логических ошибок (`ai-hub/agents/page.tsx`, `performers/[section]/page.tsx`, `pm/tasks/page.tsx`, `finance/expenses/page-client.tsx`, `TasksListView.tsx`, `ArchivedProjectsList.tsx`, `ProjectsList.tsx`, `AIProjectPlanner.tsx` и др.).  
   - Пока это не исправлено, `pnpm lint` не пройдёт.

### P1 – Важные (не блокируют немедленно, но нужны)

4) **Typecheck покрывает только web**  
   - Корневой скрипт не запускает `@collabverse/api`.  
   - Добавить двойной вызов `typecheck` для web и api.

5) **Устаревший `globals` в Jest конфиге**  
   - `jest.config.cjs` содержит `globals` с `isolatedModules`, дублируя `transform`.  
   - Убрать блок, оставить настройки в `transform`.

6) **ESM-хелпер WebSocket ломает Jest**  
   - `apps/api/src/websocket/server-runner.ts` использует `import.meta`, что при косвенном импорте даёт SyntaxError.  
   - Нужен guard или вынесение CLI-раннера в отдельный ESM-скрипт вне тестового бандла.

### P2 – Улучшения качества

7) **Шумные логи в тестах**  
   - `expense-store.ts` и factory спамят `console.info`. Заглушить в тестах.

8) **Скрипт `test` обновляет снапшоты**  
   - `jest -u` не годится как дефолт. Нужен отдельный `test:update` или убрать `-u`.

---

## 4. Что уже исправлено (сохранить)

1. Переименование `module` → `moduleItem` в `admin-modules-repository.ts`
2. Удалены `admin-platform-*`, ESLint настроен
3. Хуки безусловны в `useFeatureFlags.ts`
4. `<img>` заменены/удалены
5. `tsconfig.base.json` заполнен
6. `menuBuilder.spec.ts` ранее приведён в порядок

---

## 5. Ненужные и устаревшие файлы/код

- ✅ `apps/api/src/services/admin-platform-service.ts` - удален
- ✅ `apps/api/src/repositories/admin-platform-repository.ts` - удален
- ⚠️ Упоминания старых Stage в документации - требуют исправления

---

## 6. Дублирование кода и что можно объединить

- Реализации `MemoryExpenseStore` и `DbExpenseStore` поддерживают одинаковый контракт
- Можно вынести общие части в базовый класс или хелперы
- **Приоритет:** Низкий (P2)

---

## 7. Рекомендации по реорганизации структуры

- ✅ `tsconfig.base.json` заполнен базовыми настройками
- ✅ ESLint настроен для обоих пакетов
- ⚠️ Нужно добавить `drizzle.config.ts` в `tsconfig.json` или исключить из ESLint

---

## 8. Рекомендации по качеству кода, тестам и процессам

### Линтинг:

- ⚠️ Исправить parsing error для `drizzle.config.ts`
- ⚠️ Заменить все `any` на конкретные типы
- ✅ ESLint настроен для обоих пакетов

### Типизация:

- ✅ `tsconfig.base.json` заполнен
- ⚠️ Добавить `typecheck` для `apps/api` в корневой `package.json`

### Тесты:

- ✅ `menuBuilder.spec.ts` исправлен
- ⚠️ Исправить `stageCopy.spec.ts` (исключить файлы аудита)
- ⚠️ Обновить `uiStore.spec.ts` (заменить `ReactDOMTestUtils.act`)

### CI/CD:

- ⚠️ Добавить `typecheck` для `apps/api`
- ⚠️ Убедиться, что все проверки проходят

---

## 9. План действий (roadmap)

### Шаг 1 (P0) — разблокировать проверки
1. Починить `stageCopy.spec.ts` (исключить аудит-доки или переписать формулировки).
2. Добавить `apps/api/drizzle.config.ts` в `tsconfig.json`.
3. Закрыть кластеры lint (`any`, `ban-ts-comment`, логические ошибки) — сначала API (`ai-agents-repository.ts`, `wallet-repository.ts`, др.), затем WEB (файлы из `report_lint.txt`).

### Шаг 2 (P1) — стабилизировать конфиги
1. Обновить `package.json` `typecheck` (web + api) и прогнать.
2. Убрать `globals` из `jest.config.cjs`.
3. Добавить guard/разделение для `server-runner.ts`, чтобы Jest не падал на `import.meta`.

### Шаг 3 (P2) — шум и DX
1. Подавить логи ExpenseStore в тестах.
2. Убрать `-u` из скрипта `test`, вынести апдейт снапов в отдельный скрипт.

---

## 10. Итоговая статистика

- **Статус проверок:** lint/test/typecheck — падают
- **Ключевые блокеры:** тест на запрещённый этап, parsing `drizzle.config.ts`, кластеры `any`/`ban-ts-comment`
- **Следом:** typecheck для API, jest `globals`, guard для websocket runner

---

**Следующий шаг:** См. `FIXES_ACTION_PLAN.md` для конкретных действий и очередности.
