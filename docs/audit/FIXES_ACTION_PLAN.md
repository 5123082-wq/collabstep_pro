# План исправлений проблем из аудита кода

**Дата создания:** 2025-01-27  
**Последнее обновление:** 2025-02-04  
**Основан на:** актуальный прогон `pnpm lint`, `pnpm test`, `pnpm typecheck`

---

## 📊 Статус проверки

🔴 Проверки падают: lint, test, typecheck  
✅ Ранее исправленные пункты сохранены  
📌 Новые блокеры и регрессии зафиксированы

---

## ✅ Уже исправлено (оставляем как есть)

1. Переименование `module` → `moduleItem` в `admin-modules-repository.ts`
2. Удалены проблемные `admin-platform-*`, ESLint настроен
3. Хуки вызываются безусловно в `useFeatureFlags.ts`
4. `<img>` заменены/удалены
5. `tsconfig.base.json` заполнен
6. `menuBuilder.spec.ts` ранее исправлен

---

## 🔴 P0 - Критичные проблемы (блокируют CI)

### 1. Тест на запрещённый этап падает

- **Файл:** `apps/web/tests/unit/stageCopy.spec.ts`
- **Причина:** Находит совпадения в аудит-доках (`AUDIT_README.md`, `AUDIT_SUMMARY.md`, `CODE_AUDIT.md`, `FIXES_ACTION_PLAN.md`, `VERIFICATION_CHECKLIST.md`).
- **Решение:**  
  - В тесте исключить аудит-доки из выборки И/ИЛИ переписать формулировки в самих доках, чтобы не срабатывать паттерн.
- **Действия:**  
  1. Обновить тест или формулировки.  
  2. Запустить `pnpm test` (юнит) и убедиться, что список offenders пуст.

### 2. Parsing error ESLint для `drizzle.config.ts`

- **Файл:** `apps/api/drizzle.config.ts`
- **Проблема:** Файл не включён в `apps/api/tsconfig.json`, но lint использует его.
- **Решение (предпочтительно):** добавить в `include`:
```json
{
  "include": [
    "src/**/*.ts",
    "test/**/*.ts",
    "drizzle.config.ts"
  ]
}
```text
- **Действия:**  
  1. Внести в `apps/api/tsconfig.json`.  
  2. Прогнать `pnpm lint` и убедиться, что parsing error ушёл.

### 3. Массовые lint-ошибки `any` и нарушений правил

- **API кластеры:**  
  - `apps/api/src/repositories/ai-agents-repository.ts` (20+ `any`)  
  - `apps/api/src/repositories/wallet-repository.ts` (все методы принимают `tx: any`)  
  - `apps/api/src/data/memory.ts:71`  
  - `apps/api/src/repositories/invitations-repository.ts:140,178`  
  - `apps/api/src/repositories/organizations-repository.ts:160`  
  - `apps/api/src/repositories/users-db-repository.ts:87`  
  - `apps/api/src/websocket/types.ts:15`
- **WEB кластеры:**  
  - `ban-ts-comment` / `any` / `unused` / логические ошибки в ключевых страницах и компонентах (`app/(app)/ai-hub/agents/page.tsx`, `performers/[section]/page.tsx`, `pm/tasks/page.tsx`, `finance/expenses/page-client.tsx:503`, `components/pm/TasksListView.tsx:74`, `ArchivedProjectsList.tsx:164-185`, `ProjectsList.tsx:185-186`, `AIProjectPlanner.tsx:124` и др.)
- **Действия:**  
  1. Закрыть API-кластер (`ai-agents-repository.ts`, `wallet-repository.ts`, остальное).  
  2. Исправить фронтовые `ban-ts-comment`/`any`/`unused`/логические ошибки, начиная с файлов из отчёта lint.  
  3. Прогнать `pnpm lint` до зелёного.

---

## 🟡 P1 - Важные проблемы (не блокируют немедленно)

### 4. Typecheck не включает `apps/api`

- **Файл:** корневой `package.json`  
- **Решение:**  
```json
"typecheck": "pnpm --filter @collabverse/web typecheck && pnpm --filter @collabverse/api typecheck"
```text
- **Действия:** обновить скрипт, прогнать `pnpm typecheck` (убедиться, что оба пакета проверяются).

### 5. Устаревший `globals` в Jest конфигурации

- **Файл:** `jest.config.cjs`  
- **Проблема:** блок `globals` с `isolatedModules` дублирует `transform`.  
- **Решение:** удалить блок `globals`, оставить настройки в `transform`.  
- **Действия:** правка конфига, запуск `pnpm test` (ожидаем пропажу предупреждений ts-jest).

### 6. ESM-хелпер WebSocket ломает Jest

- **Файл:** `apps/api/src/websocket/server-runner.ts`  
- **Проблема:** `import.meta` в тестовом окружении → SyntaxError.  
- **Решение:** обернуть условием, вынести CLI в отдельный ESM-файл или замокать импорт в тестах.  
- **Действия:** выбрать подход (guard/разделение), прогнать юниты, убедиться в отсутствии SyntaxError.

---

## 🟢 P2 - Улучшения качества

### 7. Избыточные console.info в тестах

- **Файлы:** `apps/api/src/repositories/expense-store.ts`, `stores/expense-store-factory.ts`  
- **Решение:** подавлять логи в `NODE_ENV === 'test'` или моками в setup тестов.  
- **Результат:** чище вывод, легче читать падения.

### 8. Скрипт `test` обновляет снапшоты

- **Файл:** корневой `package.json` (`"test": "jest -u"`)  
- **Решение:** убрать `-u` в дефолтном скрипте; вынести обновление снапшотов в отдельный `test:update`.  
- **Результат:** избегаем непреднамеренных апдейтов снапов в CI/локально.

---

## 📋 План выполнения (по приоритетам)

### Этап 1: Разблокировать проверки (P0)

1. Починить `stageCopy.spec.ts`: исключить аудит-доки или переписать формулировки, прогнать `pnpm test`.
2. Добавить `apps/api/drizzle.config.ts` в `tsconfig.json`, перепроверить `pnpm lint`.
3. Закрыть кластеры lint:
   - API: `ai-agents-repository.ts`, `wallet-repository.ts`, `data/memory.ts`, `invitations-repository.ts`, `organizations-repository.ts`, `users-db-repository.ts`, `websocket/types.ts`.
   - WEB: файлы из `report_lint.txt` (особенно `ban-ts-comment`, `any`, `unused`, логические ошибки).
4. Прогнать `pnpm lint` до зелёного.
   - **Статус:** выполнено (lint ✅, typecheck ✅ — web, test ✅; остаются предупреждения ts-jest по `globals` и `import.meta` из `server-runner.ts`, вынесены в Этап 2).

### Этап 2: Починить typecheck и Jest (P1)

1. Обновить `package.json` — добавить typecheck для API; запустить `pnpm typecheck`.
2. Удалить `globals` из `jest.config.cjs`, перепроверить `pnpm test` (нет предупреждений ts-jest).
3. Защитить `server-runner.ts` (guard/разделение), убедиться в отсутствии SyntaxError при юнитах.
   - **Статус:** выполнено (lint ✅, typecheck ✅ web+api, test ✅; предупреждения ts-jest сняты, `server-runner.ts` больше не срывает юниты).

### Этап 3: Улучшения (P2)

1. Заглушить шум логов ExpenseStore в тестах.
2. Убрать `-u` из скрипта `test`, вынести обновление снапов в отдельный скрипт.
   - **Статус:** выполнено (lint ✅, typecheck ✅, test ✅; логи ExpenseStore заглушены в тестах, дефолтный `test` больше не обновляет снапы, добавлен `test:update`).

---

## 🎯 Критерии успеха

После Этапа 1:
- ✅ `pnpm lint` зелёный (нет parsing error и кластеров `any`/`ban-ts-comment`/логических ошибок)
- ✅ `pnpm test` зелёный (тест на запрещённый этап проходит)

После Этапа 2:
- ✅ `pnpm typecheck` запускает web и api
- ✅ Нет предупреждений ts-jest, нет SyntaxError из websocket runner

После Этапа 3:
- ✅ Тестовые логи чистые
- ✅ Снапшоты обновляются только по явному скрипту

---

## 📝 Примечания

- После каждого блока — `pnpm lint && pnpm typecheck && pnpm test`.
- Список файлов с ошибками берём из свежего `report_lint.txt`.
- Формулировки в аудит-доках не должны срабатывать на паттерн запрещённого этапа.

---

**Следующий шаг:** Этап 1, пункт 1 — починить `stageCopy.spec.ts` (исключить аудит-доки или переписать формулировки) и добавить `drizzle.config.ts` в tsconfig.
