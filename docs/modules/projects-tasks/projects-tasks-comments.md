# Проекты и задачи — Комментарии

**Статус:** stable  
**Владелец:** product  
**Создан:** 2026-01-06  
**Последнее обновление:** 2026-01-06

## Введение

Система комментариев к задачам позволяет участникам проекта обсуждать задачи, задавать вопросы, делиться информацией. Комментарии поддерживают упоминания пользователей (@mentions), прикрепление файлов, древовидную структуру (ответы на комментарии) и реакции.

**Статус реализации:** ✅ Завершён (Stage H)

## Структура комментария

### Поля комментария

```typescript
interface TaskComment {
  id: ID;
  projectId: ID;
  taskId: ID;
  parentId: ID | null; // ID родительского комментария (для ответов)
  body: string; // Текст комментария
  mentions: ID[]; // Массив ID упомянутых пользователей
  authorId: ID;
  attachments: ID[]; // Массив ID прикрепленных файлов
  createdAt: string;
  updatedAt: string;
}
```

### Древовидная структура

Комментарии поддерживают иерархию через поле `parentId`:
- **Родительский комментарий** (`parentId = null`) — комментарий верхнего уровня
- **Ответ на комментарий** (`parentId != null`) — ответ на родительский комментарий

**Отображение:**
- Комментарии отображаются в древовидной структуре
- Ответы отображаются с отступом под родительским комментарием
- Поддерживается неограниченная глубина вложенности

## Создание и редактирование комментариев

### Создание комментария

**API endpoint:**
- `POST /api/pm/tasks/[id]/comments`

**Обязательные поля:**
- `body` (string) — текст комментария

**Опциональные поля:**
- `parentId` (ID | null) — ID родительского комментария (для ответа)
- `mentions` (ID[]) — массив ID упомянутых пользователей
- `attachments` (ID[]) — массив ID прикрепленных файлов

**Права доступа:**
- `owner` — может создавать комментарии
- `manager` — может создавать комментарии
- `contributor` — может создавать комментарии
- `viewer` — может создавать комментарии (только просмотр задач)

**Автоматические действия:**
- При упоминании пользователя (`@mention`) генерируется уведомление
- При создании комментария генерируется уведомление для участников задачи

### Редактирование комментария

**API endpoint:**
- `PATCH /api/pm/tasks/[id]/comments/[commentId]`

**Доступные поля для редактирования:**
- `body` — текст комментария
- `mentions` — упомянутые пользователи
- `attachments` — прикрепленные файлы

**Права доступа:**
- Только автор комментария может редактировать свой комментарий
- `owner` и `manager` могут редактировать любые комментарии

### Удаление комментария

**API endpoint:**
- `DELETE /api/pm/tasks/[id]/comments/[commentId]`

**Действия:**
- Комментарий удаляется из базы данных
- Все ответы на комментарий также удаляются (каскадное удаление)

**Права доступа:**
- Автор комментария может удалить свой комментарий
- `owner` и `manager` могут удалять любые комментарии

## Упоминания пользователей (@mentions)

### Формат упоминаний

В тексте комментария можно упомянуть пользователя, используя формат `@username` или `@имя пользователя`.

**Обработка:**
- При сохранении комментария упоминания извлекаются из текста
- ID упомянутых пользователей сохраняются в поле `mentions`
- Генерируются уведомления для упомянутых пользователей

### Уведомления

При упоминании пользователя:
- Создается уведомление типа `comment_mentioned`
- Уведомление отправляется упомянутому пользователю
- Уведомление содержит ссылку на задачу и комментарий

**См. также:**
- [`notifications.md`](./projects-tasks-notifications.md) — документация по уведомлениям

## Файлы в комментариях

### Прикрепление файлов

**Процесс:**
1. Пользователь загружает файл через файловый менеджер
2. Файл прикрепляется к комментарию через поле `attachments`
3. Файл отображается в комментарии

**API endpoint:**
- `POST /api/files` — загрузить файл
- При создании/редактировании комментария указать `attachments: [fileId]`

**См. также:**
- [`files.md`](./projects-tasks-files.md) — документация по файловому менеджеру

### Отображение файлов

- Файлы отображаются в комментарии как список вложений
- Поддерживается предпросмотр изображений и PDF
- Файлы можно скачать или открыть в новом окне

## Реакции на комментарии

**Статус:** ⚠️ Планируется

В будущем планируется добавить поддержку эмодзи-реакций на комментарии:
- Пользователи смогут добавлять реакции (👍, ❤️, 😂 и т.д.)
- Реакции будут отображаться под комментарием
- Реакции будут использоваться для быстрой обратной связи

## UI компоненты

### TaskComments

Основной компонент для отображения списка комментариев задачи.

**Функции:**
- Отображение комментариев в древовидной структуре
- Загрузка комментариев с пагинацией
- Автоматическое обновление при создании новых комментариев
- Интеграция с WebSocket для real-time обновлений

### TaskCommentItem

Компонент для отображения отдельного комментария.

**Функции:**
- Отображение автора, текста, даты создания
- Отображение упоминаний и файлов
- Кнопки редактирования и удаления
- Форма ответа на комментарий

### TaskCommentForm

Форма для создания и редактирования комментария.

**Функции:**
- Текстовое поле с поддержкой упоминаний
- Загрузка и прикрепление файлов
- Предпросмотр упоминаний и файлов
- Валидация перед отправкой

## Интеграция

### Интеграция в задачи

Комментарии отображаются в детальном виде задачи:
- Секция "Комментарии" на странице задачи
- Комментарии загружаются автоматически при открытии задачи
- Real-time обновления через WebSocket

### Интеграция с уведомлениями

При создании комментария:
- Генерируется уведомление для участников задачи
- Генерируется уведомление для упомянутых пользователей
- Уведомления отображаются в панели уведомлений

**См. также:**
- [`notifications.md`](./projects-tasks-notifications.md) — документация по уведомлениям

### Интеграция с WebSocket

Комментарии обновляются в реальном времени через WebSocket:
- При создании нового комментария все открытые клиенты получают событие
- UI автоматически обновляется без перезагрузки страницы

**См. также:**
- [`websocket.md`](./projects-tasks-websocket.md) — документация по WebSocket

## API endpoints

### Основные операции

- `GET /api/pm/tasks/[id]/comments` — получить список комментариев задачи
- `POST /api/pm/tasks/[id]/comments` — создать комментарий
- `PATCH /api/pm/tasks/[id]/comments/[commentId]` — обновить комментарий
- `DELETE /api/pm/tasks/[id]/comments/[commentId]` — удалить комментарий

### Параметры запроса

**GET /api/pm/tasks/[id]/comments:**
- `parentId` (ID | null) — фильтр по родительскому комментарию
- `limit` (number) — количество комментариев на странице
- `offset` (number) — смещение для пагинации

## Связанные документы

- [`_module.md`](./projects-tasks-overview.md) — обзор модуля PM Core
- [`tasks.md`](./projects-tasks-tasks.md) — документация по задачам
- [`notifications.md`](./projects-tasks-notifications.md) — уведомления о комментариях
- [`files.md`](./projects-tasks-files.md) — файлы в комментариях
- [`websocket.md`](./projects-tasks-websocket.md) — real-time обновления
- [`_implementation-plan.md`](./projects-tasks-implementation-plan.md#stage-h) — Stage H: Комментарии

---

**Последнее обновление:** 2026-01-06
