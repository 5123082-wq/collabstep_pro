# Проекты и задачи — План реализации

**Статус:** active  
**Владелец:** product/engineering  
**Создан:** 2026-01-06  
**Последнее обновление:** 2026-01-06

## Обзор

План реализации модуля PM Core включает этапы от Stage H до Stage N, охватывающие все основные функции управления проектами: комментарии, уведомления, чат, файлы, диаграмма Ганта, WebSocket и AI-ассистент.

**Общая оценка:** 60-80 дней разработки  
**Фактический прогресс:** ~85% выполнено (6 из 7 этапов)

---

## Этапы реализации

| Этап    | Описание                         | Статус          | Приоритет | Начало     | Завершение | Зависимости   |
| ------- | -------------------------------- | --------------- | --------- | ---------- | ---------- | ------------- |
| Stage H | Комментарии к задачам            | ✅ Завершён     | P0        | 2024-XX-XX | 2024-XX-XX | -             |
| Stage I | Система уведомлений              | ✅ 95% завершён | P0        | 2024-XX-XX | 2024-XX-XX | Stage H       |
| Stage J | Чат проекта и файловый каталог   | ✅ Завершён     | P1        | 2024-XX-XX | 2024-XX-XX | Stage I       |
| Stage K | Диаграмма Ганта и зависимости    | ✅ Завершён     | P1        | 2024-XX-XX | 2024-XX-XX | -             |
| Stage L | Real-time обновления (WebSocket) | ✅ 95% завершён | P1        | 2024-XX-XX | 2024-XX-XX | Stage H, I, J |
| Stage M | AI-ассистент (базовый)           | ✅ Завершён     | P2        | 2024-XX-XX | 2024-XX-XX | Stage H, I, J |
| Stage N | Расширенная функциональность AI  | ❌ Не начат     | P3        | -          | -          | Stage M, K    |

**Легенда статусов:**

- ✅ Завершён
- 🔄 В работе
- ⏳ Ожидает
- ❌ Заблокирован
- ⚠️ Частично

**Легенда приоритетов:**

- P0 (Критический)
- P1 (Высокий)
- P2 (Средний)
- P3 (Низкий)

---

## Stage H: Комментарии к задачам

**Статус:** ✅ Завершён  
**Приоритет:** P0  
**Оценка времени:** 8-10 дней  
**Зависимости:** Нет

### Цель (Stage H)

Реализовать полноценную систему комментариев к задачам с API endpoints и UI компонентами.

### Задачи (Stage H)

1. **API endpoints для комментариев**
   - `GET /api/pm/tasks/[id]/comments` — получить список комментариев задачи
   - `POST /api/pm/tasks/[id]/comments` — создать комментарий
   - `PATCH /api/pm/tasks/[id]/comments/[commentId]` — обновить комментарий
   - `DELETE /api/pm/tasks/[id]/comments/[commentId]` — удалить комментарий

2. **UI компонент комментариев**
   - Компонент `TaskComments.tsx` для отображения комментариев
   - Форма создания/редактирования комментария
   - Поддержка упоминаний участников (@mentions)
   - Прикрепление файлов к комментариям
   - Древовидная структура (ответы на комментарии)

3. **Интеграция в страницу задачи**
   - Добавить секцию комментариев в детальный вид задачи
   - Обновление комментариев при создании/изменении

4. **Тестирование**
   - Unit тесты для API endpoints
   - E2E тесты для создания/редактирования комментариев

### Критерии готовности (DoD) (Stage H)

- ✅ Все API endpoints работают и покрыты тестами
- ✅ UI компонент комментариев интегрирован в задачи
- ✅ Поддерживаются упоминания и прикрепление файлов
- ✅ Комментарии отображаются в древовидной структуре

### Файлы для создания/изменения (Stage H)

- `apps/web/app/api/pm/tasks/[id]/comments/route.ts` (новый)
- `apps/web/app/api/pm/tasks/[id]/comments/[commentId]/route.ts` (новый)
- `apps/web/components/pm/TaskComments.tsx` (новый)
- `apps/web/components/pm/TaskCommentForm.tsx` (новый)
- `apps/web/tests/unit/task-comments-api.spec.ts` (новый)
- `apps/web/tests/e2e/task-comments.spec.ts` (новый)

### Результаты (Stage H)

**Реализовано:**

- ✅ Все API endpoints (GET, POST, PATCH, DELETE)
- ✅ CommentsRepository с полным функционалом
- ✅ UI компоненты: TaskComments, TaskCommentItem, TaskCommentForm
- ✅ Древовидная структура комментариев (ответы)
- ✅ Поддержка упоминаний (@mentions)
- ✅ Прикрепление файлов к комментариям
- ✅ Unit и E2E тесты

**Созданные файлы:**

- `apps/web/app/api/pm/tasks/[id]/comments/route.ts`
- `apps/web/app/api/pm/tasks/[id]/comments/[commentId]/route.ts`
- `apps/web/components/pm/TaskComments.tsx`
- `apps/web/components/pm/TaskCommentItem.tsx`
- `apps/web/components/pm/TaskCommentForm.tsx`

---

## Stage I: Система уведомлений

**Статус:** ✅ 95% завершён  
**Приоритет:** P0  
**Оценка времени:** 10-12 дней  
**Зависимости:** Stage H (для уведомлений о комментариях)

### Цель (Stage I)

Реализовать полноценную систему уведомлений для событий в проектах.

### Задачи (Stage I)

1. **Модель данных уведомлений**
   - Типы уведомлений: task_assigned, task_updated, comment_added, deadline_approaching, project_invite
   - Хранение уведомлений в памяти/БД
   - Статусы: unread, read, archived

2. **Генерация уведомлений**
   - События при создании/обновлении задач
   - События при добавлении комментариев
   - События при назначении исполнителя
   - События при приближении дедлайнов (cron job или проверка при загрузке)

3. **API endpoints**
   - `GET /api/notifications` — получить список уведомлений пользователя
   - `PATCH /api/notifications/[id]/read` — отметить как прочитанное
   - `POST /api/notifications/mark-all-read` — отметить все как прочитанные
   - `DELETE /api/notifications/[id]` — удалить уведомление

4. **UI компоненты**
   - Обновить `NotificationsPanel.tsx` с реальными данными
   - Страница `/app/notifications` с фильтрацией
   - Счётчик непрочитанных уведомлений в навигации
   - Badge на иконке уведомлений

5. **Интеграция**
   - Генерация уведомлений при событиях (задачи, комментарии)
   - Обновление счётчика в реальном времени (polling или WebSocket)

6. **Тестирование**
   - Unit тесты для генерации уведомлений
   - E2E тесты для получения/отметки уведомлений

### Критерии готовности (DoD) (Stage I)

- ✅ Уведомления генерируются при всех критических событиях
- ✅ API endpoints работают и покрыты тестами
- ✅ UI отображает уведомления с фильтрацией
- ✅ Счётчик непрочитанных обновляется автоматически

### Файлы для создания/изменения (Stage I)

- `apps/api/src/repositories/notifications-repository.ts` (новый)
- `apps/api/src/types.ts` — добавить `Notification` interface
- `apps/web/app/api/notifications/route.ts` (новый)
- `apps/web/app/api/notifications/[id]/route.ts` (новый)
- `apps/web/components/right-rail/NotificationsPanel.tsx` (обновить)
- `apps/web/app/(app)/app/notifications/page.tsx` (обновить)
- `apps/web/lib/notifications/event-generator.ts` (новый)
- `apps/web/tests/unit/notifications.spec.ts` (новый)

### Результаты (Stage I)

**Реализовано:**

- ✅ NotificationsRepository с полным CRUD
- ✅ Генератор событий (task_assigned, task_updated, comment_added, deadline_approaching, project_invite)
- ✅ Все API endpoints (GET, PATCH, DELETE, mark-all-read, unread-count)
- ✅ NotificationsPanel с реальными данными
- ✅ Страница уведомлений с фильтрацией
- ✅ Счётчик непрочитанных с автообновлением (polling 30 сек)
- ✅ Unit и E2E тесты

**Осталось:**

- ⚠️ Endpoint для приглашения участников (функция готова, но нет POST endpoint)
- ⚠️ Автоматическая проверка дедлайнов (функция готова, но не интегрирована)

**Созданные файлы:**

- `apps/api/src/repositories/notifications-repository.ts`
- `apps/web/lib/notifications/event-generator.ts`
- `apps/web/app/api/notifications/route.ts`
- `apps/web/app/api/notifications/[id]/route.ts`
- `apps/web/app/api/notifications/mark-all-read/route.ts`
- `apps/web/app/api/notifications/unread-count/route.ts`
- `apps/web/hooks/useUnreadNotifications.ts`

---

## Stage J: Чат проекта и файловый каталог

**Статус:** ✅ Завершён  
**Приоритет:** P1  
**Оценка времени:** 12-15 дней  
**Зависимости:** Stage I (для уведомлений о сообщениях в чате)

### Цель (Stage J)

Реализовать общий чат проекта и файловый каталог для централизованного управления файлами.

### Задачи чата проекта (Stage J)

1. **Модель данных чата**
   - Сообщения чата проекта (`ProjectChatMessage`)
   - Привязка к проекту, автор, текст, timestamp, вложения

2. **API endpoints чата**
   - `GET /api/pm/projects/[id]/chat` — получить историю сообщений
   - `POST /api/pm/projects/[id]/chat` — отправить сообщение
   - Поддержка пагинации для истории

3. **UI компонент чата**
   - Компонент `ProjectChat.tsx` с историей сообщений
   - Форма отправки сообщения
   - Прикрепление файлов к сообщениям
   - Отображение участников чата

4. **Интеграция**
   - Добавить вкладку "Чат" на странице проекта
   - Real-time обновления (polling или WebSocket)

### Задачи файлового каталога

1. **API endpoints файлов**
   - `GET /api/pm/projects/[id]/files` — получить список файлов проекта
   - `POST /api/pm/projects/[id]/files` — загрузить файл в проект
   - Группировка по задачам или папкам

2. **UI компонент каталога**
   - Компонент `ProjectFilesCatalog.tsx` с списком файлов
   - Фильтрация по задачам
   - Просмотр и скачивание файлов
   - Загрузка новых файлов

3. **Интеграция**
   - Добавить вкладку "Файлы" на странице проекта
   - Отображение всех файлов проекта (из задач и комментариев)

### Критерии готовности (DoD) (Stage J)

- ✅ Чат проекта работает с отправкой/получением сообщений
- ✅ Файловый каталог отображает все файлы проекта
- ✅ Можно загружать файлы напрямую в проект
- ✅ Real-time обновления работают (или polling)

### Файлы для создания/изменения (Stage J)

- `apps/api/src/repositories/project-chat-repository.ts` (новый)
- `apps/api/src/types.ts` — добавить `ProjectChatMessage`
- `apps/web/app/api/pm/projects/[id]/chat/route.ts` (новый)
- `apps/web/components/pm/ProjectChat.tsx` (новый)
- `apps/web/app/api/pm/projects/[id]/files/route.ts` (новый)
- `apps/web/components/pm/ProjectFilesCatalog.tsx` (новый)
- `apps/web/app/(app)/pm/projects/[id]/page.tsx` (обновить — добавить вкладки)

### Результаты (Stage J)

**Реализовано:**

- ✅ ProjectChatRepository + API endpoints
- ✅ ProjectChat компонент с polling и infinite scroll
- ✅ Прикрепление файлов к сообщениям
- ✅ ProjectFilesCatalog с фильтрацией
- ✅ API для загрузки файлов в проект
- ✅ Вкладки "Чат" и "Файлы" на странице проекта
- ✅ Unit и E2E тесты

**Созданные файлы:**

- `apps/api/src/repositories/project-chat-repository.ts`
- `apps/web/app/api/pm/projects/[id]/chat/route.ts`
- `apps/web/components/pm/ProjectChat.tsx`
- `apps/web/app/api/pm/projects/[id]/files/route.ts`
- `apps/web/components/pm/ProjectFilesCatalog.tsx`

---

## Stage K: Диаграмма Ганта и зависимости задач

**Статус:** ✅ Завершён  
**Приоритет:** P1  
**Оценка времени:** 12-15 дней  
**Зависимости:** Нет (но желательно после Stage H для комментариев)

### Цель (Stage K)

Реализовать диаграмму Ганта для визуализации задач на временной шкале с зависимостями.

### Задачи (Stage K)

1. **Визуализация зависимостей**
   - Отображение зависимостей между задачами на диаграмме
   - Стрелки между связанными задачами
   - Валидация циклических зависимостей

2. **Компонент диаграммы Ганта**
   - Собственная реализация на SVG (без внешних зависимостей)
   - Отображение задач как полос на временной шкале
   - Перетаскивание для изменения сроков

3. **Функциональность**
   - Изменение дат начала/окончания через перетаскивание
   - Отображение критического пути проекта
   - Масштабирование временной шкалы (дни/недели/месяцы)
   - Фильтрация задач на диаграмме

4. **Интеграция**
   - Добавить вкладку "Гант" на странице проекта
   - Синхронизация с Kanban и календарём
   - Обновление зависимостей при изменении задач

5. **API для зависимостей**
   - `GET /api/pm/tasks/[id]/dependencies` — получить зависимости задачи
   - `POST /api/pm/tasks/[id]/dependencies` — создать зависимость
   - `DELETE /api/pm/tasks/[id]/dependencies/[depId]` — удалить зависимость

6. **Тестирование**
   - Unit тесты для валидации зависимостей
   - E2E тесты для создания/удаления зависимостей
   - Тесты визуализации на диаграмме

### Критерии готовности (DoD) (Stage K)

- ✅ Диаграмма Ганта отображает задачи на временной шкале
- ✅ Можно изменять сроки через перетаскивание
- ✅ Зависимости отображаются стрелками
- ✅ Валидация циклических зависимостей работает
- ✅ Синхронизация с другими представлениями

### Файлы для создания/изменения (Stage K)

- `apps/web/components/pm/TasksGanttView.tsx` (новый)
- `apps/web/app/api/pm/tasks/[id]/dependencies/route.ts` (новый)
- `apps/web/lib/pm/gantt-utils.ts` (новый — утилиты для Ганта)
- `apps/web/app/(app)/pm/projects/[id]/page.tsx` (обновить — добавить вкладку)
- `apps/web/tests/unit/task-dependencies.spec.ts` (новый)

### Результаты (Stage K)

**Реализовано:**

- ✅ TaskDependenciesRepository с циклической валидацией
- ✅ API endpoints: `/api/pm/tasks/[id]/dependencies` (GET, POST, DELETE)
- ✅ dependency-validator.ts с функциями `hasCycle()`, `findCycle()`
- ✅ gantt-utils.ts: `calculateCriticalPath()`, `transformTasksForGantt()`
- ✅ TasksGanttView.tsx - полноценный компонент:
  - Перетаскивание задач для изменения сроков
  - SVG визуализация зависимостей стрелками
  - Отображение критического пути (красный цвет)
  - Масштабирование (день/неделя/месяц)
  - Фильтрация по статусу и исполнителю
- ✅ Интеграция: вкладка "Гант" на странице проекта

**Созданные файлы:**

- `apps/api/src/repositories/task-dependencies-repository.ts`
- `apps/web/app/api/pm/tasks/[id]/dependencies/route.ts`
- `apps/web/app/api/pm/tasks/[id]/dependencies/[depId]/route.ts`
- `apps/web/lib/pm/dependency-validator.ts`
- `apps/web/components/pm/TasksGanttView.tsx`
- `apps/web/lib/pm/gantt-utils.ts`

---

## Stage L: Real-time обновления (WebSocket)

**Статус:** ✅ 95% завершён  
**Приоритет:** P1  
**Оценка времени:** 10-12 дней  
**Зависимости:** Stage H, I, J (для обновлений комментариев, уведомлений, чата)

### Цель (Stage L)

Реализовать WebSocket для real-time обновлений интерфейса без перезагрузки страницы.

### Задачи (Stage L)

1. **Backend WebSocket сервер**
   - Настройка WebSocket сервера (Socket.io)
   - Подключение клиентов к комнатам проектов
   - Рассылка событий при изменениях

2. **Типы событий**
   - `task.updated` — обновление задачи
   - `task.created` — создание задачи
   - `comment.added` — добавление комментария
   - `chat.message` — сообщение в чате
   - `notification.new` — новое уведомление

3. **Клиентская часть**
   - WebSocket клиент для подключения к серверу
   - Подписка на события проекта
   - Обновление UI при получении событий
   - Обработка переподключения при разрыве связи

4. **Интеграция в компоненты**
   - Обновление Kanban доски в реальном времени
   - Обновление комментариев
   - Обновление чата проекта
   - Обновление счётчика уведомлений

5. **Fallback на polling**
   - Если WebSocket недоступен, использовать polling
   - Плавный переход между режимами

6. **Тестирование**
   - Тесты WebSocket соединения
   - Тесты рассылки событий
   - E2E тесты real-time обновлений

### Критерии готовности (DoD) (Stage L)

- ✅ WebSocket сервер работает и рассылает события
- ✅ Клиенты подключаются к комнатам проектов
- ✅ UI обновляется в реальном времени при событиях
- ✅ Есть fallback на polling при недоступности WebSocket

### Файлы для создания/изменения (Stage L)

- `apps/web/lib/websocket/client.ts` (новый)
- `apps/web/lib/websocket/hooks.ts` (новый — React hooks)
- `apps/web/app/api/ws/route.ts` (новый — WebSocket endpoint)
- `apps/web/components/pm/TasksBoardView.tsx` (обновить — добавить WebSocket)
- `apps/web/components/pm/ProjectChat.tsx` (обновить — добавить WebSocket)
- `apps/web/tests/unit/websocket.spec.ts` (новый)

### Результаты (Stage L)

**Реализовано:**

- ✅ WebSocket сервер на Socket.io
- ✅ Типы событий (task.updated, task.created, comment.added, chat.message, notification.new)
- ✅ WebSocket клиент и React hooks (useWebSocket)
- ✅ Интеграция в Kanban доску (real-time обновления)
- ✅ Интеграция в комментарии
- ✅ Интеграция в чат проекта
- ✅ Интеграция в уведомления
- ✅ Fallback на polling
- ✅ Обработка переподключения

**Осталось:**

- ⚠️ Unit и E2E тесты (опционально)

**Созданные файлы:**

- `apps/web/lib/websocket/client.ts`
- `apps/web/lib/websocket/hooks.ts`
- `apps/web/lib/websocket/event-broadcaster.ts`

---

## Stage M: AI-ассистент (базовая функциональность)

**Статус:** ✅ Завершён  
**Приоритет:** P2  
**Оценка времени:** 15-20 дней  
**Зависимости:** Stage H, I, J (для работы с комментариями, уведомлениями, чатом)

### Цель (Stage M)

Реализовать базовую функциональность AI-ассистента для помощи в управлении проектами.

### Задачи (Stage M)

1. **Интеграция LLM**
   - Выбор провайдера (OpenAI)
   - Настройка API ключей и конфигурации
   - Базовый клиент для запросов к LLM

2. **Генерация описаний задач**
   - Функция генерации описания задачи по названию
   - Предложение чек-листов для задач
   - UI кнопка "Сгенерировать описание" в форме задачи

3. **Суммирование комментариев**
   - Функция анализа комментариев задачи
   - Генерация сводки обсуждения
   - UI кнопка "Суммировать комментарии" в задаче

4. **Напоминания о дедлайнах**
   - Автоматическая проверка приближающихся дедлайнов
   - Генерация уведомлений через AI
   - Настройка интервала проверки

5. **Чат с AI в проекте**
   - Интеграция AI в общий чат проекта
   - Ответы на вопросы о проекте
   - Контекст проекта для AI (задачи, участники, прогресс)

6. **Ограничения и безопасность**
   - Проверка прав доступа AI
   - Валидация ответов AI перед сохранением
   - Подтверждение действий AI пользователем

7. **Тестирование**
   - Unit тесты для функций AI
   - E2E тесты для генерации описаний
   - Тесты безопасности и валидации

### Критерии готовности (DoD) (Stage M)

- ✅ AI генерирует описания задач и чек-листы
- ✅ AI суммирует комментарии
- ✅ AI отвечает на вопросы в чате проекта
- ✅ Все действия AI требуют подтверждения пользователя
- ✅ Валидация и безопасность работают

### Файлы для создания/изменения (Stage M)

- `apps/api/src/services/ai-service.ts` (новый)
- `apps/web/lib/ai/client.ts` (новый)
- `apps/web/lib/ai/prompts.ts` (новый — шаблоны промптов)
- `apps/web/components/pm/AIAssistant.tsx` (новый)
- `apps/web/components/pm/TaskAIActions.tsx` (новый)
- `apps/web/app/api/ai/generate-description/route.ts` (новый)
- `apps/web/app/api/ai/summarize-comments/route.ts` (новый)
- `apps/web/tests/unit/ai-service.spec.ts` (новый)

### Результаты (Stage M)

**Реализовано:**

- ✅ ai-service.ts - все функции:
  - generateTaskDescription()
  - generateTaskChecklist()
  - summarizeTaskComments()
  - generateDeadlineReminder()
  - generateChatResponse()
- ✅ lib/ai/client.ts - OpenAI клиент с retry логикой
- ✅ lib/ai/prompts.ts - все шаблоны промптов
- ✅ AIAssistant.tsx - полноценный UI
- ✅ AIAgentsRepository - управление AI-агентами
- ✅ API endpoints:
  - `/api/ai/generate-description`
  - `/api/ai/summarize-comments`
  - `/api/pm/projects/[id]/ai-agents`
- ✅ agent-responses.ts - обработка упоминаний агентов
- ✅ ProjectAIAgents.tsx - управление агентами проекта
- ✅ Интеграция: агенты отвечают в чате при упоминании
- ✅ Unit и E2E тесты

**Созданные файлы:**

- `apps/api/src/services/ai-service.ts`
- `apps/web/lib/ai/client.ts`
- `apps/web/lib/ai/prompts.ts`
- `apps/web/lib/ai/agent-responses.ts`
- `apps/web/components/pm/AIAssistant.tsx`
- `apps/web/components/pm/ProjectAIAgents.tsx`
- `apps/api/src/repositories/ai-agents-repository.ts`
- `apps/web/app/api/ai/generate-description/route.ts`
- `apps/web/app/api/ai/summarize-comments/route.ts`
- `apps/web/app/api/pm/projects/[id]/ai-agents/route.ts`

---

## Stage N: Расширенная функциональность AI

**Статус:** ❌ Не начат  
**Приоритет:** P3  
**Оценка времени:** 20-25 дней  
**Зависимости:** Stage M (базовая функциональность AI), Stage K (диаграмма Ганта)

### Цель (Stage N)

Реализовать расширенную функциональность AI-ассистента для автоматизации и планирования.

### Задачи (Stage N)

1. **Автоматическое создание задач**
   - Разбиение проекта на этапы
   - Генерация структуры задач из описания проекта
   - Предложение сроков и приоритетов

2. **Рекомендации по назначению**
   - Анализ загруженности участников
   - Рекомендации по назначению задач
   - Учёт навыков участников (если есть данные)

3. **Анализ перегрузок**
   - Выявление перегрузок команды по датам
   - Предложения по перераспределению задач
   - Интеграция с диаграммой Ганта

4. **Массовые операции**
   - Массовое изменение статусов по команде
   - Массовое изменение дедлайнов
   - Добавление типовых подзадач

5. **Планирование проекта**
   - Генерация плана проекта из цели
   - Предложение этапов и вех
   - Оценка сроков проекта

### Критерии готовности (DoD) (Stage N)

- ✅ AI создаёт структуру задач из описания проекта
- ✅ AI рекомендует назначение задач участникам
- ✅ AI анализирует перегрузки и предлагает решения
- ✅ Все действия требуют подтверждения

### Файлы для создания/изменения (Stage N)

- `apps/api/src/services/ai-planning-service.ts` (новый)
- `apps/web/lib/ai/planning.ts` (новый)
- `apps/web/components/pm/AIProjectPlanner.tsx` (новый)
- `apps/web/app/api/ai/generate-project-structure/route.ts` (новый)
- `apps/web/app/api/ai/analyze-workload/route.ts` (новый)

### Результаты (Stage N)

**Не реализовано:**

- ❌ ai-planning-service.ts (не существует)
- ❌ AIProjectPlanner.tsx (не существует)
- ❌ `/api/ai/generate-project-structure` (не существует)
- ❌ `/api/ai/analyze-workload` (не существует)
- ❌ Рекомендации по назначению задач
- ❌ Анализ загруженности команды
- ❌ Массовые операции через AI
- ❌ Генерация структуры проекта

**Приоритет:** P3 (Низкий) - можно отложить или пропустить

---

## История изменений

| Дата       | Изменение                      | Автор    |
| ---------- | ------------------------------ | -------- |
| 2026-01-06 | Создан план реализации PM Core | AI Agent |
| 2024-XX-XX | Stage H завершён               | Team     |
| 2024-XX-XX | Stage I завершён (95%)         | Team     |
| 2024-XX-XX | Stage J завершён               | Team     |
| 2024-XX-XX | Stage K завершён               | Team     |
| 2024-XX-XX | Stage L завершён (95%)         | Team     |
| 2024-XX-XX | Stage M завершён               | Team     |

---

## Будущие улучшения

Идеи, возникшие в процессе реализации:

| Идея                                     | Приоритет | Дата       | Контекст | Статус |
| ---------------------------------------- | --------- | ---------- | -------- | ------ |
| Email уведомления                        | P2        | 2024-XX-XX | Stage I  | ⏳     |
| Push уведомления                         | P2        | 2024-XX-XX | Stage I  | ⏳     |
| История версий файлов                    | P2        | 2024-XX-XX | Stage J  | ⏳     |
| Структурирование файлов по папкам        | P2        | 2024-XX-XX | Stage J  | ⏳     |
| Unit/E2E тесты для WebSocket             | P2        | 2024-XX-XX | Stage L  | ⏳     |
| POST endpoint для приглашения участников | P2        | 2024-XX-XX | Stage I  | ⏳     |
| Автоматическая проверка дедлайнов        | P2        | 2024-XX-XX | Stage I  | ⏳     |
| Расширенная AI функциональность          | P3        | 2024-XX-XX | Stage N  | ⏳     |

---

**Последнее обновление:** 2026-01-06