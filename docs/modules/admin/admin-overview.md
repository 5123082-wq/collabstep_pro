# Админка — Обзор

**Статус:** draft  
**Владелец:** product/engineering  
**Создан:** 2026-01-07  
**Последнее обновление:** 2026-01-07

Обзор административной панели, ключевых функций и точек доступа.

## 1) Назначение

Документ описывает админ-раздел платформы: управление пользователями, данными, шаблонами и модулями.

## 2) Текущее состояние

- UI-страницы админ-панели присутствуют.
- Часть API реализована в `/api/admin/*`.
- Доступ ограничен ролями администратора (NEEDS_CONFIRMATION по правилам).
- **Управление пользователями**: полный функционал редактирования статуса и ролей через UI.

## 3) Пользовательские маршруты (UI)

- `/admin` — главный экран
- `/admin/users` — пользователи
- `/admin/roles` — роли и доступы
- `/admin/templates` — шаблоны
- `/admin/templates/[id]/tasks` — задачи шаблона
- `/admin/features` — feature flags
- `/admin/data` — данные и операции
- `/admin/data/users/[userId]` — данные конкретного пользователя
- `/admin/audit` — аудит
- `/admin/support` — поддержка
- `/admin/releases` — релизы
- `/admin/segments` — сегменты (если используются)
- `/admin/ai-agents` — AI агенты
- `/admin/ai-assistant-indexing` — индексирование AI

## 4) API endpoints

- `GET /api/admin/users` — список пользователей
- `GET /api/admin/users/[id]` — детали пользователя
- `PATCH /api/admin/users/[id]` — обновление пользователя (статус, роли, testerAccess, notes)
- `DELETE /api/admin/users/[id]` — удаление пользователя
- `GET /api/admin/data/stats` — агрегаты по данным
- `POST /api/admin/data/clear` — очистка данных
- `GET /api/admin/data/users/[userId]/items` — данные пользователя
- `GET /api/admin/templates` — список шаблонов
- `GET /api/admin/templates/[id]` — шаблон
- `POST /api/admin/templates/[id]/tasks` — задачи шаблона
- `PATCH /api/admin/templates/[id]/tasks/[taskId]` — обновление задачи шаблона
- `GET /api/admin/modules` — список модулей
- `GET /api/admin/modules/[id]` — модуль
- `GET /api/admin/stats` — сводные метрики
- `GET /api/admin/ai-assistant/indexing-config` — настройки индексирования
- `POST /api/admin/ai-assistant/reindex` — переиндексация

## 5) Roadmap (draft)

### P0 — базовая фиксация

- [ ] Зафиксировать правила доступа и список ролей администратора
- [ ] Описать назначение всех админ-страниц
- [ ] Согласовать аудит и логирование действий администратора

### P1 — процессы

- [ ] Описать стандартные операции (поддержка, восстановление данных)
- [ ] Определить политики по удалению/очистке данных

### P2 — интеграции

- [ ] Описать интеграции админ-панели с аналитикой
- [ ] Определить процедуры релизов и миграций

## NEEDS_CONFIRMATION

- Полный перечень прав администратора
- Какие операции доступны без подтверждения
- Политика аудита и хранения логов

---

**Связанные документы:**
- [Управление пользователями](./admin-users-management.md)
- [Управление данными](./admin-data-management.md)
- [Обзор платформы](../../platform/overview.md)
