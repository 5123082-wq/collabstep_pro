# Руководство для AI агентов по обновлению документации

**Статус:** stable  
**Владелец:** product + engineering  
**Создан:** 2026-01-07  
**Последнее обновление:** 2026-01-07

## Назначение

Это руководство предназначено для AI агентов, которые обновляют документацию при реализации фич. Оно содержит четкие правила, процессы и примеры работы с документацией.

> **ВАЖНО:** Это руководство дополняет [правила работы с документацией](../../.cursor/rules/documentation.mdc). При конфликте приоритет имеют правила из `.cursor/rules/documentation.mdc`.

> **См. также:**
> - [Чеклист для PR](./docs-pr-checklist.md) - проверка перед созданием PR
> - [Руководство по миграции](./docs-migration-guide.md) - как работать с документацией
> - [Правила работы с документацией](../../.cursor/rules/documentation.mdc) - детальные правила

---

## Правила работы с документацией

### 1. НЕ изобретать факты

**Критически важно:** Если информация отсутствует или неясна, НЕ придумывайте её. Вместо этого:

1. Помечайте `NEEDS_CONFIRMATION` с указанием:
   - **Владелец** - кто должен подтвердить (product, engineering, design)
   - **Дедлайн** - когда ожидается подтверждение (YYYY-MM-DD или "TBD")
   - **Контекст** - почему требуется подтверждение

2. Сохраняйте оригинальные документы в архиве до завершения миграции

3. Проверяйте актуальность информации, сверяя с кодом

**Пример:**

```markdown
> **NEEDS_CONFIRMATION:** Требуется уточнение бизнес-логики для определения прав доступа к новой фиче
> **Владелец:** product
> **Дедлайн:** 2026-01-15
> **Контекст:** Новая фича X требует определения прав доступа, но в существующих документах нет информации
```

### 2. Сохранять оригиналы

**Правило:** До завершения миграции сохраняйте оригинальные документы в архиве (`docs/archive/`).

**При миграции документа:**

1. Создать новый документ в новой структуре
2. Переместить оригинал в `docs/archive/` с датой
3. Обновить ссылки на новый документ

### 3. Проверять актуальность

**Правило:** Перед обновлением документации проверяйте актуальность информации.

**Источники для проверки:**

1. **Код** - типы, интерфейсы, схемы в `apps/api/src/` и `apps/web/`
2. **Существующие документы** - проверьте, нет ли противоречий
3. **Пользователь** - если информация отсутствует, спросите пользователя

**Пример проверки:**

```typescript
// Проверка типов в коде
// apps/api/src/repositories/pm-repository.ts
export interface Project {
  id: string;
  name: string;
  // ... другие поля
}

// Обновить документацию на основе этого интерфейса
```

### 4. Использовать шаблоны

**Правило:** При создании новых документов используйте шаблоны.

**Доступные шаблоны:**

- `docs/modules/_template-module.md` - для создания `_module.md`
- `docs/modules/_template-implementation-plan.md` - для создания `_implementation-plan.md`
- `docs/architecture/adr/_template-adr.md` - для создания ADR документов

**Процесс:**

1. Скопировать шаблон
2. Заполнить все секции
3. Удалить примеры и инструкции из шаблона
4. Обновить метаданные

---

## Обновление документации при реализации фич

### Шаг 1: Определить какие документы нужно обновить

**Проверочный список:**

- [ ] Модульная документация (`docs/modules/<module>/_module.md`)
- [ ] Поддокументы модуля (например, `projects.md`, `tasks.md`)
- [ ] План реализации (`_implementation-plan.md` или `ROADMAP.md`)
- [ ] Platform документация:
  - [ ] `overview.md` (если изменилась структура модулей)
  - [ ] `glossary.md` (если появились новые термины)
  - [ ] `roles-permissions.md` (если изменились права)
  - [ ] `analytics-events.md` (если добавились события)
- [ ] `changelog.md` (если фича вышла)
- [ ] `CONTINUITY.md` (если применимо)

### Шаг 2: Обновить модульную документацию

**Файл:** `docs/modules/<module>/_module.md`

**Что обновить:**

1. **Раздел "Purpose"** - добавить описание новой функциональности
2. **Раздел "Key objects"** - добавить новые объекты (если появились)
3. **Раздел "Top user scenarios"** - добавить пользовательский сценарий
4. **Таблица "Functional catalog"** - добавить запись:

```markdown
| Feature | What it does | Doc | Status |
|---------|--------------|-----|--------|
| Новая фича X | Описание фичи | [new-feature.md](./new-feature.md) | draft |
```

5. **Обновить метаданные:**
   - "Последнее обновление": текущая дата
   - "Статус": draft → review → stable (по мере готовности)

**Если нужен отдельный поддокумент:**

1. Создать `docs/modules/<module>/new-feature.md`
2. Описать функциональность детально
3. Добавить ссылку в таблицу "Functional catalog"

### Шаг 3: Обновить планы реализации

**Файлы:** `docs/modules/<module>/_implementation-plan.md` или `docs/ROADMAP.md`

**При старте этапа:**

1. Найти этап в таблице
2. Обновить статус: ⏳ → 🔄
3. Добавить дату начала: `2026-01-07`
4. Обновить "Последнее обновление" в заголовке

**При завершении этапа:**

1. Найти этап в таблице
2. Обновить статус: 🔄 → ✅
3. Добавить дату завершения: `2026-01-15`
4. Обновить "Последнее обновление" в заголовке

**Пример:**

```markdown
| Этап | Описание | Статус | Приоритет | Начало | Завершение | Зависимости |
|------|----------|--------|-----------|--------|------------|-------------|
| Stage I | Уведомления | 🔄 В работе | P0 | 2026-01-07 | - | Stage H |  ← Обновлено
```

### Шаг 4: Обновить ROADMAP.md

**Файл:** `docs/ROADMAP.md`

**Если есть план реализации в ROADMAP:**

1. Найти раздел модуля
2. Найти план реализации
3. Обновить статус этапа в таблице (аналогично шагу 3)
4. Обновить "Последнее обновление" в заголовке плана
5. Обновить общий статус плана (например, "50% завершено")

**Пример:**

```markdown
#### План реализации раздела "Проекты" (Stage H-N)

**Статус:** 🔄 50% завершено (3 из 7 этапов)  ← Обновлено
**Последнее обновление:** 2026-01-07  ← Обновлено
```

### Шаг 5: Обновить changelog.md

**Файл:** `docs/platform/changelog.md`

**Только если фича вышла в релиз:**

1. Добавить запись в раздел `[Unreleased]` или создать новый раздел с версией
2. Использовать правильные разделы:
   - **Добавлено** - новые фичи
   - **Изменено** - изменения существующих фич
   - **Исправлено** - исправления багов

**Пример:**

```markdown
## [Unreleased]

### Добавлено
- Комментарии к задачам с упоминаниями (@mentions)
- Новая фича X  ← Добавлено
```

### Шаг 6: Проверить ссылки и метаданные

**Проверки:**

1. **Ссылки:**
   - Все внутренние ссылки работают (нет битых ссылок)
   - Относительные пути правильные
   - Ссылки на существующие документы

2. **Метаданные:**
   - "Последнее обновление" обновлено на текущую дату
   - "Статус" актуален (draft/review/stable)
   - "Владелец" указан правильно

3. **Markdownlint:**
   - Файл проходит проверку markdownlint
   - Нет ошибок форматирования

---

## Ведение хронологии

### Обновление CONTINUITY.md

**Файл:** `CONTINUITY.md` (в корне репозитория)

**Когда обновлять:**

- При завершении значимых этапов
- При изменении ключевых решений
- При изменении текущих задач

**Что обновлять:**

1. **Раздел "State"** - добавить завершенные задачи:

```markdown
## State

- Tasks 01–16 complete (file manager).
- Новая фича X реализована (2026-01-07)  ← Добавлено
```

2. **Раздел "Now"** - обновить текущие задачи:

```markdown
### Now

- ✅ **РЕОРГАНИЗАЦИЯ ДОКУМЕНТАЦИИ: Фаза 7 в процессе** (2026-01-07)  ← Обновлено
```

3. **Раздел "Key Decisions"** - добавить новые решения (если есть)

### Обновление дат в документах

**Правило:** При каждом изменении документа обновлять "Последнее обновление".

**Формат:**

```markdown
**Последнее обновление:** YYYY-MM-DD
```

**Где обновлять:**

- В заголовке документа (метаданные)
- В таблицах планов (даты начала и завершения этапов)
- В истории изменений (если есть)

### Ведение истории изменений

**В планах реализации можно вести историю:**

```markdown
## История изменений

| Дата | Изменение | Автор |
|------|-----------|-------|
| 2026-01-07 | Обновлен статус Stage I на "В работе" | AI Agent |
| 2026-01-15 | Завершен Stage I | AI Agent |
```

---

## Работа с планами

### Обновление статусов этапов

**Статусы:**

- ✅ **Завершён** - этап полностью реализован
- 🔄 **В работе** - этап активно разрабатывается
- ⏳ **Ожидает** - этап запланирован, но не начат
- ❌ **Заблокирован** - этап не может быть начат из-за зависимостей
- ⚠️ **Частично** - этап частично выполнен

**Процесс обновления:**

1. Найти этап в таблице плана
2. Обновить статус
3. Добавить дату начала (при старте) или дату завершения (при завершении)
4. Обновить "Последнее обновление" в заголовке документа

### Добавление дат начала и завершения

**Формат дат:** `YYYY-MM-DD`

**При старте этапа:**

```markdown
| Stage I | Уведомления | 🔄 В работе | P0 | 2026-01-07 | - | Stage H |
                                                      ↑
                                              Дата начала
```

**При завершении этапа:**

```markdown
| Stage I | Уведомления | ✅ Завершён | P0 | 2026-01-07 | 2026-01-15 | Stage H |
                                                                    ↑
                                                            Дата завершения
```

### Отслеживание зависимостей

**Правило:** При обновлении зависимостей обновлять колонку "Зависимости" в таблице.

**Формат:**

```markdown
| Этап | Описание | Статус | Приоритет | Начало | Завершение | Зависимости |
|------|----------|--------|-----------|--------|------------|-------------|
| Stage I | Уведомления | ⏳ | P0 | - | - | Stage H |  ← Зависимость от Stage H
```

**Если зависимость изменилась:**

1. Обновить колонку "Зависимости"
2. Указать причину изменения в описании этапа или истории изменений

---

## Работа с NEEDS_CONFIRMATION

### Когда помечать NEEDS_CONFIRMATION

**Ситуации:**

1. **Отсутствие данных** - информация отсутствует в существующих документах
2. **Неопределенность** - информация неясна или противоречива
3. **Требуется решение** - нужно решение от владельца (product, engineering, design)
4. **Проверка актуальности** - информация может быть устаревшей

**НЕ помечать NEEDS_CONFIRMATION:**

- Если информация есть в коде и может быть проверена
- Если информация есть в существующих документах и актуальна
- Если можно сделать разумное предположение на основе контекста

### Формат метки NEEDS_CONFIRMATION

**Обязательные поля:**

```markdown
> **NEEDS_CONFIRMATION:** [краткое описание того, что требует подтверждения]
> **Владелец:** product | engineering | design | [другой]
> **Дедлайн:** YYYY-MM-DD | TBD
> **Контекст:** [подробное описание, почему требуется подтверждение, откуда возник вопрос]
```

**Пример:**

```markdown
> **NEEDS_CONFIRMATION:** Требуется уточнение прав доступа к новой фиче "Комментарии"
> **Владелец:** product
> **Дедлайн:** 2026-01-15
> **Контекст:** Новая фича "Комментарии" требует определения прав доступа (кто может создавать/редактировать/удалять комментарии), но в существующих документах нет информации о правах для комментариев. В коде есть проверки прав, но требуется подтверждение бизнес-логики.
```

### Как закрывать NEEDS_CONFIRMATION

**Процесс:**

1. **Получить подтверждение:**
   - От владельца (product, engineering, design)
   - Из кода (если информация есть в коде)
   - Из существующих документов (если информация найдена)

2. **Обновить документ:**
   - Заменить `NEEDS_CONFIRMATION` на актуальную информацию
   - Удалить метку `NEEDS_CONFIRMATION`
   - Обновить "Последнее обновление"

3. **Добавить примечание (опционально):**
   - Указать источник подтверждения
   - Указать дату закрытия

**Пример:**

**До:**

```markdown
> **NEEDS_CONFIRMATION:** Требуется уточнение прав доступа к комментариям
> **Владелец:** product
> **Дедлайн:** 2026-01-15
> **Контекст:** ...
```

**После:**

```markdown
## Права доступа к комментариям

Права доступа к комментариям определяются ролью пользователя в проекте:
- **owner, manager, contributor** - могут создавать, редактировать и удалять свои комментарии
- **viewer** - могут только просматривать комментарии

> **Примечание:** Подтверждено product 2026-01-15
```

### Где искать информацию для закрытия

**Приоритет источников:**

1. **Код** - типы, интерфейсы, схемы, проверки прав:
   - `apps/api/src/repositories/` - репозитории
   - `apps/api/src/services/` - сервисы
   - `apps/web/lib/schemas/` - схемы валидации
   - `apps/web/app/api/` - API endpoints

2. **Существующие документы:**
   - `docs/platform/roles-permissions.md` - права доступа
   - `docs/modules/<module>/_module.md` - описание модуля
   - `docs/ROADMAP.md` - планы реализации

3. **Пользователь:**
   - Если информация отсутствует в коде и документах, спросите пользователя

**Пример поиска:**

```typescript
// 1. Проверить код
// apps/api/src/repositories/pm-repository.ts
export async function canUserComment(projectId: string, userId: string): Promise<boolean> {
  const member = await getProjectMember(projectId, userId);
  return member.role === 'owner' || member.role === 'manager' || member.role === 'contributor';
}

// 2. Обновить документацию на основе кода
```

---

## Примеры обновления документации при реализации фич

### Пример 1: Реализация новой фичи "Комментарии"

**Шаг 1: Определить документы для обновления**

- ✅ `docs/modules/projects-tasks/projects-tasks-overview.md`
- ✅ `docs/modules/projects-tasks/projects-tasks-comments.md` (создать новый)
- ✅ `docs/modules/projects-tasks/projects-tasks-implementation-plan.md`
- ✅ `docs/ROADMAP.md`
- ✅ `docs/platform/changelog.md` (если фича вышла)

**Шаг 2: Обновить `_module.md`**

```markdown
## 4) Functional catalog

| Feature | What it does | Doc | Status |
|---------|--------------|-----|--------|
| Комментарии | Комментарии к задачам с упоминаниями | [comments.md](./comments.md) | stable |  ← Добавлено
```

**Шаг 3: Обновить план реализации**

```markdown
| Stage H | Комментарии к задачам | ✅ Завершён | P0 | 2024-10-01 | 2024-10-10 | - |  ← Обновлено
```

**Шаг 4: Обновить ROADMAP**

```markdown
**Статус:** ✅ 14% завершено (1 из 7 этапов)  ← Обновлено
**Последнее обновление:** 2024-10-10  ← Обновлено
```

**Шаг 5: Обновить changelog (если фича вышла)**

```markdown
## [Unreleased]

### Добавлено
- Комментарии к задачам с упоминаниями (@mentions)  ← Добавлено
```

### Пример 2: Обновление статуса этапа

**До:**

```markdown
| Stage I | Уведомления | ⏳ Ожидает | P0 | - | - | Stage H |
```

**После (при старте):**

```markdown
| Stage I | Уведомления | 🔄 В работе | P0 | 2026-01-07 | - | Stage H |  ← Обновлено
```

**После (при завершении):**

```markdown
| Stage I | Уведомления | ✅ Завершён | P0 | 2026-01-07 | 2026-01-15 | Stage H |  ← Обновлено
```

---

## Чеклист перед завершением работы

Перед завершением обновления документации проверьте:

- [ ] Все необходимые документы обновлены
- [ ] Метаданные обновлены ("Последнее обновление")
- [ ] Статусы этапов обновлены (если применимо)
- [ ] Даты добавлены (начало/завершение этапов)
- [ ] Ссылки проверены (нет битых ссылок)
- [ ] NEEDS_CONFIRMATION помечены (если есть неопределенности)
- [ ] Markdownlint проходит без ошибок
- [ ] CONTINUITY.md обновлен (если применимо)

---

## Связанные документы

- [Чеклист для PR](./docs-pr-checklist.md)
- [Руководство по миграции](./docs-migration-guide.md)
- [Процесс релиза](./release-process.md)
- [Правила работы с документацией](../../.cursor/rules/documentation.mdc)
- [План реорганизации документации](../reorganization/PLAN_DOCUMENTATION_REORGANIZATION.md)
- [ROADMAP](../ROADMAP.md)
- [Changelog](../platform/changelog.md)
- [Шаблон модуля](../modules/_template-module.md)
- [Шаблон плана реализации](../modules/_template-implementation-plan.md)

---

**Последнее обновление:** 2026-01-07

