# 🔐 Настройка AI ключей (безопасно)

## ✅ Что было сделано

1. **Создан файл** `apps/web/.env.local` для безопасного хранения ключей
2. **Обновлён API** - ключи теперь используются только на сервере
3. **Убрано хранение** ключей в localStorage браузера
4. **Добавлена документация** по структуре env файлов

## 📝 Как добавить свои ключи

### 1. Откройте файл:
```bash
apps/web/.env.local
```

### 2. Замените значения на свои:

**Для OpenAI:**
```bash
OPENAI_API_KEY=sk-proj-ваш-реальный-ключ-здесь
```

**Для Yandex Cloud:**
```bash
YANDEX_API_KEY=AQVN...ваш-реальный-ключ
YANDEX_FOLDER_ID=b1gbd9cc2ueuq6k6elnc
YANDEX_MODEL_URI=yandexgpt/latest
```

### 3. Перезапустите сервер:
```bash
pnpm dev
```

## 🔒 Безопасность

- ✅ Ключи хранятся только на сервере в `.env.local`
- ✅ Ключи НЕ отправляются с клиента
- ✅ Ключи НЕ доступны в браузере
- ✅ Файл `.env.local` в `.gitignore` (не коммитится)

## 📁 Зачем разные env файлы?

См. подробное объяснение: [ENV_FILES_EXPLANATION.md](./ENV_FILES_EXPLANATION.md)

**Кратко:**
- `.env` - общие переменные (БД, auth)
- `.env.local` - ⭐ **ваши секретные ключи** (AI, токены)
- `.env.example` - шаблон для команды (безопасно коммитить)

## ⚠️ Важно

1. **Никогда не коммитьте** `.env.local` в git
2. **Не делитесь** содержимым `.env.local` публично
3. **Используйте разные ключи** для dev/staging/production

## 🧪 Тестирование

Для тестирования без реальных ключей используйте:
- `TEST_KEY` для OpenAI
- `YANDEX_TEST_KEY` для Yandex

Эти значения вернут мок-данные.

