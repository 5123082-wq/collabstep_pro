# Восстановление пользователей после очистки базы данных

## ⚠️ Критическая ситуация

Скрипт `cleanup-users-db.ts` был запущен и удалил всех пользователей кроме `admin.demo@collabverse.test`.

**Текущее состояние:**

- В базе данных остался только 1 пользователь (администратор)
- Все остальные 199 пользователей были удалены
- Связанные данные (организации, проекты) были переназначены на администратора

## 🔍 Проверка возможности восстановления

### Вариант 1: Vercel Postgres автоматические бэкапы

Vercel Postgres предоставляет автоматические бэкапы. Проверьте:

1. **Войдите в Vercel Dashboard**
   - Перейдите в ваш проект
   - Откройте раздел **Storage** → **Postgres**
   - Найдите раздел **Backups** или **Point-in-time recovery**

2. **Проверьте доступные бэкапы**
   - Vercel Postgres обычно хранит бэкапы на 7 дней
   - Проверьте, есть ли бэкап до момента запуска скрипта `cleanup-users-db.ts`

3. **Восстановление из бэкапа**
   - Если бэкап найден, можно восстановить базу данных на момент до удаления
   - **ВНИМАНИЕ**: Это восстановит ВСЮ базу данных, включая все изменения после бэкапа

### Вариант 2: Проверка логов базы данных

Если используется Vercel Postgres с логированием:

1. Проверьте логи транзакций
2. Найдите момент удаления пользователей
3. Определите, можно ли восстановить данные из логов

### Вариант 3: Внешние бэкапы

Проверьте, есть ли у вас:

- Ручные SQL дампы базы данных
- Автоматические бэкапы на другом сервере
- Экспорты данных через API

## 🛠️ Действия для восстановления

### Если найден бэкап Vercel Postgres

1. **Создайте новую базу данных** (для безопасности)
2. **Восстановите бэкап** в новую базу
3. **Проверьте данные** в восстановленной базе
4. **Перенесите данные** в основную базу (если нужно)

### Если бэкапа нет

К сожалению, без бэкапа восстановление невозможно. Пользователям придется:

- Зарегистрироваться заново через форму регистрации
- Восстановить свои организации и проекты вручную

## 📋 Предотвращение в будущем

### Рекомендации:

1. **Всегда создавайте бэкап перед запуском скриптов очистки**

   ```bash
   # Пример создания дампа
   pg_dump $POSTGRES_URL > backup_$(date +%Y%m%d_%H%M%S).sql
   ```

2. **Добавьте подтверждение в скрипт очистки**
   - Скрипт должен требовать явного подтверждения
   - Показывать количество пользователей, которые будут удалены
   - Требовать ввод специального кода для подтверждения

3. **Используйте транзакции с откатом**
   - Оберните операции удаления в транзакцию
   - Добавьте возможность отката изменений

4. **Настройте автоматические бэкапы**
   - Включите автоматические бэкапы в Vercel Postgres
   - Настройте регулярные экспорты данных

## 🔒 Безопасность скриптов

### Улучшения для `cleanup-users-db.ts`:

1. Добавить проверку окружения (не запускать в production без подтверждения)
2. Добавить предварительный просмотр данных для удаления
3. Добавить возможность отката (soft delete вместо hard delete)
4. Логировать все операции удаления

## 📞 Контакты для помощи

Если нужна помощь с восстановлением:

1. Проверьте документацию Vercel Postgres: https://vercel.com/docs/storage/vercel-postgres
2. Обратитесь в поддержку Vercel для восстановления бэкапа
3. Проверьте логи приложения на наличие информации о пользователях

## 📝 История проблемы

- **Дата обнаружения**: 2026-01-03
- **Причина**: Запуск скрипта `scripts/cleanup-users-db.ts`
- **Результат**: Удалены все пользователи кроме `admin.demo@collabverse.test`
- **Статус**: Требуется проверка бэкапов Vercel Postgres
