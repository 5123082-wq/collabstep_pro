# Анализ реализации komanda.md

Этот документ содержит анализ текущего состояния проекта по сравнению с требованиями, описанными в `docs/development/komanda.md`.

## Общее резюме

Значительная часть функционала для **Каталога исполнителей** и **Профилей** уже реализована как на бэкенде, так и на фронтенде. Однако, отсутствуют ключевые пользовательские сценарии (User Flows), описанные в документе, особенно касающиеся **регистрации** и **расширенного приглашения** в команду/проект.

---

## 1. Регистрация и Онбординг (Раздел 2)

**Статус: ❌ Не реализовано**

В `komanda.md` описан сценарий:
1. Базовая регистрация.
2. Выбор роли: "Я представляю бизнес" или "Я исполнитель".
3. В зависимости от выбора — создание Организации или Профиля исполнителя.

**Текущая реализация:**
- Работает только базовая регистрация (Имя, Email, Пароль).
- После регистрации пользователь попадает в систему без выбора роли.
- Создание организации или профиля происходит только через настройки, по инициативе пользователя.

**Что нужно добавить:**
- Экран выбора роли после регистрации (`/onboarding/role-selection`).
- Флоу создания организации (`/onboarding/create-organization`).
- Флоу создания профиля исполнителя (`/onboarding/create-profile`).

---

## 2. Каталог исполнителей и Профиль (Разделы 3 и 5)

**Статус: ✅ Частично реализовано**

**Реализовано:**
- **БД**: Таблица `performer_profile` существует со всеми нужными полями.
- **UI**: Страница каталога (`SpecialistsCatalog.tsx`) с фильтрами (Роль, Навыки, Ставка) работает.
- **Профиль**: Страница настроек профиля (`PerformerProfileForm.tsx`) позволяет заполнить данные и включить "Показывать в каталоге".

**Что можно улучшить:**
- Визуальное отображение карточки в каталоге (сейчас базовое).
- Добавить модерацию/верификацию (упомянуто как опциональное).

---

## 3. Приглашение в команду (Разделы 4 и 6)

**Статус: ⚠️ Частично реализовано (есть пробелы)**

В `komanda.md` описан детальный флоу приглашения из каталога:
1. Кнопка "Пригласить в команду".
2. Модальное окно с выбором:
   - Организации.
   - **Роли в организации** (Дизайнер, Разработчик и т.д.).
   - **Проекта** (опционально "Сразу добавить в проект").

**Текущая реализация:**
- **Кнопка**: Есть в каталоге.
- **Модальное окно**: `InvitePerformerModal.tsx` позволяет выбрать **только Организацию**.
- **Бэкенд**: Endpoint `/api/performers/[userId]/invite-to-organization` поддерживает `organizationId` и `projectId`, но фронтенд не отправляет `projectId`. Роль в организации жестко не задается (вероятно, default).

**Что нужно добавить:**
- **UI Модального окна**:
  - Добавить выпадающий список "Роль в команде".
  - Добавить чекбокс "Добавить в проект" и выпадающий список активных проектов.
- **Бэкенд**:
  - Обновить логику приглашения, чтобы сохранять выбранную роль.
  - Убедиться, что приглашение в проект создается корректно вместе с приглашением в организацию.

---

## 4. Управление командой проекта (Раздел 5)

**Статус: ⚠️ Требует проверки интеграции**

**Реализовано:**
- Таблицы `project_member` и `project_invite`.
- Базовое добавление участников в проект.

**Что нужно добавить:**
- Единый UI выбора исполнителя в задаче/проекте, который ищет и по участникам организации, и (если не найден) предлагает пригласить по email или найти в каталоге. Сейчас эти функции, вероятно, разрознены.

---

## 5. Редактирование профиля пользователя (New Request)

**Статус: ✅ Реализовано**

**Реализованные компоненты:**
- ✅ `/settings/profile`: Полноценная страница редактирования профиля
- ✅ API: `/api/me/profile` (GET/PATCH)
- ✅ Unit-тесты: `tests/unit/me-profile-api.spec.ts`

**Реализованные поля:**
- ✅ Аватар (URL)
- ✅ Имя отображаемое
- ✅ Email (только для чтения)
- ✅ Должность / Title
- ✅ Отдел / Департамент
- ✅ Локация (Город/Страна)
- ✅ **Часовой пояс** (дополнительно добавлено)

**Особенности:**
- Валидация полей (минимальная длина имени, формат URL)
- Возможность удаления аватара (пустая строка)
- Автоматическое обновление в sidebar
- Toast-уведомления об успехе/ошибке

---

## Итоговый план действий (To-Do)

### ✅ ВЫПОЛНЕНО

1.  **Реализовать Онбординг**:
    - [x] Создать страницу выбора роли (`/onboarding/page.tsx`)
    - [x] Реализовать форму создания Организации (`/onboarding/create-organization/page.tsx`)
    - [x] Реализовать форму создания Профиля исполнителя (`/onboarding/create-profile/page.tsx`)
    - [x] Создать layout для онбординга (`/onboarding/layout.tsx`)

2.  **Улучшить Приглашения**:
    - [x] Доработать `InvitePerformerModal.tsx`: добавить выбор Проекта
    - [x] API уже поддерживает `projectId` для привязки к проекту
    - ⚠️ Выбор роли **не реализован** - требует миграции БД (добавление поля `role` в таблицу `organizationInvites`)

3.  **Профиль пользователя**:
    - [x] Реализовать страницу редактирования профиля (`/settings/profile`)
    - [x] Добавить поля: Аватар, Имя, Title, Department, Location, Timezone
    - [x] Написать unit-тесты для API профиля
    - [x] Исправить критические TypeScript ошибки

### 📋 Осталось (опционально)

4.  **Интеграция в Проекты**:
    - [ ] Проверить и доработать UI добавления участника в проект (поиск по каталогу прямо из проекта)

5.  **Безопасность** (не входило в текущий план):
    - [ ] Добавить возможность смены пароля
    - [ ] Настройки уведомлений (Email, Push)

---

## Статистика реализации

- **Создано файлов**: 5
- **Изменено файлов**: 3
- **Написано тестов**: 8 test cases
- **Статус сборки**: ✅ PASSED
- **Статус TypeCheck**: ✅ PASSED
