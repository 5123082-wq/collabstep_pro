# Пошаговая инструкция: Настройка локального тестирования

**Дата:** 2026-01-06  
**Статус:** Готово к выполнению

---

## ✅ Что уже сделано

1. ✅ Исправлен `apps/api/drizzle.config.ts` — теперь поддерживает `POSTGRES_URL` как fallback
2. ✅ Созданы скрипты для удобного запуска тестов:
   - `pnpm test:local:setup` — запуск локального Postgres
   - `pnpm test:local` — запуск тестов с локальной БД
   - `pnpm test:local:cleanup` — остановка локальной БД
   - `pnpm test:local:env` — запуск тестов с переменными из окружения
3. ✅ Создана документация: `docs/getting-started/local-testing.md`

---

## 📋 Пошаговый план выполнения

### Шаг 1: Проверка текущего состояния

**Цель:** Убедиться, что всё готово к тестированию.

```bash
# Проверить, что исправление применено
cat apps/api/drizzle.config.ts | grep -A 5 "POSTGRES_URL"

# Проверить, что скрипты созданы
ls -la scripts/test/*.sh

# Проверить, что скрипты исполняемые
ls -l scripts/test/*.sh
```

**Ожидаемый результат:**
- `drizzle.config.ts` содержит логику с `POSTGRES_URL` как fallback
- Все скрипты существуют и имеют права на выполнение

---

### Шаг 2: Выбор варианта тестирования

У вас есть два варианта:

#### Вариант A: Локальный Postgres через Docker (Рекомендуется)

**Преимущества:**
- Полное соответствие CI окружению
- Изоляция от продакшен БД
- Быстрый запуск и остановка

**Требования:**
- Docker установлен и запущен

#### Вариант B: Использование существующей Neon БД

**Преимущества:**
- Не требует Docker
- Переменные уже настроены в `.env.local`

**Недостатки:**
- ⚠️ Риск изменения данных в продакшен БД
- Медленнее из-за сетевого подключения

**Рекомендация:** Использовать Вариант A для безопасности.

---

### Шаг 3A: Настройка локального Postgres (Вариант A)

```bash
# 1. Запустить локальный Postgres
pnpm test:local:setup

# Ожидаемый вывод:
# 🐘 Запуск PostgreSQL контейнера для тестов...
# ✅ PostgreSQL готов!
# 📝 Переменные окружения для использования:
#    export DATABASE_URL=postgresql://test:test@localhost:5432/testdb
#    export POSTGRES_URL=postgresql://test:test@localhost:5432/testdb
```

**Проверка:**
```bash
# Проверить, что контейнер запущен
docker ps | grep collabverse-postgres-test

# Должен быть виден контейнер со статусом "Up"
```

---

### Шаг 3B: Использование существующей БД (Вариант B)

**⚠️ ВНИМАНИЕ:** Этот вариант использует вашу продакшен БД из `.env.local`.

Переменные уже установлены в `.env.local`:
- `DATABASE_URL` — указывает на Neon
- `POSTGRES_URL` — указывает на Neon

**Проверка переменных:**
```bash
# Проверить переменные (не показывать полные credentials)
cat apps/web/.env.local | grep -E "(DATABASE_URL|POSTGRES_URL)" | head -2
```

---

### Шаг 4: Применение схемы БД

#### Для Варианта A (локальный Postgres):

```bash
# Скрипт автоматически установит переменные и применит схему
pnpm test:local
```

**Или вручную:**
```bash
export DATABASE_URL=postgresql://test:test@localhost:5432/testdb
export POSTGRES_URL=postgresql://test:test@localhost:5432/testdb

pnpm --filter @collabverse/api db:push
```

#### Для Варианта B (существующая БД):

```bash
# Использовать скрипт с переменными из окружения
pnpm test:local:env
```

**Или вручную:**
```bash
# Переменные уже загружены из .env.local через drizzle.config.ts
pnpm --filter @collabverse/api db:push
```

**Ожидаемый результат:**
- Схема БД применена успешно
- Нет ошибок подключения

---

### Шаг 5: Запуск тестов

#### Для Варианта A:

```bash
# Скрипт уже применил схему и запустит тесты
pnpm test:local
```

#### Для Варианта B:

```bash
# Если схема уже применена
pnpm -w test

# Или использовать скрипт (применит схему и запустит тесты)
pnpm test:local:env
```

**Ожидаемый результат:**
- Тесты запускаются
- Подключение к БД работает
- Тесты выполняют операции с БД

---

### Шаг 6: Проверка результатов

**Что проверить:**

1. **Тесты запускаются без ошибок подключения:**
   ```bash
   # Должны видеть запуск тестов, без ошибок типа "connection refused"
   ```

2. **Тесты выполняют операции с БД:**
   ```bash
   # В логах должны быть операции типа:
   # - delete from "attachment"
   # - insert into "file"
   ```

3. **Нет ошибок типа "relation does not exist":**
   ```bash
   # Если есть такие ошибки — схема не применена, повторите Шаг 4
   ```

---

### Шаг 7: Очистка (только для Варианта A)

После завершения тестирования остановите локальный Postgres:

```bash
pnpm test:local:cleanup
```

**Проверка:**
```bash
# Контейнер должен быть остановлен
docker ps | grep collabverse-postgres-test
# Не должно быть вывода
```

---

## 🐛 Решение проблем

### Проблема: "connection refused"

**Причина:** PostgreSQL не запущен или недоступен.

**Решение:**
```bash
# Для Варианта A: проверьте, запущен ли контейнер
docker ps | grep collabverse-postgres-test

# Если нет — запустите
pnpm test:local:setup

# Для Варианта B: проверьте подключение к Neon
# Возможно, проблема с сетью или credentials
```

### Проблема: "relation does not exist"

**Причина:** Схема БД не применена.

**Решение:**
```bash
# Применить схему вручную
pnpm --filter @collabverse/api db:push
```

### Проблема: "port 5432 already in use"

**Причина:** Порт 5432 занят другим процессом.

**Решение:**
```bash
# Найти процесс, использующий порт
lsof -i :5432

# Остановить процесс или использовать другой порт
# (требует изменения скриптов)
```

### Проблема: Docker не запущен

**Причина:** Docker Desktop не запущен.

**Решение:**
```bash
# Запустить Docker Desktop
# Или использовать Вариант B (существующая БД)
```

---

## 📝 Следующие шаги

После успешного тестирования:

1. ✅ Убедиться, что CI проходит (там уже настроен Postgres service)
2. ✅ Обновить документацию, если обнаружены проблемы
3. ✅ Рассмотреть добавление тестовых данных для более полного тестирования

---

## 🔗 Связанные документы

- [Локальный запуск тестов](../../getting-started/local-testing.md) — полная документация
- [CI Tests Database Fix Recommendations](CI_TESTS_DATABASE_FIX_RECOMMENDATIONS.md) — анализ проблемы
- [CI Database Setup Analysis](CI_DATABASE_SETUP_ANALYSIS.md) — анализ работы другого агента

---

**Последнее обновление:** 2026-01-06

