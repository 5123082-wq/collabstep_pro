# Анализ проблемы с настройкой БД в CI и локальном окружении

**Дата:** 2026-01-06  
**Контекст:** Анализ работы другого агента по настройке PostgreSQL в CI и проблемы с локальным запуском

---

## 📋 Резюме ситуации

Другой агент выполнил правильные шаги для настройки CI:
1. ✅ Добавил PostgreSQL service в `.github/workflows/ci.yml`
2. ✅ Настроил переменные окружения `POSTGRES_URL` и `DATABASE_URL` в CI
3. ✅ Добавил шаг `pnpm --filter @collabverse/api db:push` перед тестами

**Проблема:** При локальном запуске `db:push` команда падает из-за того, что `drizzle.config.ts` пытается подключиться к внешнему Neon из `.env.local`, который недоступен в sandbox окружении.

---

## 🔍 Анализ проблемы

### Корневая причина

**Проблема в `apps/api/drizzle.config.ts`:**

```typescript
// Текущий код
if (!process.env.DATABASE_URL) {
    dotenv.config({ path: path.resolve(__dirname, '../../apps/web/.env.local') });
}
```

**Что происходит:**
1. `drizzle-kit push` использует `DATABASE_URL` (не `POSTGRES_URL`)
2. Если `DATABASE_URL` не установлен, конфиг загружает `.env.local` из `apps/web/`
3. В `.env.local` указан внешний Neon (например, `neondb_owner@ep-...`)
4. При локальном запуске без установленного `DATABASE_URL` происходит попытка подключения к внешнему Neon
5. В sandbox окружении внешние подключения могут быть заблокированы → ошибка `ENOTFOUND`

### Разница между переменными

В проекте используются две переменные:
- **`POSTGRES_URL`** — используется `@vercel/postgres` и `apps/api/src/db/config.ts`
- **`DATABASE_URL`** — используется `drizzle-kit` (команды `db:push`, `db:generate`)

**В CI обе переменные установлены:**
```yaml
env:
  POSTGRES_URL: postgresql://test:test@localhost:5432/testdb
  DATABASE_URL: postgresql://test:test@localhost:5432/testdb
```

**Локально:** Обычно установлен только `POSTGRES_URL` в `.env.local`, `DATABASE_URL` может отсутствовать.

---

## ✅ Оценка работы другого агента

### Что сделано правильно

1. **Добавлен PostgreSQL service в CI** ✅
   - Правильная конфигурация с health checks
   - Корректные порты и переменные окружения

2. **Установлены обе переменные окружения** ✅
   - `POSTGRES_URL` для приложения
   - `DATABASE_URL` для drizzle-kit

3. **Добавлен шаг применения схемы** ✅
   - `db:push` перед тестами — правильный подход

### Что можно улучшить

1. **Проблема с локальным запуском:**
   - `drizzle.config.ts` не учитывает `POSTGRES_URL` как fallback
   - Нет явного указания, что для локального запуска нужен локальный Postgres

2. **Отсутствие документации:**
   - Нет инструкций для локального запуска тестов с БД
   - Не описано, как настроить локальный Postgres

---

## 💡 Рекомендации

### Вариант 1: Исправить `drizzle.config.ts` (Рекомендуется)

**Проблема:** `drizzle.config.ts` использует только `DATABASE_URL`, игнорируя `POSTGRES_URL`.

**Решение:** Обновить конфиг, чтобы он использовал `POSTGRES_URL` как fallback:

```typescript
// apps/api/drizzle.config.ts
import { defineConfig } from 'drizzle-kit';
import * as dotenv from 'dotenv';
import path from 'path';

// Приоритет: DATABASE_URL > POSTGRES_URL > .env.local
let databaseUrl = process.env.DATABASE_URL;

if (!databaseUrl) {
  // Используем POSTGRES_URL как fallback
  databaseUrl = process.env.POSTGRES_URL;
}

if (!databaseUrl) {
  // Последний fallback: загружаем из .env.local
  dotenv.config({ path: path.resolve(__dirname, '../../apps/web/.env.local') });
  databaseUrl = process.env.DATABASE_URL || process.env.POSTGRES_URL;
}

if (!databaseUrl) {
  throw new Error('DATABASE_URL or POSTGRES_URL must be set');
}

export default defineConfig({
    schema: './src/db/schema.ts',
    out: './src/db/migrations',
    dialect: 'postgresql',
    dbCredentials: {
        url: databaseUrl,
    },
});
```

**Преимущества:**
- Работает и с `DATABASE_URL`, и с `POSTGRES_URL`
- Не требует изменений в CI (там обе переменные установлены)
- Решает проблему локального запуска

**Время:** 15-30 минут

---

### Вариант 2: Использовать локальный Postgres для локального запуска

**Решение:** Настроить локальный PostgreSQL и использовать его для тестов.

**Шаги:**

1. **Запустить PostgreSQL в Docker:**
```bash
docker run --name collabverse-postgres-test \
  -e POSTGRES_USER=test \
  -e POSTGRES_PASSWORD=test \
  -e POSTGRES_DB=testdb \
  -p 5432:5432 \
  -d postgres:16
```

2. **Установить переменные окружения перед запуском:**
```bash
export DATABASE_URL=postgresql://test:test@localhost:5432/testdb
export POSTGRES_URL=postgresql://test:test@localhost:5432/testdb

pnpm --filter @collabverse/api db:push
pnpm -w test
```

3. **Или создать скрипт для удобства:**
```json
// package.json
{
  "scripts": {
    "test:local": "DATABASE_URL=postgresql://test:test@localhost:5432/testdb POSTGRES_URL=postgresql://test:test@localhost:5432/testdb pnpm -w test"
  }
}
```

**Преимущества:**
- Полное соответствие CI окружению
- Можно тестировать реальные SQL запросы

**Недостатки:**
- Требует установки Docker
- Нужно помнить запускать Postgres перед тестами

**Время:** 30 минут на настройку

---

### Вариант 3: Использовать внешнюю тестовую БД (Neon/Supabase)

**Решение:** Создать отдельную тестовую БД в Neon и использовать её для локальных тестов.

**Преимущества:**
- Не требует локального Postgres
- Всегда доступна

**Недостатки:**
- Требует настройки credentials
- Может быть медленнее
- Нужно следить за чистотой данных

**Время:** 15 минут на настройку

---

## 🎯 Рекомендуемый план действий

### Этап 1: Исправить `drizzle.config.ts` (Критично)

**Цель:** Сделать `drizzle.config.ts` совместимым с обеими переменными окружения.

1. Обновить `apps/api/drizzle.config.ts` для поддержки `POSTGRES_URL` как fallback
2. Протестировать локально с установленным `POSTGRES_URL`
3. Убедиться, что CI продолжает работать (там обе переменные установлены)

**Время:** 15-30 минут  
**Приоритет:** Высокий (решает проблему локального запуска)

---

### Этап 2: Добавить документацию для локального запуска

**Цель:** Описать процесс локального запуска тестов с БД.

1. Создать раздел в `docs/getting-started/` или обновить существующий
2. Описать варианты:
   - Локальный Postgres через Docker
   - Внешняя тестовая БД
   - Использование существующей БД (с предупреждением)
3. Добавить команды для быстрого запуска

**Время:** 30 минут  
**Приоритет:** Средний (улучшает developer experience)

---

### Этап 3: Оптимизация CI (Опционально)

**Цель:** Улучшить производительность и надежность CI.

1. Добавить кэширование миграций (если применимо)
2. Оптимизировать время ожидания health checks
3. Рассмотреть параллельное выполнение тестов

**Время:** 1-2 часа  
**Приоритет:** Низкий (оптимизация)

---

## 📝 Выводы

### Оценка работы другого агента: **8/10**

**Что сделано хорошо:**
- ✅ Правильная настройка CI с PostgreSQL service
- ✅ Корректная конфигурация переменных окружения
- ✅ Правильный порядок шагов (миграции перед тестами)

**Что можно улучшить:**
- ⚠️ Не учтена проблема локального запуска
- ⚠️ Не исправлен `drizzle.config.ts` для поддержки `POSTGRES_URL`
- ⚠️ Отсутствует документация для локального запуска

### Рекомендация по подходу

**Подход правильный** — использование реальной БД в CI для интеграционных тестов это стандартная практика. Проблема не в подходе, а в деталях реализации:

1. **CI настроен правильно** ✅
2. **Локальный запуск требует доработки** ⚠️
3. **`drizzle.config.ts` нужно исправить** ⚠️

### Следующие шаги

1. **Немедленно:** Исправить `drizzle.config.ts` для поддержки `POSTGRES_URL`
2. **В ближайшее время:** Добавить документацию для локального запуска
3. **Опционально:** Настроить локальный Postgres для разработчиков

---

## 🔗 Связанные файлы

- `.github/workflows/ci.yml` — CI workflow конфигурация
- `apps/api/drizzle.config.ts` — конфигурация Drizzle Kit
- `apps/api/src/db/config.ts` — конфигурация подключения к БД
- `docs/development/reports/CI_TESTS_DATABASE_FIX_RECOMMENDATIONS.md` — исходный документ с рекомендациями

---

**Последнее обновление:** 2026-01-06  
**Автор:** AI Assistant  
**Статус:** Требует реализации (Этап 1)

