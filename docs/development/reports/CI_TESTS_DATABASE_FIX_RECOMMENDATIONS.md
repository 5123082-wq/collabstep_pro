# Рекомендации по исправлению проблем с тестами в CI

**Дата:** 2026-01-06  
**Контекст:** После исправления ошибки "POSTGRES_URL is a placeholder" в CI тесты падают из-за отсутствия подключения к базе данных  
**PR:** #37 - refactor: реорганизация документации и скриптов

---

## 📋 Резюме

После исправления проблемы с `POSTGRES_URL` в CI workflow (установка переменной окружения `POSTGRES_URL=postgresql://test:test@localhost:5432/testdb`), тесты начали пытаться подключиться к реальной базе данных, но падают с ошибками подключения, так как такой БД не существует в CI окружении.

**Статус:** Требуется исправление  
**Приоритет:** Средний (блокирует прохождение CI, но не критично для функциональности)

---

## 🔍 Анализ проблемы

### Корневая причина

**Критически важно:** Тесты напрямую импортируют и используют реальный объект `db` из `apps/api/src/db/config.ts`:

```typescript
import { db } from '@collabverse/api/db/config';
// ...
await db.delete(attachments);
await db.insert(files).values({...});
```

**Проблема:**

- Объект `db` создается при импорте модуля через `drizzle(sql, { schema })`
- `sql` из `@vercel/postgres` **автоматически пытается подключиться** к реальной БД через `POSTGRES_URL`
- В CI окружении **нет реальной БД**, поэтому подключение **всегда падает**
- Тесты **не могут работать без реальной БД** - они не используют моки или in-memory БД

### Текущая ситуация

1. **Исправлено:**
   - ✅ Ошибка "POSTGRES_URL is a placeholder" устранена
   - ✅ Переменная `POSTGRES_URL` корректно установлена в CI workflow
   - ✅ Конфигурация БД больше не выбрасывает ошибку при загрузке модулей

2. **Проблема:**
   - ❌ Тесты **напрямую используют реальный объект `db`**, который требует реального подключения к PostgreSQL
   - ❌ Тесты пытаются выполнить реальные SQL запросы (`delete from "attachment"`, `delete from "file"` и т.д.)
   - ❌ В CI окружении **нет реальной базы данных PostgreSQL**
   - ❌ **Тесты не могут работать без реальной БД** - они не используют моки или in-memory БД

### Затронутые тесты

Следующие тестовые файлы требуют подключения к БД:

- `apps/web/tests/unit/project-files.spec.ts`
- `apps/web/tests/unit/files-direct-upload.spec.ts`
- `apps/web/tests/unit/share-links-api.spec.ts`
- `apps/web/tests/unit/project-chat.spec.ts`
- `apps/web/tests/unit/task-comments-api.spec.ts`

### Ошибки в логах

```
AggregateError:
Failed query: delete from "attachment"
params:

at Object.<anonymous> (apps/web/tests/unit/project-files.spec.ts:47:5)
```

Тесты выполняют операции очистки БД в `beforeEach`/`afterEach` хуках, но не могут подключиться к реальной БД, потому что:

1. **Объект `db` создается при импорте** и сразу пытается подключиться к БД
2. **В CI нет реальной БД** на `localhost:5432`
3. **Тесты не используют моки** - они напрямую работают с реальным Drizzle ORM объектом
4. **Подключение падает** с `AggregateError` при попытке выполнить любой SQL запрос

**Вывод:** Эти тесты **всегда будут падать в CI** без реальной БД, так как они не могут работать без подключения к PostgreSQL.

---

## 💡 Рекомендации по исправлению

### Вариант 1: Настроить тестовую БД в CI (Рекомендуется)

**Описание:** Добавить PostgreSQL service в GitHub Actions workflow для запуска тестов.

**Преимущества:**

- Тесты работают с реальной БД (интеграционные тесты)
- Близко к продакшен окружению
- Можно тестировать реальные SQL запросы и миграции

**Недостатки:**

- Медленнее выполнение тестов
- Требует настройки service в CI

**Реализация:**

1. Добавить PostgreSQL service в `.github/workflows/ci.yml`:

```yaml
jobs:
  build-test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: testdb
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    steps:
      # ... существующие шаги
      - name: Run tests
        env:
          POSTGRES_URL: postgresql://test:test@localhost:5432/testdb
        run: pnpm -w test
```

2. Применить миграции перед запуском тестов:

```yaml
- name: Setup database
  env:
    POSTGRES_URL: postgresql://test:test@localhost:5432/testdb
  run: |
    # Применить миграции БД
    pnpm --filter @collabverse/api run migrate
```

**Оценка времени:** 2-3 часа

---

### Вариант 2: Использовать моки БД в тестах

**Описание:** Мокировать подключение к БД в тестах, чтобы они не требовали реальной БД.

**Преимущества:**

- Быстрое выполнение тестов
- Не требует настройки БД в CI
- Изолированные unit тесты

**Недостатки:**

- Не тестируют реальные SQL запросы
- Требует рефакторинга тестов
- Моки могут не соответствовать реальному поведению БД

**Реализация:**

1. Создать mock для Drizzle ORM:

```typescript
// apps/web/tests/__mocks__/db.ts
import { vi } from 'vitest';

export const db = {
  select: vi.fn(),
  insert: vi.fn(),
  update: vi.fn(),
  delete: vi.fn(),
  // ... другие методы
};
```

2. Обновить тесты для использования моков:

```typescript
// apps/web/tests/unit/project-files.spec.ts
import { db } from '@/tests/__mocks__/db';

beforeEach(() => {
  vi.clearAllMocks();
  // Настройка моков для каждого теста
});
```

**Оценка времени:** 4-6 часов (рефакторинг всех тестов)

---

### Вариант 3: Пропускать тесты, требующие БД, если БД недоступна

**Описание:** Добавить проверку доступности БД и пропускать тесты, если БД недоступна.

**⚠️ ВАЖНО:** Этот вариант **не решает проблему полностью**, так как:

- Тесты все равно будут пытаться импортировать `db`, который попытается подключиться
- Подключение может упасть **до** того, как тест проверит доступность БД
- Нужно мокировать импорт `db` или отложить его создание

**Преимущества:**

- Минимальные изменения в коде (если правильно реализовано)
- Тесты не падают в CI без БД
- Можно запускать локально с БД

**Недостатки:**

- Тесты не выполняются в CI (пропускаются)
- Может скрыть реальные проблемы
- Не идеально для CI/CD
- Требует мокирования импорта `db` или изменения архитектуры

**Реализация:**

1. Добавить helper для проверки БД:

```typescript
// apps/web/tests/helpers/db-check.ts
import { db } from '@/api/src/db/config';

export async function isDbAvailable(): Promise<boolean> {
  try {
    await db.execute(sql`SELECT 1`);
    return true;
  } catch {
    return false;
  }
}
```

2. Использовать в тестах:

```typescript
// apps/web/tests/unit/project-files.spec.ts
import { isDbAvailable } from '../helpers/db-check';

describe('Project Files API', () => {
  beforeAll(async () => {
    if (!(await isDbAvailable())) {
      console.warn('Database not available, skipping tests');
      return;
    }
  });

  // ... тесты
});
```

**Оценка времени:** 1-2 часа

---

## 🎯 Рекомендуемый план действий

### ⚠️ Критическое понимание

**Эти тесты НЕ МОГУТ работать без реальной БД**, потому что:

- Они напрямую импортируют реальный объект `db`
- Объект `db` создается при импорте и пытается подключиться к БД
- Нет механизма мокирования или использования in-memory БД

**Поэтому есть только 2 реальных варианта:**

1. **Настроить реальную БД в CI** (Вариант 1) - единственный способ, чтобы тесты реально работали
2. **Рефакторить тесты для использования моков** (Вариант 2) - требует значительных изменений

### Этап 1: Краткосрочное решение (Вариант 1 - PostgreSQL service)

**Цель:** Настроить реальную БД в CI, чтобы тесты могли работать.

1. Добавить PostgreSQL service в CI workflow
2. Применить миграции перед запуском тестов
3. Убедиться, что все тесты проходят

**Время:** 2-3 часа  
**Приоритет:** Высокий (единственный способ заставить тесты работать)

### Этап 2: Долгосрочное решение (Вариант 2 - Рефакторинг тестов)

**Цель:** Рефакторить тесты для использования моков или in-memory БД.

1. Создать mock для Drizzle ORM или использовать in-memory БД (например, better-sqlite3)
2. Обновить все тесты для использования моков
3. Убедиться, что моки покрывают все случаи использования
4. Оптимизировать время выполнения тестов

**Время:** 4-6 часов  
**Приоритет:** Средний (улучшает изоляцию тестов, но требует значительных изменений)

### Этап 3: Оптимизация (опционально)

**Цель:** Улучшить изоляцию и скорость тестов.

1. Рассмотреть использование in-memory БД (better-sqlite3) для unit тестов
2. Разделить unit и integration тесты
3. Настроить параллельное выполнение тестов
4. Добавить кэширование миграций в CI

**Время:** 4-6 часов  
**Приоритет:** Низкий (оптимизация)

---

## 📝 Дополнительные замечания

### Текущая конфигурация

В `.github/workflows/ci.yml` уже установлена переменная:

```yaml
- name: Run tests
  env:
    POSTGRES_URL: postgresql://test:test@localhost:5432/testdb
  run: pnpm -w test
```

Эта переменная корректна, но требует наличия реальной БД на `localhost:5432`.

### Альтернативные решения

1. **Использовать in-memory БД** (например, SQLite для тестов)
   - Быстро, но может не соответствовать PostgreSQL поведению

2. **Использовать Docker Compose в CI**
   - Более гибко, но сложнее в настройке

3. **Использовать внешний тестовый сервис БД** (например, Neon, Supabase)
   - Просто, но требует настройки credentials и может быть медленнее

---

## 🔗 Связанные файлы

- `.github/workflows/ci.yml` - CI workflow конфигурация
- `apps/web/tests/unit/project-files.spec.ts` - Пример теста, требующего БД
- `apps/api/src/db/config.ts` - Конфигурация подключения к БД
- `apps/api/src/db/schema.ts` - Схема БД (Drizzle ORM)

---

## ✅ Чеклист для реализации

### Вариант 1 (PostgreSQL service в CI)

- [ ] Добавить `services.postgres` в `.github/workflows/ci.yml`
- [ ] Настроить health checks для PostgreSQL
- [ ] Добавить шаг применения миграций перед тестами
- [ ] Обновить переменную `POSTGRES_URL` для использования service
- [ ] Протестировать локально с Docker
- [ ] Запустить CI и проверить прохождение тестов
- [ ] Оптимизировать время выполнения (если необходимо)

### Вариант 2 (Моки БД)

- [ ] Создать mock для Drizzle ORM
- [ ] Обновить все тесты для использования моков
- [ ] Убедиться, что моки покрывают все случаи использования
- [ ] Протестировать локально
- [ ] Запустить CI и проверить прохождение тестов

### Вариант 3 (Пропуск тестов без БД) - ⚠️ Не рекомендуется

**Проблема:** Этот вариант не работает, так как `db` импортируется при загрузке модуля и пытается подключиться до того, как тест может проверить доступность БД.

**Требуется:**

- [ ] Мокировать импорт `db` через `jest.mock()` или `vi.mock()`
- [ ] Или изменить архитектуру для отложенного создания `db`
- [ ] Создать helper для проверки доступности БД
- [ ] Обновить все тесты для использования мокированного `db`
- [ ] Протестировать локально (с БД и без)
- [ ] Запустить CI и проверить, что тесты пропускаются корректно

**⚠️ Внимание:** Этот вариант требует значительных изменений и может быть сложнее, чем просто настроить БД в CI.

---

**Последнее обновление:** 2026-01-06  
**Автор:** AI Assistant  
**Статус:** Требует реализации
