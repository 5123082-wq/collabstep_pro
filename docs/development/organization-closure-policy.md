# Политика закрытия организации

> **Версия**: 1.0  
> **Дата**: 2025-01-15  
> **Статус**: Требует реализации

> ⚠️ **Важно**: Этот документ является частью комплекта документации.  
> Для начала разработки используйте **[План реализации](./organization-closure-implementation-plan.md)** как основной документ.

## 📋 Содержание

1. [Обзор](#обзор)
2. [Классификация данных](#классификация-данных)
3. [Архитектура решения](#архитектура-решения)
4. [Интеграция новых модулей](#интеграция-новых-модулей)
5. [Checklist для разработчиков](#checklist-для-разработчиков)
6. [FAQ](#faq)

---

## Обзор

При проектировании любого модуля, связанного с организациями, разработчик **ОБЯЗАН** учитывать политику закрытия организации и интегрировать свой модуль в систему проверок закрытия.

### Цель

Реализовать безопасный механизм закрытия/удаления организации с учётом:
- **Финансовых обязательств** перед третьими лицами
- **Сохранения данных** пользователя (архивация на 30 дней)
- **Расширяемости** для будущих модулей (маркетинг, подписки и т.д.)

### Принципы

1. **Финансовые обязательства БЛОКИРУЮТ закрытие** до их урегулирования
2. **Данные без обязательств АРХИВИРУЮТСЯ** на 30 дней
3. **Временные данные УДАЛЯЮТСЯ** сразу
4. **Система расширяема** через плагины-проверщики (Closure Checkers)

---

## Классификация данных

### 🔴 Финансовые обязательства (BLOCKING)

Данные, которые **БЛОКИРУЮТ** закрытие организации до их урегулирования:

| Тип | Примеры | Действие для закрытия |
|-----|---------|----------------------|
| **Контракты** | Активные договоры с исполнителями | Завершить/отменить все контракты |
| **Подписки** | Платные тарифы, SaaS-интеграции | Отменить все подписки |
| **Маркетинг** | Оплаченные рекламные кампании | Остановить и завершить кампании |
| **Платежи** | Незавершённые транзакции | Дождаться завершения |
| **Баланс** | Средства на кошельке организации | Вывести все средства |
| **Расходы** | Незакрытые расходы (pending, approved, payable) | Закрыть или отменить |

**Правило**: Если модуль создаёт финансовые обязательства перед третьими лицами или влечёт денежные потери при удалении — это **BLOCKING**.

**Статусы контрактов, блокирующие закрытие:**
- `accepted` — исполнитель принял контракт
- `funded` — средства зарезервированы
- `completed` — работа завершена (нужна оплата)
- `disputed` — спор по контракту

**Статусы контрактов, НЕ блокирующие:**
- `offer` — можно отменить
- `paid` — уже оплачено

### 📦 Данные без обязательств (ARCHIVABLE)

Данные, которые **НЕ БЛОКИРУЮТ** закрытие, но должны быть сохранены:

| Тип | Примеры | Срок хранения |
|-----|---------|---------------|
| **Документы** | Файлы, контракты (PDF), отчёты | 30 дней |
| **Настройки** | Конфигурации интеграций | 30 дней |
| **История** | Логи, аудит, история изменений | 30 дней |
| **Аналитика** | Отчёты, дашборды | 30 дней |
| **Метаданные** | Описания проектов, задачи (без финансов) | 30 дней |

**Правило**: Если данные представляют ценность для пользователя, но не создают обязательств — они **АРХИВИРУЮТСЯ** на 30 дней.

**Доступ к архиву:**
- Владелец организации имеет доступ к архиву в течение 30 дней
- Возможность скачать все документы одним архивом
- Автоматическое удаление через 30 дней (cron job)

### 🗑️ Удаляемые данные (DELETABLE)

Данные, которые удаляются сразу при закрытии:

| Тип | Примеры |
|-----|---------|
| **Кэш** | Временные данные, превью |
| **Сессии** | Активные сессии участников |
| **Черновики** | Несохранённые изменения |
| **Приглашения** | Pending invites |
| **Уведомления** | Уведомления участников |
| **Члены** | Organization members (cascade) |
| **Проекты** | Projects (cascade, но документы архивируются) |

---

## Архитектура решения

### Система модульных проверок (Closure Checkers)

Каждый модуль регистрирует свой "checker", который:
1. Проверяет наличие блокеров (финансовые обязательства)
2. Собирает данные для архивации
3. Архивирует данные при закрытии
4. Удаляет архивированные данные через 30 дней

### Интерфейс Closure Checker

```typescript
export interface OrganizationClosureChecker {
  moduleId: string;        // 'contracts' | 'marketing' | 'documents'
  moduleName: string;      // 'Контракты' | 'Маркетинг' | 'Документы'
  
  // Проверить блокеры и данные для архивации
  check(organizationId: string): Promise<ClosureCheckResult>;
  
  // Архивировать данные модуля (вызывается если нет блокеров)
  archive(organizationId: string, archiveId: string): Promise<void>;
  
  // Удалить архивированные данные (через 30 дней)
  deleteArchived(archiveId: string): Promise<void>;
}
```

### Реестр проверщиков

Все checkers регистрируются в `ClosureCheckerRegistry`, который:
- Автоматически запускает проверки всех модулей
- Координирует архивацию
- Управляет удалением архивов

### Flow закрытия организации

```
1. Владелец инициирует закрытие
   ↓
2. Запускаются все Closure Checkers
   ↓
3. Собираются блокеры и данные для архивации
   ↓
4. Если есть блокеры → показать список + инструкции
   ↓
5. Если блокеров нет → подтверждение закрытия
   ↓
6. Создание OrganizationArchive
   ↓
7. Архивирование данных через все checkers
   ↓
8. Удаление проектов, задач, участников (cascade)
   ↓
9. Установка org.status = 'archived'
   ↓
10. Уведомления владельцу и участникам
    ↓
11. (Через 30 дней) Cron job удаляет архив
```

---

## Интеграция новых модулей

### Шаг 1: Создать Closure Checker

Создайте файл в `apps/api/src/services/closure/checkers/[module-name]-checker.ts`:

```typescript
import { 
  OrganizationClosureChecker, 
  ClosureCheckResult,
  ClosureBlocker,
  ArchivableData
} from '../types';
import { myModuleRepository } from '@/repositories/my-module-repository';

export class MyModuleClosureChecker implements OrganizationClosureChecker {
  moduleId = 'my-module';
  moduleName = 'Мой модуль';
  
  async check(organizationId: string): Promise<ClosureCheckResult> {
    const blockers: ClosureBlocker[] = [];
    const archivableData: ArchivableData[] = [];
    
    // 1. Найти финансовые обязательства
    const obligations = await myModuleRepository.findActiveObligations(organizationId);
    for (const item of obligations) {
      blockers.push({
        moduleId: this.moduleId,
        type: 'financial',
        severity: 'blocking',
        id: item.id,
        title: 'Активное обязательство',
        description: `Описание обязательства: ${item.description}`,
        actionRequired: 'Завершите обязательство',
        actionUrl: `/my-module/${item.id}`
      });
    }
    
    // 2. Найти данные для архивации (если есть)
    const data = await myModuleRepository.findData(organizationId);
    for (const item of data) {
      archivableData.push({
        moduleId: this.moduleId,
        type: 'data',
        id: item.id,
        title: item.title,
        sizeBytes: item.sizeBytes,
        metadata: { /* дополнительные данные */ }
      });
    }
    
    return { 
      moduleId: this.moduleId, 
      moduleName: this.moduleName, 
      blockers, 
      archivableData 
    };
  }
  
  async archive(organizationId: string, archiveId: string): Promise<void> {
    // Архивировать данные модуля
    const data = await myModuleRepository.findData(organizationId);
    for (const item of data) {
      await archivedMyModuleRepository.create({
        archiveId,
        originalId: item.id,
        // ... другие поля
      });
    }
  }
  
  async deleteArchived(archiveId: string): Promise<void> {
    // Удалить архивированные данные
    const archived = await archivedMyModuleRepository.findByArchive(archiveId);
    for (const item of archived) {
      // Удалить файлы из storage если есть
      if (item.fileId) {
        await storageService.deleteFile(item.fileId);
      }
    }
    await archivedMyModuleRepository.deleteByArchive(archiveId);
  }
}
```

### Шаг 2: Зарегистрировать checker

В файле `apps/api/src/services/closure/index.ts`:

```typescript
import { MyModuleClosureChecker } from './checkers/my-module-checker';

// В конце файла (после инициализации registry):
closureCheckerRegistry.register(new MyModuleClosureChecker());
```

### Шаг 3: Добавить миграцию (если нужно)

Если модуль хранит данные, которые должны архивироваться, создайте таблицу:

```sql
CREATE TABLE "archived_my_module_entity" (
  "id" text PRIMARY KEY NOT NULL,
  "archive_id" text NOT NULL REFERENCES "organization_archive"("id") ON DELETE CASCADE,
  "original_id" text NOT NULL,
  "expires_at" timestamp NOT NULL,
  -- ... другие поля сущности
  "created_at" timestamp DEFAULT now()
);

CREATE INDEX "archived_my_module_archive_idx" ON "archived_my_module_entity"("archive_id");
CREATE INDEX "archived_my_module_expires_idx" ON "archived_my_module_entity"("expires_at");
```

### Шаг 4: Обновить UI (если нужно)

Если модуль добавляет новые типы блокеров, обновите компонент `ClosureBlockersCard.tsx` для корректного отображения.

---

## Checklist для разработчиков

При разработке нового модуля, связанного с организациями:

### Обязательные пункты

- [ ] Определил тип данных модуля (BLOCKING / ARCHIVABLE / DELETABLE)
- [ ] Создал `ClosureChecker` для модуля
- [ ] Зарегистрировал checker в `ClosureCheckerRegistry`
- [ ] Добавил таблицу архива (если ARCHIVABLE)
- [ ] Написал unit тесты для checker
- [ ] Добавил E2E тест для проверки закрытия с данным модулем
- [ ] Обновил документацию (если нужно)

### Рекомендуемые пункты

- [ ] Добавил примеры в документацию
- [ ] Обновил UI preview закрытия (если есть новые блокеры)
- [ ] Добавил уведомления для пользователя (если нужно)
- [ ] Проверил работу cron job для удаления архивов

---

## FAQ

### Q: Что если пользователь хочет восстановить организацию?

**A:** В течение 30 дней владелец может обратиться в поддержку. После 30 дней — данные удалены безвозвратно.

### Q: Как тестировать закрытие организации?

**A:** 
- Используйте тестовые организации
- Есть E2E тесты в `apps/web/tests/e2e/organization-closure.spec.ts`
- Можно создать тестовую организацию с контрактами и проверить блокировку

### Q: Мой модуль не вписывается в категории BLOCKING/ARCHIVABLE/DELETABLE?

**A:** Обсудите с архитектором. Возможно нужна новая категория или гибридный подход.

### Q: Что делать если модуль ещё не реализован (как маркетинг)?

**A:** Создайте заглушку checker'а, которая возвращает пустые результаты. Когда модуль будет готов — реализуйте полную логику.

### Q: Как обрабатывать частичную архивацию (не все данные)?

**A:** Архивируются только данные, которые помечены как `archivableData` в методе `check()`. Остальное удаляется.

### Q: Что если в модуле есть и финансовые обязательства, и данные для архивации?

**A:** Это нормально. Метод `check()` должен вернуть и `blockers`, и `archivableData`. Блокеры предотвратят закрытие, а данные будут заархивированы после устранения блокеров.

### Q: Как долго хранятся архивы?

**A:** По умолчанию 30 дней. Можно расширить до 60 или 90 дней через настройки, но это требует изменения схемы БД.

---

## Связанные документы

- [Техническая спецификация](./organization-closure-specification.md)
- [API документация](./organization-closure-api.md)
- [Примеры реализации](./organization-closure-examples.md)

---

**Последнее обновление**: 2025-01-15  
**Автор**: AI Assistant  
**Статус**: Требует реализации

