Summary
- Добавлены настройки проекта на шаге 2: visibility, type, stage, deadline, плюс итоговый summary перед созданием.
- Тексты хедера обновлены без упоминания дефолтной приватности.
- В запрос создания и телеметрию передаются новые поля, deadline отправляется только при заполнении.
- Страница создания проекта фактически редиректит на список; актуальная точка — модалка, wizard обновлён для консистентности.

Что изменено
- apps/web/components/pm/CreateProjectModal.tsx: добавлены поля настроек, summary, дефолты, прокинуты новые поля в POST и trackEvent.
- apps/web/app/(app)/projects/create/ProjectCreateWizardClient.tsx: зеркальные изменения (на случай восстановления страницы).

Дефолты/поведение полей
- visibility = private (выбор private/public).
- stage = discovery.
- type = опционально, по умолчанию пусто ("Не выбран").
- deadline = опционально, не отправляется при пустом значении.

Риски/допущения
- Wizard сейчас не используется (страницы create редиректят), но оставлен в актуальном состоянии на случай включения.

Тесты
- Не запускались (изменения UI).

Открытые вопросы
- Нужно ли отображать настройки в каком-то ещё entrypoint, если wizard будет включён обратно?
