# Задание: Завершение миграции блоков контента — Финальный этап

## Контекст

Проведен полный аудит миграции блоков контента. Результаты:

- ✅ **98.8% мигрировано** (83/84 файла)
- ✅ Все критичные компоненты мигрированы
- ✅ Инфраструктура работает корректно
- ⚠️ Осталось 8 использований старых стилей в 5 файлах

**Отчет аудита:** `docs/development/content-blocks-migration-audit-report.md`

## Цель

Исправить все найденные проблемы и завершить миграцию на единую систему блоков контента, достигнув 100% миграции основных блоков контента.

## Важные замечания

- **Будьте осторожны** при миграции — проверяйте функциональность после каждого изменения
- Сохраняйте все интерактивные функции (hover эффекты, клики и т.д.)
- Тестируйте после каждого изменения
- Обновляйте документацию по мере выполнения задач
- Проверяйте визуально, что блок выглядит так же или лучше после миграции

## Документы для изучения

1. **Отчет аудита:** `docs/development/content-blocks-migration-audit-report.md`
   - Все найденные проблемы
   - Рекомендации по исправлению
   - Статистика миграции

2. **Roadmap миграции:** `docs/development/content-blocks-migration-roadmap.md`
   - Общая статистика миграции
   - История изменений

3. **Документация компонента:** `apps/web/docs/ui/content-blocks.md`
   - API `ContentBlock` и `ContentBlockTitle`
   - Варианты и размеры
   - Примеры использования

4. **CSS-классы:** `apps/web/styles/globals.css`
   - Текущие стили блоков контента

5. **Токены дизайн-системы:** `apps/web/design-tokens.ts`
   - Токены для блоков контента

---

## Приоритет 1: Критичные блоки контента (Средний приоритет)

### Задача 1.1: Модальное окно редактирования пользователя

**Файл:** `apps/web/app/(app)/admin/users/page.tsx`  
**Строка:** 538  
**Приоритет:** Средний

**Текущий код:**
```tsx
<div className="w-full max-w-md rounded-2xl border border-neutral-800 bg-neutral-950 p-6 shadow-xl">
```

**Задача:**
- [ ] Добавить импорт `ContentBlock` (если еще не добавлен)
- [ ] Заменить `<div>` на `<ContentBlock>`
- [ ] Сохранить все классы для размеров и теней (`w-full max-w-md shadow-xl`)
- [ ] Проверить визуально, что модальное окно выглядит корректно
- [ ] Проверить функциональность (открытие, закрытие, редактирование)
- [ ] Убедиться, что нет ошибок линтера

**Ожидаемый результат:**
```tsx
<ContentBlock className="w-full max-w-md shadow-xl">
```

---

### Задача 1.2: Модальное окно изменения лимита бюджета

**Файл:** `apps/web/app/(app)/pm/projects/[id]/page.tsx`  
**Строка:** 543  
**Приоритет:** Средний

**Текущий код:**
```tsx
<div className="w-full max-w-md rounded-2xl border border-neutral-800 bg-neutral-950 p-6 shadow-2xl">
```

**Задача:**
- [ ] Добавить импорт `ContentBlock` (если еще не добавлен)
- [ ] Заменить `<div>` на `<ContentBlock>`
- [ ] Сохранить все классы для размеров и теней (`w-full max-w-md shadow-2xl`)
- [ ] Проверить визуально, что модальное окно выглядит корректно
- [ ] Проверить функциональность (открытие, закрытие, изменение лимита)
- [ ] Убедиться, что нет ошибок линтера

**Ожидаемый результат:**
```tsx
<ContentBlock className="w-full max-w-md shadow-2xl">
```

---

### Задача 1.3: Модальное окно деталей вакансии

**Файл:** `apps/web/components/marketplace/VacancyDetail.tsx`  
**Строка:** 277  
**Приоритет:** Средний

**Текущий код:**
```tsx
<div className="w-full max-w-xl rounded-3xl border border-neutral-900 bg-neutral-950/90 p-6">
```

**Задача:**
- [ ] Добавить импорт `ContentBlock` (если еще не добавлен)
- [ ] Заменить `<div>` на `<ContentBlock>`
- [ ] Сохранить все классы для размеров (`w-full max-w-xl`)
- [ ] Проверить визуально, что модальное окно выглядит корректно
- [ ] Проверить функциональность (открытие, закрытие, просмотр деталей)
- [ ] Убедиться, что нет ошибок линтера

**Ожидаемый результат:**
```tsx
<ContentBlock className="w-full max-w-xl">
```

---

### Задача 1.4: Пустое состояние проектов

**Файл:** `apps/web/app/(app)/projects/ProjectsOverviewPageClient.tsx`  
**Строка:** 833  
**Приоритет:** Средний

**Текущий код:**
```tsx
<div className="rounded-3xl border border-neutral-900 bg-neutral-950/70 p-12 text-center">
```

**Задача:**
- [ ] Добавить импорт `ContentBlock` (если еще не добавлен)
- [ ] Заменить `<div>` на `<ContentBlock>`
- [ ] Сохранить классы для отступов и выравнивания (`p-12 text-center`)
- [ ] Проверить визуально, что пустое состояние выглядит корректно
- [ ] Проверить функциональность (отображение при отсутствии проектов)
- [ ] Убедиться, что нет ошибок линтера

**Ожидаемый результат:**
```tsx
<ContentBlock className="p-12 text-center">
```

---

## Приоритет 2: Вспомогательные элементы (Низкий приоритет)

### Задача 2.1: Пагинация проектов

**Файл:** `apps/web/components/pm/ProjectsList.tsx`  
**Строка:** 32  
**Приоритет:** Низкий

**Текущий код:**
```tsx
<nav aria-label="Пагинация проектов" className="flex items-center justify-between rounded-2xl border border-neutral-900 bg-neutral-950/60 px-4 py-3">
```

**Задача:**
- [ ] Заменить старые стили на CSS классы `.content-block` и `.content-block-sm`
- [ ] Сохранить все функциональные классы (`flex items-center justify-between px-4 py-3`)
- [ ] Сохранить `aria-label` для доступности
- [ ] Проверить визуально, что пагинация выглядит корректно
- [ ] Проверить функциональность (переключение страниц)
- [ ] Убедиться, что нет ошибок линтера

**Ожидаемый результат:**
```tsx
<nav aria-label="Пагинация проектов" className="content-block content-block-sm flex items-center justify-between px-4 py-3">
```

---

### Задача 2.2: Пагинация архивных проектов

**Файл:** `apps/web/components/pm/ArchivedProjectsList.tsx`  
**Строка:** 32  
**Приоритет:** Низкий

**Текущий код:**
```tsx
<nav aria-label="Пагинация архивных проектов" className="flex items-center justify-between rounded-2xl border border-neutral-900 bg-neutral-950/60 px-4 py-3">
```

**Задача:**
- [ ] Заменить старые стили на CSS классы `.content-block` и `.content-block-sm`
- [ ] Сохранить все функциональные классы (`flex items-center justify-between px-4 py-3`)
- [ ] Сохранить `aria-label` для доступности
- [ ] Проверить визуально, что пагинация выглядит корректно
- [ ] Проверить функциональность (переключение страниц)
- [ ] Убедиться, что нет ошибок линтера

**Ожидаемый результат:**
```tsx
<nav aria-label="Пагинация архивных проектов" className="content-block content-block-sm flex items-center justify-between px-4 py-3">
```

---

### Задача 2.3: Пагинация в ProjectsOverviewPageClient

**Файл:** `apps/web/app/(app)/projects/ProjectsOverviewPageClient.tsx`  
**Строка:** 880  
**Приоритет:** Низкий

**Текущий код:**
```tsx
<nav className="flex items-center justify-between rounded-3xl border border-neutral-900 bg-neutral-950/70 px-4 py-3">
```

**Задача:**
- [ ] Заменить старые стили на CSS классы `.content-block` и `.content-block-sm`
- [ ] Сохранить все функциональные классы (`flex items-center justify-between px-4 py-3`)
- [ ] Проверить визуально, что пагинация выглядит корректно
- [ ] Проверить функциональность (переключение страниц)
- [ ] Убедиться, что нет ошибок линтера

**Ожидаемый результат:**
```tsx
<nav className="content-block content-block-sm flex items-center justify-between px-4 py-3">
```

---

### Задача 2.4: Skeleton loader

**Файл:** `apps/web/app/(app)/projects/ProjectsOverviewPageClient.tsx`  
**Строка:** 828  
**Приоритет:** Низкий

**Текущий код:**
```tsx
<div className="h-64 animate-pulse rounded-3xl border border-neutral-900 bg-neutral-950/60" />
```

**Задача:**
- [ ] Решить: использовать `ContentBlock` или оставить как есть
- [ ] Если мигрировать: заменить на `ContentBlock` с классом `animate-pulse`
- [ ] Сохранить высоту (`h-64`)
- [ ] Проверить визуально, что skeleton выглядит корректно
- [ ] Проверить функциональность (отображение при загрузке)
- [ ] Убедиться, что нет ошибок линтера

**Ожидаемый результат (если мигрировать):**
```tsx
<ContentBlock className="h-64 animate-pulse" />
```

**Примечание:** Skeleton loader может иметь специфичные стили, поэтому миграция опциональна.

---

## Финальная проверка

### Задача 3.1: Проверка всех изменений

- [ ] Проверить все измененные файлы на ошибки линтера
- [ ] Проверить визуально все измененные компоненты
- [ ] Проверить функциональность всех измененных компонентов
- [ ] Убедиться, что нет регрессий

**Команда для проверки линтера:**
```bash
cd apps/web && npm run lint
```

---

### Задача 3.2: Обновление документации

- [ ] Обновить `docs/development/content-blocks-migration-roadmap.md`:
  - [ ] Отметить все задачи как завершенные
  - [ ] Обновить статистику миграции (100%)
  - [ ] Добавить запись в историю изменений

- [ ] Обновить `docs/development/content-blocks-migration-audit-report.md`:
  - [ ] Отметить все проблемы как исправленные
  - [ ] Обновить статистику (100% миграции)
  - [ ] Добавить запись о завершении миграции

- [ ] Обновить `apps/web/docs/ui/content-blocks.md` (если нужно):
  - [ ] Добавить примеры из мигрированных компонентов (если есть новые паттерны)

---

### Задача 3.3: Финальная проверка использования старых стилей

- [ ] Выполнить поиск всех использований старых стилей:
  ```bash
  grep -r "rounded-[23]xl border border-neutral-[89]00.*bg-neutral-950" apps/web --include="*.tsx" --include="*.ts" | grep -v "content-block" | grep -v "node_modules" | grep -v ".md"
  ```

- [ ] Убедиться, что все найденные использования либо:
  - Мигрированы на `ContentBlock` или CSS классы
  - Являются элементами форм (input, textarea) - это нормально
  - Являются кнопками или badges - это нормально
  - Находятся в документации (примеры) - это нормально

- [ ] Если найдены новые использования старых стилей в основных блоках контента - мигрировать их

---

## Критерии успешного завершения

### ✅ Миграция считается завершенной, если

1. **Все критичные блоки мигрированы:**
   - ✅ Все 4 модальных окна мигрированы
   - ✅ Пустое состояние мигрировано

2. **Вспомогательные элементы мигрированы (опционально):**
   - ✅ Пагинация мигрирована (3 файла)
   - ✅ Skeleton loader мигрирован (если решено мигрировать)

3. **Документация обновлена:**
   - ✅ Roadmap обновлен (100% миграции)
   - ✅ Отчет аудита обновлен
   - ✅ История изменений полная

4. **Проверка пройдена:**
   - ✅ Нет ошибок линтера
   - ✅ Все компоненты работают корректно
   - ✅ Нет регрессий
   - ✅ Поиск старых стилей не находит основных блоков контента

---

## Оценка времени

- **Приоритет 1 (критичные блоки):** 1-2 часа
  - 4 модальных окна: ~30 минут каждое
  - Пустое состояние: ~15 минут

- **Приоритет 2 (вспомогательные элементы):** 30 минут - 1 час
  - 3 пагинации: ~10 минут каждая
  - Skeleton loader: ~15 минут (если мигрировать)

- **Финальная проверка:** 30 минут
  - Проверка линтера: ~10 минут
  - Обновление документации: ~20 минут

**Итого:** 2-3.5 часа

---

## Шаблон для проверки каждого файла

Для каждого файла выполните:

1. **Открыть файл** и найти проблемное место
2. **Добавить импорт** (если нужно):
   ```tsx
   import { ContentBlock } from '@/components/ui/content-block';
```text
3. **Заменить старые стили** на `ContentBlock` или CSS классы
4. **Проверить визуально** - открыть страницу в браузере
5. **Проверить функциональность** - протестировать все функции
6. **Проверить линтер** - убедиться, что нет ошибок
7. **Отметить задачу как выполненную** ✅

---

## Примеры миграции

### Пример 1: Модальное окно

**До:**
```tsx
<div className="w-full max-w-md rounded-2xl border border-neutral-800 bg-neutral-950 p-6 shadow-xl">
  <h2>Заголовок</h2>
  <p>Контент</p>
</div>
```

**После:**
```tsx
<ContentBlock className="w-full max-w-md shadow-xl">
  <h2>Заголовок</h2>
  <p>Контент</p>
</ContentBlock>
```

### Пример 2: Пагинация

**До:**
```tsx
<nav className="flex items-center justify-between rounded-2xl border border-neutral-900 bg-neutral-950/60 px-4 py-3">
  {/* Кнопки пагинации */}
</nav>
```

**После:**
```tsx
<nav className="content-block content-block-sm flex items-center justify-between px-4 py-3">
  {/* Кнопки пагинации */}
</nav>
```

### Пример 3: Пустое состояние

**До:**
```tsx
<div className="rounded-3xl border border-neutral-900 bg-neutral-950/70 p-12 text-center">
  <h3>Пусто</h3>
  <p>Здесь ничего нет</p>
</div>
```

**После:**
```tsx
<ContentBlock className="p-12 text-center">
  <h3>Пусто</h3>
  <p>Здесь ничего нет</p>
</ContentBlock>
```

---

## Важные напоминания

1. **Всегда проверяйте визуально** - откройте страницу в браузере после каждого изменения
2. **Сохраняйте функциональность** - не удаляйте важные классы (например, `w-full`, `max-w-md`, `shadow-xl`)
3. **Используйте правильные варианты** - для пустых состояний используйте `variant="dashed"`, если нужно
4. **Проверяйте линтер** - убедитесь, что нет ошибок после каждого изменения
5. **Обновляйте документацию** - отмечайте выполненные задачи в roadmap

---

**Дата создания:** 2024-12-19  
**Статус:** К выполнению  
**Приоритет:** Средний  
**Зависимости:** Завершение аудита миграции

---

## Чеклист выполнения

### Приоритет 1: Критичные блоки

- [ ] Задача 1.1: Модальное окно редактирования пользователя
- [ ] Задача 1.2: Модальное окно изменения лимита бюджета
- [ ] Задача 1.3: Модальное окно деталей вакансии
- [ ] Задача 1.4: Пустое состояние проектов

### Приоритет 2: Вспомогательные элементы

- [ ] Задача 2.1: Пагинация проектов
- [ ] Задача 2.2: Пагинация архивных проектов
- [ ] Задача 2.3: Пагинация в ProjectsOverviewPageClient
- [ ] Задача 2.4: Skeleton loader (опционально)

### Финальная проверка миграции

- [ ] Задача 3.1: Проверка всех изменений
- [ ] Задача 3.2: Обновление документации
- [ ] Задача 3.3: Финальная проверка использования старых стилей

---

**После завершения всех задач миграция будет считаться полностью завершенной! 🎉**

