# Быстрое руководство по настройке Vercel Postgres

> **Последнее обновление:** 2026-01-05

Это руководство поможет вам быстро настроить Vercel Postgres для работы с Collabverse.

---

## 🚀 Быстрый старт (5 минут)

### 1. Создайте базу данных на Vercel

1. Откройте [vercel.com](https://vercel.com) → ваш проект
2. **Storage** → **Create Database** → **Postgres** → **Create**
3. Vercel автоматически создаст `POSTGRES_URL`

### 2. Установите переменные окружения на Vercel

В **Settings** → **Environment Variables** добавьте:

```env
# Обязательно
DATABASE_URL=$POSTGRES_URL
AUTH_STORAGE=db
AUTH_SECRET=<сгенерируйте: openssl rand -base64 32>
NEXTAUTH_URL=https://your-domain.vercel.app

# Демо-администратор
DEMO_ADMIN_EMAIL=admin.demo@collabverse.test
DEMO_ADMIN_PASSWORD=admin.demo
```

### 3. Примените миграции

**Вариант A: Через Vercel CLI (рекомендуется)**

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите и подключитесь
vercel login
vercel link

# Получите переменные окружения
vercel env pull .env.local

# Примените миграции
cd apps/api
pnpm db:push
```

**Вариант B: Через SQL редактор Vercel**

1. Откройте базу данных в Vercel
2. Перейдите в **Query**
3. Выполните SQL из файлов по порядку:
   - `apps/api/src/db/migrations/0000_lying_mauler.sql`
   - `apps/api/src/db/migrations/0001_chemical_lake.sql`
   - `apps/api/src/db/migrations/0002_spicy_domino.sql`

### 4. Инициализируйте администратора

```bash
npx tsx scripts/cleanup-users-db.ts
```

Этот скрипт:
- Создаст `admin.demo@collabverse.test` в БД
- Настроит права администратора
- Удалит всех остальных пользователей (если есть)

### 5. Проверьте работу

```bash
# Локально
pnpm dev

# Откройте http://localhost:3000
# Войдите как: admin.demo@collabverse.test / admin.demo
```

---

## 📋 Полный чеклист настройки

### Что уже должно быть сделано

1. ✅ База данных создана на Vercel
2. ✅ `POSTGRES_URL` настроен в `.env.local`
3. ✅ Переменные окружения установлены

### Что нужно сделать сейчас

#### 1. Установить AUTH_STORAGE=db

Добавьте в `apps/web/.env.local`:

```env
AUTH_STORAGE=db
```

Это переключит систему с памяти на базу данных.

#### 2. Применить миграции базы данных

```bash
cd apps/api
pnpm db:push
```

Или через SQL редактор Vercel (если используете Neon/Vercel Postgres):
1. Откройте базу данных
2. Перейдите в **Query**
3. Выполните SQL из файлов по порядку:
   - `apps/api/src/db/migrations/0000_lying_mauler.sql`
   - `apps/api/src/db/migrations/0001_chemical_lake.sql`
   - `apps/api/src/db/migrations/0002_spicy_domino.sql`

#### 3. Инициализировать администратора

После применения миграций запустите:

```bash
npx tsx scripts/cleanup-users-db.ts
```

Это создаст `admin.demo@collabverse.test` в базе данных с правильными правами.

#### 4. Настроить переменные на Vercel (для production)

В настройках проекта на Vercel добавьте:

```env
# Обязательно
DATABASE_URL=$POSTGRES_URL
AUTH_STORAGE=db
AUTH_SECRET=<сгенерируйте: openssl rand -base64 32>
NEXTAUTH_URL=https://your-domain.vercel.app

# Демо-администратор
DEMO_ADMIN_EMAIL=admin.demo@collabverse.test
DEMO_ADMIN_PASSWORD=admin.demo
```

> ⚠️ **Важно:** Не добавляйте `DEMO_USER_EMAIL` и `DEMO_USER_PASSWORD` - эти пользователи удалены.

---

## 📋 Полный список переменных окружения

### Обязательные для работы с БД

```env
# Database
POSTGRES_URL=<ваш URL из Vercel/Neon>
DATABASE_URL=$POSTGRES_URL

# Auth Storage (критически важно!)
AUTH_STORAGE=db

# NextAuth
AUTH_SECRET=<сгенерируйте: openssl rand -base64 32>
NEXTAUTH_URL=http://localhost:3000  # или https://your-domain.vercel.app
```

### Рекомендуемые

```env
# Базовые настройки
NAV_V1=on
APP_LOCALE=ru
FEATURE_PROJECTS_V1=1
AUTH_DEV=on

# Finance Storage
FIN_EXPENSES_STORAGE=db

# Демо-администратор
DEMO_ADMIN_EMAIL=admin.demo@collabverse.test
DEMO_ADMIN_PASSWORD=admin.demo
```

### Опциональные (Google OAuth)

```env
GOOGLE_CLIENT_ID=<ваш Client ID>
GOOGLE_CLIENT_SECRET=<ваш Client Secret>
```

---

## 🔍 Проверка после настройки

### 1. Проверка таблиц в БД

В SQL редакторе Vercel/Neon выполните:

```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public'
ORDER BY table_name;
```

Должны быть созданы таблицы:
- `user` ✅
- `account` ✅
- `session` ✅
- `userControl` ✅
- `organization` ✅
- `project` ✅
- И другие...

### 2. Проверка администратора

```sql
SELECT id, name, email, 
       "passwordHash" IS NOT NULL as has_password,
       "createdAt"
FROM "user"
WHERE email = 'admin.demo@collabverse.test';
```

Должен быть один пользователь с:
- ID: `00000000-0000-0000-0000-000000000001`
- Email: `admin.demo@collabverse.test`
- Пароль установлен: `true`

### 3. Проверка прав администратора

```sql
SELECT "userId", status, roles, "testerAccess"
FROM "userControl"
WHERE "userId" = '00000000-0000-0000-0000-000000000001';
```

Должны быть роли: `['productAdmin', 'featureAdmin']`

### 4. Тестирование входа

1. Откройте http://localhost:3000
2. Перейдите на `/login`
3. Войдите как:
   - Email: `admin.demo@collabverse.test`
   - Пароль: `admin.demo`
4. Проверьте, что вход успешен

### 5. Тестирование регистрации

1. Перейдите на `/register`
2. Заполните форму новым пользователем
3. Проверьте, что пользователь создан в БД:

```sql
SELECT id, name, email, "createdAt"
FROM "user"
WHERE email != 'admin.demo@collabverse.test'
ORDER BY "createdAt" DESC;
```

---

## 🔧 Команды для работы с БД

```bash
# Применить миграции
cd apps/api && pnpm db:push

# Генерация новых миграций (если изменили схему)
cd apps/api && pnpm db:generate

# Просмотр схемы в браузере
cd apps/api && pnpm drizzle-kit studio
```

---

## ⚠️ Критически важно

1. **AUTH_STORAGE=db** — обязательно для использования БД. Без этого система будет использовать память вместо БД.
2. **POSTGRES_URL** — создается автоматически при создании БД
3. **Миграции** — нужно применить один раз перед первым использованием
4. **Демо-пользователи** — только `admin.demo@collabverse.test`, остальные удалены
5. **Администратор** — должен быть создан в БД через скрипт `cleanup-users-db.ts`
6. **Новые пользователи** — регистрируются через форму `/register`

---

## ✅ После выполнения всех шагов

Система будет полностью работать с Vercel Postgres:
- ✅ Все данные сохраняются в БД
- ✅ Администратор может управлять пользователями
- ✅ Новые пользователи регистрируются через форму
- ✅ Все операции работают корректно

---

## 📚 Дополнительная документация

- **[VERCEL_POSTGRES_SETUP_CHECKLIST.md](./VERCEL_POSTGRES_SETUP_CHECKLIST.md)** — Полный чеклист настройки
- **[vercel-postgres-setup.md](./vercel-postgres-setup.md)** — Подробная инструкция по настройке
- **[Руководство по очистке базы данных](../runbooks/DATABASE_CLEANUP_GUIDE.md)** — Очистка БД и настройка администратора
- **[Быстрый старт](./QUICK_SETUP_GUIDE.md)** — Общее руководство по началу работы с проектом
