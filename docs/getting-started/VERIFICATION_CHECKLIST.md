# Чеклист проверки исправлений

Используйте этот чеклист для проверки каждого исправления перед коммитом.

**Последнее обновление:** 2025-02-04

---

## 🔍 Перед началом работы

- [ ] Прочитать `FIXES_ACTION_PLAN.md`
- [ ] Создать отдельную ветку для исправлений
- [ ] Прогнать базовые проверки: `pnpm lint`, `pnpm typecheck`, `pnpm test` (зафиксировать падения)

---

## ✅ После каждого исправления

### Базовая проверка

- [ ] Код компилируется без ошибок: `pnpm typecheck` (web и api)
- [ ] Линтинг проходит: `pnpm lint`
- [ ] Тесты проходят: `pnpm test`
- [ ] Нет новых предупреждений/шумных логов

### Специфичные проверки

#### Тест на запрещённый этап

- [ ] Аудит-доки исключены из выборки теста или формулировки переписаны
- [ ] `apps/web/tests/unit/stageCopy.spec.ts` зелёный

#### Parsing error для `drizzle.config.ts`

- [ ] Файл добавлен в `apps/api/tsconfig.json:include` (или исключён из lint)
- [ ] `pnpm lint` не показывает parsing error

#### Линтинг (`any` / `ban-ts-comment` / логические ошибки)

- [ ] Кластеры в API (`ai-agents-repository.ts`, `wallet-repository.ts`, др.) закрыты
- [ ] Кластеры во фронте (файлы из `report_lint.txt`) закрыты
- [ ] `pnpm lint` зелёный

#### Typecheck

- [ ] Скрипт `typecheck` в корне проверяет web и api
- [ ] `pnpm typecheck` зелёный

#### Jest конфигурация

- [ ] Блок `globals` убран из `jest.config.cjs`
- [ ] Нет предупреждений ts-jest
- [ ] Websocket runner не ломает юниты (нет SyntaxError)

#### Шумные логи и скрипты

- [ ] Логи ExpenseStore подавлены в тестах
- [ ] Скрипт `test` не обновляет снапшоты по умолчанию (если меняли)

---

## 🎯 После завершения Этапа 1 (P0)

- [ ] `pnpm lint` зелёный
- [ ] `pnpm test` зелёный (включая `stageCopy.spec.ts`)

**Критерии успеха:** 0 ошибок линтинга, 0 падающих тестов

---

## 🎯 После завершения Этапа 2 (P1)

- [ ] Typecheck проверяет web и api
- [ ] Нет предупреждений ts-jest
- [ ] Нет SyntaxError от websocket runner

**Критерии успеха:** `pnpm typecheck` зелёный, `pnpm test` без предупреждений

---

## 🎯 После завершения Этапа 3 (P2)

- [ ] Тестовые логи чистые
- [ ] Снапшоты обновляются только явным скриптом

---

## 📝 Перед коммитом

- [ ] Все изменения протестированы
- [ ] Код соответствует стилю проекта
- [ ] Нет закомментированного кода
- [ ] Обновлена документация (если нужно)
- [ ] Коммит-сообщение описывает изменения
- [ ] Запущены все проверки: `pnpm lint && pnpm typecheck && pnpm test`

---

## 🚀 После коммита

- [ ] Проверить CI pipeline
- [ ] Убедиться, что все проверки проходят
- [ ] Если что-то упало - исправить немедленно

---

## 🔧 Быстрые команды для проверки

```bash

# Полная проверка

pnpm lint && pnpm typecheck && pnpm test

# Только линтинг

pnpm lint

# Только типы

pnpm typecheck

# Только тесты

pnpm test

# Проверка конкретного пакета

pnpm --filter @collabverse/web typecheck
pnpm --filter @collabverse/api typecheck
```

---

## ⚠️ Важные замечания

1. **Не коммитить изменения, если хотя бы одна проверка не проходит!**
2. После исправления P0 проблем обязательно проверить CI
3. Если тесты падают локально, но проходят в CI (или наоборот) - это требует дополнительного расследования
4. Все изменения должны быть протестированы локально перед коммитом

---

## 📊 Текущий статус

**Статус:** lint/test/typecheck падают; приоритеты см. `FIXES_ACTION_PLAN.md`.

---

**Последнее обновление:** 2025-02-04
