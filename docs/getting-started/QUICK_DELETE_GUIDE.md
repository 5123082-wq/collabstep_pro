# Быстрое руководство по удалению проектов и задач

## Проблема решена! ✅

Теперь в системе есть надежный механизм удаления всех проектов и задач из памяти.

## Что было исправлено

### Предыдущие проблемы

- ❌ Скрипты пытались работать напрямую с репозиториями
- ❌ Не работали с запущенным сервером
- ❌ Изменения не сохранялись в глобальной памяти Next.js

### Текущее решение

- ✅ Работа через API endpoints
- ✅ Правильное управление глобальной памятью
- ✅ Каскадное удаление связанных данных
- ✅ UI в админ-панели для удобного управления
- ✅ Подробная статистика по пользователям

## Быстрый старт

### Метод 1: Через UI (Рекомендуется)

1. **Откройте админ-панель**
```text
   http://localhost:3000/app/admin
   ```

2. **Перейдите в "Управление данными"**
   - Или напрямую: `http://localhost:3000/app/admin/data`

3. **Просмотрите статистику**
   - Общее количество проектов и задач
   - Данные по каждому пользователю
   - Детали проектов

4. **Удалите данные**
   - **Все данные**: кнопка "Удалить все данные" в красной зоне
   - **Данные пользователя**: кнопка корзины рядом с пользователем

### Метод 2: Через скрипт

1. **Убедитесь, что сервер запущен**
   ```bash
   pnpm dev
   # или
   npm run dev
   ```

2. **Запустите скрипт удаления**
   ```bash
   npx tsx scripts/clear-all-data.ts
   ```

3. **Результат**
```text
   ════════════════════════════════════════════════════════════════════════════════
   🗑️  УДАЛЕНИЕ ВСЕХ ДАННЫХ ЧЕРЕЗ API
   ════════════════════════════════════════════════════════════════════════════════
   
   📊 ШАГ 1: Получение текущей статистики...
   📈 Текущее состояние:
      • Всего проектов: 2
      • Всего задач: 10
      • Пользователей с данными: 2
   
   🗑️  ШАГ 2: Удаление всех данных...
   ✅ Удаление завершено:
      • Удалено проектов: 2
      • Удалено задач: 10
   
   📊 ШАГ 3: Проверка результата...
   ✅ Все данные успешно удалены!
   ```

## Возможности системы

### 📊 Статистика

- Общее количество проектов и задач
- Данные по каждому пользователю
- Детальный список проектов с количеством задач

### 🗑️ Удаление

- **Полное удаление**: все проекты и задачи
- **По пользователю**: только данные выбранного пользователя
- **Каскадное**: автоматическое удаление связанных данных

### 🔒 Безопасность

- Требуется роль администратора
- Двойное подтверждение перед удалением
- Предупреждения о необратимости операции

## API Endpoints

### Получение статистики

```bash
GET /api/admin/data/stats
```

### Удаление всех данных

```bash
POST /api/admin/data/clear
Content-Type: application/json

{
  "confirm": true
}
```

### Удаление данных пользователя

```bash
POST /api/admin/data/clear
Content-Type: application/json

{
  "confirm": true,
  "userId": "user.demo@collabverse.test"
}
```

## Пример использования cURL

```bash

# 1. Получить статистику

curl -X GET http://localhost:3000/api/admin/data/stats \
  -H "Cookie: cv_session=YOUR_SESSION_TOKEN"

# 2. Удалить все данные

curl -X POST http://localhost:3000/api/admin/data/clear \
  -H "Content-Type: application/json" \
  -H "Cookie: cv_session=YOUR_SESSION_TOKEN" \
  -d '{"confirm": true}'
```

## Что удаляется

При удалении проектов автоматически очищаются:
- ✅ Все задачи
- ✅ Зависимости между задачами
- ✅ Участники проектов
- ✅ Расходы и вложения
- ✅ Бюджеты
- ✅ Уведомления
- ✅ Сообщения чата

## Устранение неполадок

### Сервер не запущен

```bash

# Запустите сервер

pnpm dev
```

### Нет прав доступа

Убедитесь, что вы авторизованы как администратор:
- Email: `admin.demo@collabverse.test`
- Роль: `admin`

### Скрипт не находит данные

Проверьте, что данные действительно есть в системе:
1. Откройте `/pm/projects` в браузере
2. Или используйте API: `GET /api/pm/projects`

## Файлы системы

```text
apps/web/
  app/
    api/admin/data/
      stats/route.ts     # Статистика
      clear/route.ts     # Удаление
    (app)/app/admin/
      data/page.tsx      # UI страница
      page.tsx           # Карточка в админке

scripts/
  clear-all-data.ts      # Скрипт удаления

docs/
  admin/
    data-management.md   # Полная документация
```

## Поддержка

Если возникли проблемы:
1. Проверьте, что сервер запущен
2. Откройте консоль браузера для ошибок
3. Проверьте логи сервера
4. Убедитесь, что используете роль администратора

## Дополнительная информация

Полная документация: [docs/admin/data-management.md](../admin/data-management.md)

