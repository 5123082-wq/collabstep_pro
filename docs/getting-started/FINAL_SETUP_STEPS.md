# Финальные шаги для работы с Vercel Postgres

## ✅ Текущий статус

Проверка показала, что у вас уже настроено:
- ✅ `POSTGRES_URL` установлен в `.env.local`
- ✅ `AUTH_STORAGE=db` установлен

## 🎯 Что нужно сделать прямо сейчас

### Шаг 1: Применить миграции базы данных

```bash
cd apps/api
pnpm db:push
```

Эта команда создаст все необходимые таблицы в вашей базе данных.

### Шаг 2: Инициализировать администратора

```bash
cd ../..
npx tsx scripts/cleanup-users-db.ts
```

Этот скрипт:
- Создаст `admin.demo@collabverse.test` в БД (если его нет)
- Настроит права администратора
- Удалит всех остальных пользователей (если есть)

### Шаг 3: Перезапустить сервер

```bash
pnpm dev
```

После перезапуска система будет использовать базу данных вместо памяти.

## 📋 Проверка после настройки

### 1. Проверка таблиц в БД

В SQL редакторе Vercel/Neon выполните:

```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public'
ORDER BY table_name;
```

Должны быть созданы таблицы:
- `user` ✅
- `account` ✅
- `session` ✅
- `userControl` ✅
- `organization` ✅
- `project` ✅
- И другие...

### 2. Проверка администратора

```sql
SELECT id, name, email, 
       "passwordHash" IS NOT NULL as has_password,
       "createdAt"
FROM "user"
WHERE email = 'admin.demo@collabverse.test';
```

Должен быть один пользователь с:
- ID: `00000000-0000-0000-0000-000000000001`
- Email: `admin.demo@collabverse.test`
- Пароль установлен: `true`

### 3. Проверка прав администратора

```sql
SELECT "userId", status, roles, "testerAccess"
FROM "userControl"
WHERE "userId" = '00000000-0000-0000-0000-000000000001';
```

Должны быть роли: `['productAdmin', 'featureAdmin']`

### 4. Тестирование входа

1. Откройте http://localhost:3000
2. Перейдите на `/login`
3. Войдите как:
   - Email: `admin.demo@collabverse.test`
   - Пароль: `admin.demo`
4. Проверьте, что вход успешен

### 5. Тестирование регистрации

1. Перейдите на `/register`
2. Заполните форму новым пользователем
3. Проверьте, что пользователь создан в БД:

```sql
SELECT id, name, email, "createdAt"
FROM "user"
WHERE email != 'admin.demo@collabverse.test'
ORDER BY "createdAt" DESC;
```

## 🔧 Команды для работы с БД

```bash

# Применить миграции

cd apps/api && pnpm db:push

# Генерация новых миграций (если изменили схему)

cd apps/api && pnpm db:generate

# Просмотр схемы в браузере

cd apps/api && pnpm drizzle-kit studio
```

## ⚠️ Важные замечания

1. **Миграции нужно применить только один раз** - после этого база данных готова к работе
2. **AUTH_STORAGE=db** - критически важно для использования БД
3. **Администратор** - создается автоматически скриптом `cleanup-users-db.ts`
4. **Новые пользователи** - регистрируются через форму `/register`

## 📚 Дополнительная документация

- **[QUICK_SETUP_GUIDE.md](./QUICK_SETUP_GUIDE.md)** - Быстрое руководство
- **[VERCEL_POSTGRES_SETUP_CHECKLIST.md](./VERCEL_POSTGRES_SETUP_CHECKLIST.md)** - Полный чеклист
- **[SETUP_SUMMARY.md](./SETUP_SUMMARY.md)** - Итоговая сводка

## ✅ После выполнения всех шагов

Система будет полностью работать с Vercel Postgres:
- ✅ Все данные сохраняются в БД
- ✅ Администратор может управлять пользователями
- ✅ Новые пользователи регистрируются через форму
- ✅ Все операции работают корректно

