# 🤖 Руководство по настройке OpenAI API для AI-функциональности

## 📋 Содержание

1. [Получение API ключа OpenAI](#получение-api-ключа-openai)
2. [Настройка проекта](#настройка-проекта)
3. [Конфигурация параметров](#конфигурация-параметров)
4. [Тестирование функциональности](#тестирование-функциональности)
5. [Управление затратами](#управление-затратами)
6. [Устранение проблем](#устранение-проблем)

---

## 🔑 Получение API ключа OpenAI

### Шаг 1: Регистрация на OpenAI

1. Перейдите на сайт: https://platform.openai.com/signup
2. Зарегистрируйтесь или войдите в существующий аккаунт
3. Подтвердите email адрес

### Шаг 2: Настройка биллинга

⚠️ **Важно:** Без настройки оплаты API не будет работать!

1. Перейдите в раздел **Billing**: https://platform.openai.com/account/billing/overview
2. Нажмите **"Add payment method"**
3. Добавьте кредитную карту
4. Установите лимит расходов (рекомендуется начать с $5-10)

### Шаг 3: Создание API ключа

1. Перейдите в раздел **API keys**: https://platform.openai.com/api-keys
2. Нажмите **"+ Create new secret key"**
3. Дайте ключу понятное название, например: `CollabStep Development`
4. **Скопируйте ключ немедленно!** Он показывается только один раз
5. Сохраните ключ в надёжном месте (менеджер паролей, защищённый файл)

Формат ключа: `sk-proj-XXXXXXXXXXXXXXXXXXXXXXXXXXXX`

---

## ⚙️ Настройка проекта

### Шаг 1: Создание .env.local файла

В корне проекта `apps/web/` создайте файл `.env.local`:

```bash
cd apps/web
touch .env.local
```

### Шаг 2: Добавление переменных окружения

Откройте `.env.local` и добавьте:

```bash
# ============================================
# OpenAI API Configuration
# ============================================

# Ваш API ключ (ОБЯЗАТЕЛЬНО)
OPENAI_API_KEY=sk-proj-ваш-реальный-ключ-здесь

# Включение AI функциональности (ОБЯЗАТЕЛЬНО)
NEXT_PUBLIC_FEATURE_AI_V1=true
FEATURE_AI_V1=true

# ============================================
# Опциональные настройки
# ============================================

# Модель OpenAI (по умолчанию: gpt-3.5-turbo)
OPENAI_MODEL=gpt-3.5-turbo

# Максимальное количество токенов в ответе (по умолчанию: 1000)
OPENAI_MAX_TOKENS=1000

# Temperature - контроль случайности (0-2, по умолчанию: 0.7)
# 0 = детерминированный, 2 = очень креативный
OPENAI_TEMPERATURE=0.7

# ============================================
# Rate Limiting (защита от переиспользования)
# ============================================

# Лимит запросов на пользователя в час
AI_RATE_LIMIT_PER_USER_HOUR=20

# Лимит запросов на endpoint в час для пользователя
AI_RATE_LIMIT_PER_ENDPOINT_HOUR=10

# Глобальный лимит запросов в час
AI_RATE_LIMIT_GLOBAL_HOUR=100

# Минимальный интервал между запросами (мс)
AI_RATE_LIMIT_MIN_INTERVAL=3000

# ============================================
# Ограничения стоимости
# ============================================

# Максимальная стоимость одного запроса (USD)
AI_MAX_REQUEST_COST=0.5
```

### Шаг 3: Проверка .gitignore

⚠️ **КРИТИЧЕСКИ ВАЖНО:** Убедитесь что `.env.local` в `.gitignore`!

```bash
# Проверьте что эта строка есть в .gitignore:
cat .gitignore | grep .env.local
```

Если нет - добавьте:
```bash
echo ".env.local" >> .gitignore
```

### Шаг 4: Установка зависимостей

```bash
cd apps/web
pnpm install
```

Это установит пакет `openai` версии 4.68.0

### Шаг 5: Перезапуск сервера разработки

```bash
# Остановите текущий сервер (Ctrl+C)
# Запустите заново:
pnpm dev
```

---

## 🎯 Конфигурация параметров

### Выбор модели

#### **gpt-3.5-turbo** (рекомендуется для разработки)
- ✅ **Быстрая** - отвечает за 1-2 секунды
- ✅ **Дешёвая** - ~$0.002 за 1000 токенов
- ⚠️ Менее качественные ответы

```bash
OPENAI_MODEL=gpt-3.5-turbo
```

#### **gpt-4** (для продакшена)
- ✅ **Умная** - лучше понимает контекст
- ✅ **Точная** - качественные ответы
- ⚠️ **Медленная** - 3-5 секунд
- ⚠️ **Дорогая** - ~$0.03 за 1000 токенов

```bash
OPENAI_MODEL=gpt-4
```

#### **gpt-4-turbo** (баланс)
- ✅ **Быстрее чем GPT-4**
- ✅ **Дешевле чем GPT-4** - ~$0.01 за 1000 токенов
- ✅ Хорошее качество

```bash
OPENAI_MODEL=gpt-4-turbo
```

### Настройка токенов

```bash
# Короткие ответы (200-500 токенов)
OPENAI_MAX_TOKENS=500

# Средние ответы (рекомендуется)
OPENAI_MAX_TOKENS=1000

# Длинные ответы (до 2000 токенов)
OPENAI_MAX_TOKENS=2000
```

💡 **Совет:** 1 токен ≈ 4 символа на английском, ≈ 2 символа на русском

### Настройка температуры

```bash
# Детерминированный (всегда одинаковые ответы)
OPENAI_TEMPERATURE=0

# Сбалансированный (рекомендуется)
OPENAI_TEMPERATURE=0.7

# Креативный (разные ответы)
OPENAI_TEMPERATURE=1.5
```

---

## ✅ Тестирование функциональности

### Тест 1: Генерация описания задачи

1. Откройте проект: `http://localhost:3000/pm/projects/[id]`
2. Нажмите **"Создать задачу"**
3. Введите название: `Создать REST API для пользователей`
4. Нажмите **"✨ Сгенерировать описание"**
5. Ожидайте 2-5 секунд
6. **Ожидаемый результат:** Поле описания заполнится структурированным текстом

### Тест 2: Суммирование комментариев

1. Откройте задачу с комментариями
2. Нажмите **"Суммировать комментарии"**
3. Ожидайте 2-5 секунд
4. **Ожидаемый результат:** Появится всплывающее окно со сводкой

### Тест 3: Чат с AI в проекте

1. Откройте вкладку **"Чат"** в проекте
2. Напишите: `@ai-assistant расскажи о статусе проекта`
3. Отправьте сообщение
4. **Ожидаемый результат:** AI-агент ответит в течение 5-10 секунд

### Тест 4: AI-агенты

1. Перейдите на вкладку **"AI-агенты"**
2. Нажмите **"Добавить агента"**
3. Выберите **"AI Помощник"**
4. **Ожидаемый результат:** Агент добавлен в проект
5. В чате напишите: `@ai-assistant привет`
6. **Ожидаемый результат:** Агент автоматически ответит

### Проверка в консоли браузера

Откройте DevTools (F12) → Console:

```javascript
// Проверьте что нет ошибок типа:
// ❌ "OPENAI_API_KEY is not set"
// ❌ "AI service is not configured"

// Должны быть логи:
// ✅ [AI Audit] { success: true, ... }
```

---

## 💰 Управление затратами

### Мониторинг использования

1. Перейдите: https://platform.openai.com/usage
2. Просмотрите:
   - **Текущие расходы**
   - **Использование токенов**
   - **Количество запросов**

### Установка лимитов

#### Hard Limit (жёсткий лимит)
1. Откройте: https://platform.openai.com/account/billing/limits
2. Установите **Hard limit** (например: $10)
3. API прекратит работу при достижении лимита

#### Soft Limit (мягкий лимит)
1. Установите **Soft limit** (например: $5)
2. Получите email уведомление при достижении

### Ожидаемые расходы

**При использовании gpt-3.5-turbo:**

| Операция | Токенов | Стоимость |
|----------|---------|-----------|
| Генерация описания задачи | ~500 | $0.001 |
| Суммирование комментариев | ~600 | $0.0012 |
| Чат с AI (1 сообщение) | ~400 | $0.0008 |
| AI-агент ответ | ~200 | $0.0004 |

**Примерный бюджет:**
- 100 описаний задач = ~$0.10
- 100 суммирований = ~$0.12
- 500 AI сообщений = ~$0.40

💡 **Рекомендация:** Начните с $5-10 для тестирования

### Оптимизация затрат

1. **Используйте gpt-3.5-turbo** для разработки
2. **Уменьшите MAX_TOKENS** для простых задач
3. **Включите rate limiting** (уже настроен)
4. **Кэшируйте** часто запрашиваемые ответы (будущая оптимизация)

---

## 🔧 Устранение проблем

### Проблема: "OPENAI_API_KEY is not set"

**Решение:**
1. Проверьте что файл `.env.local` создан в `apps/web/`
2. Проверьте что ключ начинается с `sk-proj-` или `sk-`
3. Перезапустите сервер: `pnpm dev`
4. Очистите кэш Next.js: `rm -rf .next && pnpm dev`

### Проблема: "Invalid API key"

**Решение:**
1. Проверьте ключ на https://platform.openai.com/api-keys
2. Создайте новый ключ если старый не работает
3. Убедитесь что нет лишних пробелов в `.env.local`

### Проблема: "Rate limit exceeded"

**Решение:**
1. Подождите 1 минуту
2. Проверьте usage: https://platform.openai.com/usage
3. Увеличьте лимит в настройках OpenAI

### Проблема: "Insufficient quota"

**Решение:**
1. Добавьте баланс: https://platform.openai.com/account/billing/overview
2. Проверьте что платёжный метод активен
3. Для нового аккаунта может потребоваться первый платёж ($5+)

### Проблема: Медленные ответы (>10 секунд)

**Решение:**
1. Переключитесь на `gpt-3.5-turbo`
2. Уменьшите `OPENAI_MAX_TOKENS`
3. Проверьте интернет соединение
4. Проверьте статус OpenAI: https://status.openai.com/

### Проблема: Некачественные ответы

**Решение:**
1. Увеличьте `OPENAI_TEMPERATURE` для креативности
2. Или используйте `gpt-4` для лучшего качества
3. Проверьте промпты в `apps/web/lib/ai/prompts.ts`

---

## 📊 Логи и мониторинг

### Включение детальных логов

В `.env.local` добавьте:

```bash
NODE_ENV=development
```

Логи будут в консоли сервера:

```bash
[AI Audit] {
  timestamp: '2025-11-16T10:30:00.000Z',
  userId: 'user@example.com',
  endpoint: '/api/ai/generate-description',
  success: true,
  cost: 0.001
}
```

### Мониторинг в продакшене

Для продакшена рекомендуется добавить:
- **Sentry** для отслеживания ошибок
- **DataDog** для метрик использования
- **LogRocket** для replay сессий с проблемами

---

## 🔐 Безопасность

### ⚠️ НИКОГДА не делайте:

❌ Не коммитьте `.env.local` в git  
❌ Не делитесь API ключом публично  
❌ Не используйте production ключ для разработки  
❌ Не храните ключ в коде

### ✅ ВСЕГДА делайте:

✅ Храните ключи в `.env.local` (игнорируется git)  
✅ Используйте разные ключи для dev/staging/prod  
✅ Ротируйте ключи раз в 3-6 месяцев  
✅ Настройте rate limiting (уже настроен)  
✅ Мониторьте необычную активность

### Ротация ключей

Если ключ скомпрометирован:

1. Создайте новый ключ на OpenAI
2. Обновите `.env.local`
3. Перезапустите сервер
4. Удалите старый ключ на OpenAI

---

## 📚 Полезные ссылки

### OpenAI
- **Dashboard**: https://platform.openai.com/
- **API Keys**: https://platform.openai.com/api-keys
- **Usage**: https://platform.openai.com/usage
- **Billing**: https://platform.openai.com/account/billing/overview
- **Documentation**: https://platform.openai.com/docs
- **Status**: https://status.openai.com/

### Документация проекта
- **AI README**: `apps/web/lib/ai/README.md`
- **Stage M Отчёт**: `docs/development/projects-master-guide.md`
- **Этот гайд**: `docs/AI_SETUP_GUIDE.md`

---

## 🎓 Следующие шаги

После успешной настройки:

1. ✅ Протестируйте все AI функции
2. ✅ Настройте мониторинг затрат
3. ✅ Оптимизируйте промпты если нужно
4. ✅ Переходите к Stage N (расширенная AI функциональность)

---

## 💬 Получение помощи

Если возникли проблемы:

1. Проверьте секцию [Устранение проблем](#устранение-проблем)
2. Проверьте логи в консоли браузера (F12)
3. Проверьте логи сервера разработки
4. Проверьте статус OpenAI: https://status.openai.com/

---

**Последнее обновление:** 2025-11-16  
**Версия:** 1.0  
**Stage:** M - AI-ассистент (базовая функциональность)

