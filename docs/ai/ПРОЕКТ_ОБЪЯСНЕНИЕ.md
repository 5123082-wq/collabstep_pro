# 📖 Что это за проект и как с ним работать

## 🎯 Что это такое

**Collabverse** — это платформа для управления проектами с функциями:
- CRM для проектов и задач
- Финансовый модуль (расходы, бюджеты)
- Маркетплейс услуг
- Система аутентификации

## 🏗️ Структура проекта

Это **монорепозиторий** (один репозиторий с несколькими приложениями):

```text
collabstep-new-3/
├── apps/
│   ├── web/          ← Frontend (Next.js приложение)
│   │   └── app/      ← Страницы и API routes
│   └── api/          ← Backend (WebSocket сервер, опционально)
├── docs/             ← Вся документация
├── scripts/          ← Вспомогательные скрипты
└── package.json      ← Корневой файл с командами
```

## 🛠️ Технологии

### Frontend

- **Next.js 14** — React фреймворк (основное приложение)
- **TypeScript** — типизация
- **Tailwind CSS** — стили
- **Zustand** — управление состоянием

### Backend

- **Next.js API Routes** — API endpoints (`apps/web/app/api/`)
- **TypeScript** — типизация

### Инструменты

- **pnpm** — менеджер пакетов (быстрее npm)
- **Jest** — unit тесты
- **Playwright** — E2E тесты

## ✅ Что нужно для работы

1. **Node.js 20** (у вас установлена версия 22, это тоже работает)
2. **pnpm 9+** (у вас установлена версия 9.15.9 ✅)

## 🚀 Как запустить проект

### Шаг 1: Установить зависимости

```bash
pnpm install
```text
(Вы уже это сделали ✅)

### Шаг 2: Настроить переменные окружения

```bash
pnpm ensure-env
```text
(Скрипт создаст файл `.env` с нужными настройками ✅)

### Шаг 3: Запустить проект

```bash
pnpm dev
```

Откроется на **http://localhost:3000**

## 📝 Основные команды

| Команда | Что делает |
|---------|-----------|
| `pnpm dev` | Запуск в режиме разработки (основная команда) |
| `pnpm build` | Сборка для production |
| `pnpm start` | Запуск production сборки |
| `pnpm test` | Запуск unit-тестов |
| `pnpm test:e2e` | Запуск E2E тестов |
| `pnpm verify` | Полная проверка (линт, typecheck, build, тесты) |
| `pnpm ensure-env` | Создать/обновить `.env` файл |

## 🔐 Демо-аккаунты

После запуска можно войти с демо-аккаунтами:

**Администратор:**
- Email: `admin.demo@collabverse.test`
- Пароль: `demo-admin`

**Пользователь:**
- Email: `user.demo@collabverse.test`
- Пароль: `demo-user`

## 📂 Где что находится

### Frontend страницы

- `apps/web/app/` — страницы Next.js
  - `(marketing)/` — маркетинговые страницы (главная, логин, регистрация)
  - `(app)/` — основное приложение (дашборд, проекты, задачи)
  - `api/` — API endpoints

### Компоненты

- `apps/web/components/` — React компоненты
  - `ui/` — базовые UI компоненты
  - `pm/` — компоненты для управления проектами
  - `finance/` — финансовые компоненты

### Backend логика

- `apps/web/app/api/` — API endpoints
- `apps/web/lib/` — библиотеки и утилиты

## ⚙️ Переменные окружения

Файл `.env` уже создан скриптом `ensure-env`. Основные переменные:

- `NAV_V1=on` — включить новую навигацию
- `APP_LOCALE=ru` — язык интерфейса
- `AUTH_DEV=on` — dev-режим авторизации
- `DEMO_*` — данные демо-аккаунтов
- `FIN_EXPENSES_STORAGE=memory` — хранилище расходов (memory для dev)

## 🐛 Если что-то не работает

### Порт занят

```bash
pnpm kill:ports  # Остановить процессы на портах 3000, 3001, 3002, 8080
```

### Очистить кэш

```bash
rm -rf apps/web/.next node_modules
pnpm install
```

### Проверить версии

```bash
node --version  # Должно быть 20+
pnpm --version  # Должно быть 9+
```

## 📚 Документация

- **[Быстрый старт](../getting-started/quick-start.md)** — детальная инструкция
- **[Настройка](../getting-started/setup.md)** — подробная настройка
- **[Архитектура](../architecture/system-analysis.md)** — как устроен проект

## 🎯 Что дальше

1. **Запустите проект:** `pnpm dev`
2. **Откройте браузер:** http://localhost:3000
3. **Войдите с демо-аккаунтом** (кнопки на странице `/login`)
4. **Изучите код:** начните с `apps/web/app/` для страниц

## ⚠️ Важные замечания

- Проект на **начальной стадии разработки**
- Используется **dev-авторизация** (не production-ready)
- Данные хранятся **в памяти** (перезагрузка = потеря данных)
- WebSocket сервер **опционален** (для real-time функций)

---

**Нужна помощь?** Смотрите документацию в папке `docs/` или файл `CHEAT_SHEET.md` для быстрых команд.

